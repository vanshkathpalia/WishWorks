{
  "name": "flipkart-autofill",
  "private": true,
  "//": "version/description/author exist for electron-builder, which refuses to build without a version and warns without the other two. The version is what shows in the installer and in Help > About, so bump it when you hand a new build to somebody.",
  "version": "1.3.0",
  "description": "WishWorks Seller OS — prepare listing images and fill the Flipkart form",
  "author": "WishWorks",
  "type": "module",
  "main": "out/main/main.js",
  "scripts": {
    "app": "electron-vite dev",
    "app:build": "electron-vite build",
    "app:dist": "electron-vite build && electron-builder --config electron-builder.yml",
    "typecheck:gui": "tsc --noEmit -p gui/tsconfig.json",
    "start": "tsx src/start.ts",
    "images": "tsx src/images.ts",
    "finish": "tsx src/finish.ts",
    "paste": "tsx src/paste.ts",
    "check": "tsx src/check.ts",
    "login": "tsx src/login.ts",
    "fill": "tsx src/fill.ts",
    "scan": "tsx src/scan.ts",
    "inspect": "tsx src/inspect.ts",
    "verify": "tsx verify-engine.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "electron-updater": "^6.8.9",
    "playwright": "^1.61.1",
    "sharp": "^0.35.3",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^5.2.0",
    "electron": "^43.2.0",
    "electron-builder": "^26.15.3",
    "electron-vite": "^5.0.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "typescript": "^7.0.2",
    "vite": "^7.3.6",
    "vitest": "^4.1.10"
  }
}
