# TICKET_STATUS — living ledger

> **Source of truth for where this project stands.** Update the instant a ticket's status
> changes.
>
> Related: [`CORRECTIONS.md`](CORRECTIONS.md) (mistake/diagnosis trail) ·
> [`NOTION_BOARD_SEED.md`](NOTION_BOARD_SEED.md) (board spec — **the board does not exist yet**;
> rewritten 2026-07-26 to match this file). `NOTION_SYNC.md` was deleted: a hand-regenerated delta
> goes stale faster than the ledger it summarises. **This file is the single source of truth.**

**Project reframe (2026-07-21, user-set).** This project is **not** a data-analytics /
seller-intelligence platform. It is: *automate our listing work, understand Flipkart and
Meesho properly, and make selling easier.* Tickets below reflect that. The old
`docs/architecture/` set carrying that framing was deleted 2026-07-26 (closes WW-020).

**Deviation from the spec, deliberate:** ticket titles use `[P_n][L_n]` without the `[ROLE]`
segment, because Vansh ruled out roles/tracks on 2026-07-19 (*"don't care about role part —
FE BE track and all"*). Everything else follows `NOTION_GUIDELINE` v2.

Legend: ✅ Done · 🔄 In progress · ⬜ Not started · ⛔ Blocked

---

## Phase 0 — Listing Factory

### Foundation

| ID | Title | Type | Status | Left at | Done |
|---|---|---|---|---|---|
| WW-001 | `[P0][L1]` Research Flipkart + Meesho listing APIs and constraints | Research | ✅ | Hard limit confirmed: no API creates new products. Flipkart needs an existing FSN; Meesho has no public API | 2026-07-19 |
| WW-002 | `[P0][L1]` Draft phase model and architecture docs | Docs | ✅ | Delivered, then **rejected as not useful** — see C-001. Superseded by WW-003 | 2026-07-19 |
| WW-003 | `[P0][L1]` Pivot to tools-first working rule | Chore | ✅ | Rule written into `CLAUDE.md`. `docs/learning/` note added | 2026-07-19 |
| WW-004 | `[P0][L2]` Review partner's Python bots (`flipkart_bot.py`, `listing_bot.py`) | Research | ✅ | Verdict: blind keyboard macros (Tab N times), Windows-only, cannot run on Vansh's Mac. Nothing adopted | 2026-07-20 |

### flipkart-autofill — the form filler

| ID | Title | Type | Status | Left at | Done |
|---|---|---|---|---|---|
| WW-005 | `[P0][L2]` Playwright scaffold + persistent login | Feature | ✅ | `npm run login`, profile saved to `profile/`, OTP asked once ever | 2026-07-20 |
| WW-006 | `[P0][L2]` Fix login cookie detection | Bug | ✅ | Guessed cookie names replaced with real ones — C-002 | 2026-07-20 |
| WW-007 | `[P0][L2]` Fix session lost between commands | Bug | ✅ | Shared persistent profile — C-005 | 2026-07-20 |
| WW-008 | `[P0][L3]` `npm run scan` — auto-discover a category's fields | Feature | ✅ | 71 fields captured for `balloon-decoration`. New category = zero code | 2026-07-20 |
| WW-009 | `[P0][L3]` Fix scan capturing 0 fields, then wrong labels | Bug | ✅ | Label-based targeting — C-003 | 2026-07-20 |
| WW-010 | `[P0][L3]` Fix combobox label regex | Bug | ✅ | `/^Search+Check/` never matched as intended — C-004 | 2026-07-20 |
| WW-011 | `[P0][L3]` `npm run fill` — type values with read-back verification | Feature | ✅ | ✅/⚠️/⏭️ per field. Can be wrong, never *silently* wrong | 2026-07-20 |
| WW-012 | `[P0][L3]` Fix false ⚠️ on multi-value fields | Bug | ✅ | Read committed chips, not `input.value` — C-006 | 2026-07-20 |
| WW-013 | `[P0][L4]` Category defaults + placeholder guard | Feature | ✅ | Refuses to run while `TODO_` placeholders remain | 2026-07-20 |
| WW-014 | `[P0][L4]` Non-technical handover: `START-HERE.md` + simple npm scripts | Docs | ✅ | `npm start` menu-driven; AI prompt writes the product `.json` | 2026-07-20 |
| WW-015 | `[P0][L4]` Verify Price/Stock/Shipping tab against the real form | Bug | ⬜ | **Never run against the live form.** Field names came from an older script — expect ⏭️/⚠️. Do not let it save until clean | — |

### Image pipeline

| ID | Title | Type | Status | Left at | Done |
|---|---|---|---|---|---|
| WW-016 | `[P0][L5]` Research image formats + marketplace specs | Research | ✅ | WebP → JPEG required; 1:1, ≥1000px, sRGB not CMYK, no watermark on main. Written to `docs/image-playbook.md` | 2026-07-21 |
| WW-017 | `[P0][L5]` AI prompts: inventory extraction + image edit + contents infographic | Feature | ✅ | Step 0 (JSON inventory, counts hand-checked) → Prompt A (edit, outpaint to square) → Prompt B (infographic) | 2026-07-21 |
| WW-018 | `[P0][L5]` Correct frame-fill + metadata claims in the playbook | Bug | ✅ | Both withdrawn/corrected — C-007, C-008. Confidence levels now stated inline | 2026-07-21 |
| WW-019 | `[P0][L5]` `npm run images` — deterministic spec gate | Feature | ✅ | 1500×1500 sRGB JPEG q90, renamed `<ID>-<n>.jpg`. Two bugs found by testing — C-009, C-010. Verified on 8 outputs | 2026-07-21 |
| WW-038 | `[P0][L5]` Test suite for the image pipeline | Feature | ✅ | 39 end-to-end vitest tests — real CLI against a temp dir. Covers formats, dimensions, squaring/no-distortion, crop scope, both stages, metadata content, ordering, safety. `npm test`. Paths made overridable (`WW_IMAGES_DIR`) so tests never touch working folders | 2026-07-21 |
| WW-039 | `[P0][L5]` Richer image metadata | Feature | ✅ | Tested all 14 candidate EXIF tags; only ImageDescription/Artist/Copyright/Software/DateTime/Make/Model actually write via sharp (XP* and XMP silently dropped). Description now composes per-image line + product name + keywords. Google's attribution pair included | 2026-07-21 |
| WW-040 | `[P0][L1]` Repo cleanup + doc rewrite | Docs | ✅ | Root reduced to CLAUDE.md + README.md + 2 dirs. Guides/reference/samples split under `docs/`. `THE-FLOW.md` rewritten for the 3-folder flow; `image-playbook.md` fully rewritten — it had gone stale (said Meesho serves WebP, referenced moved files, 3 contradictory metadata sections) | 2026-07-21 |
| WW-029 | `[P0][L5]` Crop scope + full-flow doc | Feature | ✅ | Crop defaults to **every** image (Vansh corrected mid-build — the tag is on all of them), `--crop-images=1,3` narrows it. Step 0 prompt now describes every uploaded image, not just two. `THE-FLOW.md` written for the non-technical partner | 2026-07-21 |
| WW-021 | `[P0][L5]` Measure the Meesho watermark crop offset | Feature | ⛔ | **Blocked on Vansh** — needs 3–4 raw `.webp` downloads to determine fixed-pixel vs percentage. `--crop-bottom=N` exists but the default is unmeasured | — |
| WW-022 | `[P0][L5]` Replace blog-sourced specs with the category Excel template's Guidelines sheet | Research | ⛔ | **Blocked on Vansh** — needs the balloon/party category template from the Meesho Supplier Panel. It outranks every source currently cited | — |
| WW-023 | `[P0][L5]` Resolve the image-metadata question | Research | ⛔ | **REOPENED — C-016 withdrawn, see C-019.** Measured: Meesho's `.avif` *does* carry EXIF, and `YCbCrPositioning` suggests it is passed through from the original upload, not regenerated. That image simply never had a description. Blocked on WW-037 | — |
| WW-037 | `[P0][L5]` Run the live metadata experiment | Research | ⛔ | **Deferred by Vansh — do NOT upload `METADATA-TEST-upload-this.jpg` as-is:** it is his partner's image with the Meesho tag still on it, so uploading would be a duplicate. Correct order: run the full pipeline on a real product first (convert → crop tag → AI recreate → metadata), upload *that*, then download the served `.avif` and check whether marker `WISHWORKS-METADATA-TEST-XK7Q9` (or the product's own description) survived. Settles WW-023 | — |
| WW-036 | `[P0][L5]` Accept `.avif` input | Bug | ✅ | Meesho serves AVIF, not WebP — every real download would have been skipped. Added `.avif/.heic/.heif`, verified on the real file — C-017 | 2026-07-21 |
| WW-035 | `[P0][L5]` Source higher-resolution originals | Chore | ⛔ | **Blocked on Vansh — highest business impact open item.** Website downloads are 512×512, half the 1000px zoom threshold. Get originals from the partner directly, or reshoot. No software fix exists — C-018 | — |
| WW-034 | `[P0][L5]` Test whether Meesho rejects non-square uploads | Research | ⬜ | Settles C-014. Upload a deliberately non-square test image in the Supplier Panel and see if it is refused | — |
| WW-028 | `[P0][L5]` Per-image metadata descriptions written by AI from both source images | Feature | ✅ | Step 0 prompt now takes image 1 + image 2 and returns an `images` block; product `.json` carries `"images": {"1": …, "2": …}`; tool writes each into that JPEG's EXIF, with product-level fallback. Verified: 3 images, 3 distinct EXIF strings — C-013 | 2026-07-21 |
| WW-041 | `[P0][L5]` `npm run finish` — clean-image shortcut | Feature | ✅ | For partner/AI images that are already clean (no tag, JPEG). Embeds descriptions + renames flat to `~/Downloads/wishworks-ready/<ID>.<n>.jpg`; **no resize/crop** (pixels untouched — marketplaces don't require 1500² from the seller). ID from folder name (`ANP 1 - p` → `ANP-1`). Shared EXIF helpers extracted to `src/image-meta.ts`; images.ts 50 tests still green. Verified on ANP-1: 2 files out, descriptions confirmed in-file, source intact. See `docs/learning/2-finish-shortcut-for-clean-images.md` | 2026-07-24 |
| WW-042 | `[P0][L5]` Meesho-residue metadata scan (both image tools) | Feature | ✅ | `meeshoResidue()` in `image-meta.ts` scans source EXIF/IPTC/XMP for `meesho`/`s-######`, warns ⚠️ in both `finish` and `images` (`--final` + stage 1). **Measured on a real Meesho `.avif`: no code in metadata — the `s-#####` tag is pixel-only, so the scan is cheap insurance, not the tag defence.** Also strengthened Prompt A/B in `image-playbook.md` with an explicit human corner-check for the tag | 2026-07-24 |
| WW-043 | `[P0][L5]` `npm run check` — read embedded descriptions back | Feature | ✅ | Finder/Preview don't show EXIF ImageDescription on macOS (`mdls` returns null), so there was no way to confirm ingestion. `src/check.ts` walks IFD0 directly (no new dep) and prints ✅/✖ per JPEG in `~/Downloads/wishworks-ready` (or `--in=` file/folder). Verified: ANP-1 ✅, ANP-2 ✖ (no meta file yet) | 2026-07-24 |
| WW-044 | `[P0][L5]` `finish` — pick which descriptions file, like `npm start` | Feature | ✅ | On a single folder, `finish` now shows a numbered menu of `image-meta/*.json` (folder's match tagged `← matches this folder`) + a "(none)" option, mirroring the product picker in `npm start`. `--id=` or a non-TTY skips it. Chosen file's name becomes the output ID. Verified in a pty: pick 1 → embeds, pick (none) → skips; 50 tests green | 2026-07-24 |
| WW-045 | `[P0][L5]` Main-image method — read the pack → build the decoration | Docs | ✅ | Evolved through live testing with Vansh (C-020, C-021). **Final:** ChatGPT, 2 messages — (1) read the "what's in the packet" sheet in text (sums total if none printed); (2) build the decoration honouring DISPLAYED counts, distinct items exact / latex spaced+capped, zero text in image; + optional 3rd operator-written count fix. Documented the count-fidelity ceiling honestly. `image-playbook.md` main-image section replaced (old Prompt A v1/v2 archived at foot), Which-AI table, pipeline diagram, squaring note + `THE-FLOW.md` Conversation 2 all updated | 2026-07-24 |
| WW-046 | `[P0][L4]` `.png`-save clarity in the docs | Docs | ✅ | ChatGPT hands back a `.png`; docs said "save as 1.jpg", inviting the avif→jpg rename trap the same pages warn against. `THE-FLOW.md` + `image-playbook.md` now say: save as `1.png`, delete the file it replaces (one per number), `--final`/`finish` converts it. Logged C-022 | 2026-07-25 |
| WW-055 | `[P0][L5]` Restore the true Net Weight on the test listing — and weigh a sealed kit | Chore | ⛔ | **Blocked on Vansh — do not leave this.** Test C of WW-054 set `Net Weight = 10000 g` on a live listing to prove declared weight does not move the displayed shipping fee. It doesn't — but the field is **not** inert. Meesho's *displayed* fee (buyer-facing, image-derived) and Meesho's *actual* charge (deducted at settlement) are two different numbers: **the courier weighs the parcel at pickup, and if it exceeds the declaration Meesho charges the difference back**. A 10 kg declaration left saved is also a compliance problem on a live listing. Action: (1) revert that listing to the true packed weight, (2) weigh one sealed kit on a kitchen scale — the `160 g` used elsewhere looks under-declared for a kit holding 40 latex balloons (~120 g) + a balloon pump (~60 g) + banner + 16 cutouts + LED, so likely 300–400 g, and under-declaring buys nothing since it does not lower the displayed fee either. Procedure in `SHIPPING-COST.md` §6. Keep photos of the packed parcel on the scale — weight-discrepancy charges are disputable from the supplier panel in roughly a 7-day window, and evidence is what wins them | — |
| WW-058 | `[P0][L4]` Clean the inventory CSV + Notion build spec for the inventory | Docs | ⚠️ | **Partial — the important tab was never exported.** Vansh reported "some fields aren't responding" in `inventory for distribution - Dropdown.csv`. Root cause found and it is one cell: the **Banner** row has an empty `specific material`, so its VLOOKUP against the material master returns `#N/A`, and that single `#N/A` propagates through the SUM into `SKU Cost = #VALUE!`. Nothing else is broken. Also diagnosed the "properties didn't come with it" complaint — **a Google Sheets CSV export is one tab only** (the `gid` in the URL) and carries neither formulas nor data-validation dropdown lists, only the values last computed; the 88 junk `#N/A` rows below the data are stale formula fill. Cleaned to `inventory for distribution - Dropdown.clean.csv`: 10 real lines, stray `","` literal column and trailing empty column dropped, error values blanked, header added. Check figures preserved — **quantities sum to 88** (matches the sheet's own total) and costs to **₹80.50 + 2 unknowns**. Confirmed the sheet's "cost of material" is a **line** cost, not a unit cost (20 balloons = ₹16, i.e. ₹0.80 each), which the old column name hid. The inventory build spec (originally `docs/guides/NOTION-INVENTORY-SETUP.md`, folded into `NOTION_BOARD_SEED.md` §WW-058 on 2026-07-26): 3 databases (`Materials` master / `Kits` per-SKU / `Kit Lines` junction) with relations+rollups replacing VLOOKUP+SUM, seed rows for MKU003, and a `Cost complete` flag — because Notion rollups *skip* blanks instead of erroring, so a missing cost would silently under-total where the spreadsheet at least screamed `#VALUE!`. **Still open:** the tab exported was **Dropdown** (kit builder), not **inventory calculation** (the stock master Vansh called most important) — so unit costs in the spec are derived, and stock/reorder columns are guesses until that tab is exported | 2026-07-26 |
| WW-057 | `[P0][L5]` `npm run metaprobe` — does Meesho read image METADATA? | Research | ✅ | **Answer: no. All five variants returned ₹63, identical.** Metadata has zero effect on the fee — closed permanently. `src/metaprobe.ts` decoded the source to raw pixels once and re-encoded every variant from those bytes, so all five were **pixel-identical** with metadata the only variable: M0 stripped (0 B EXIF) · M1 pipeline EXIF (454 B) · M2 300 DPI · M3 SubjectDistance + FocalLength (506 B) · M4 truthful weight/dimensions in the description (576 B) — verified distinct before upload. **Bonus finding, and the more valuable one: the estimator is DETERMINISTIC.** Five uploads of identical pixels gave the identical fee five times out of five, so there is no measurement noise — every earlier reading (₹54 · ₹63 · ₹89 · ₹256) was real signal caused by real differences in image content. The mechanism is consistent; it just isn't modellable. Caveat recorded: Meesho may cache by pixel hash, in which case the estimator never saw the metadata rather than ignoring it — same practical answer, not worth another test. **Two bugs caught pre-delivery by verifying instead of assuming:** `withMetadata()` writes a 186 B EXIF block even when empty (M0 was not actually stripped), and sharp addresses the Exif sub-IFD as **`IFD2`, not `ExifIFD`** — the wrong key made sharp silently drop M3's tags, leaving it byte-identical to M1 and the probe worthless. Both fixed and re-verified; 56 tests green | 2026-07-26 |
| WW-056 | `[P0][L5]` Meesho shipping optimisation — **CLOSED, no lever found** | Research | ✅ | Nine live A/B variants on one listing: ₹54 · ₹63 · ₹68 ×3 · ₹69 ×2 · ₹89 · ₹95 · ₹108 · **₹256**. Three hypotheses formed and each killed by the next test — *object count* (V2 balloons-only and V4 everything both ₹68), *size in frame* (tiniest image cost **more**), *density* (perfect rank order for six readings, then V8 was airier than the winner and cost **4×**). Values jump rather than slide, suggesting the image is **classified into a category** carrying a rate, not measured — that is the one untried thread (does the image auto-change the category/attributes?). Best honest result was **₹5/order** on an image ugly enough to lose more in conversion than it saved; worst roll ₹190 above baseline. **Stopped on Vansh's call** — *"an image that sells beats an image that saves ₹5"*, correct. `SHIPPING-COST.md` cut from a playbook to a short findings page (all V1–V8 prompts deleted deliberately, results kept so nobody re-runs this); `image-playbook.md` Message 2 restored as the winner on both looks and price, with its 85%-fill line and bare-wall SETTING (both added on unevidenced vendor advice) reverted. **One durable rule survives: read the shipping figure before submitting any main-image change — not to optimise, to catch a ₹256 before it goes live** | 2026-07-25 |
| WW-054 | `[P0][L5]` Meesho shipping fee = the main image. Settled by experiment | Research | ✅ | **Vansh proved it on a live listing and the assistant was wrong three rounds running (C-029).** Image → ₹54; swap back to the original image, nothing else touched → ₹68 (reverses, so not drift); Net Weight → 10000 g on the ₹54 listing → still ₹54 (declared weight is not an input at all). Meesho's form has no L/B/H and Size is just `Free Size`, so the listing-time estimate is derived from the photo. Flipkart's declare-weight-and-dimensions logic does **not** transfer — that error drove versions 1 and 2 of the guide. `SHIPPING-COST.md` rewritten around a per-listing procedure: generate 3–4 hero candidates → upload each → note the fee → keep the cheapest → log it. Candidate list led by **flat-lay of the deflated pack** (largest look-vs-ship gap in our catalog; no vendor tests it). Worth **₹14/order** at the observed spread. Also answers the original question: the fee recomputes on **every** main-image change, so re-test after any later swap. **Open for Vansh:** what was actually different about the ₹54 image? That detail seeds the prompt rule | 2026-07-25 |
| WW-053 | `[P0][L5]` Rewrite `SHIPPING-COST.md` — Meesho has no dimension fields (C-028) | Docs | ⚠️ | **Superseded by WW-054.** Correctly dropped the dimension-declaration premise but still treated declared weight as a lever; test C disproved that | 2026-07-25 | **Reverses WW-051/052's conclusion.** Vansh supplied the real Meesho listing form: no length/breadth/height anywhere (Flipkart asks, Meesho doesn't), declared Net Weight 160 g = cheapest slab, and he observes the displayed fee moving with the image alone. So Meesho must infer volume from category/attributes/image — the image claim is mechanically plausible and was rejected too hard. Doc rewritten around the distinction that dissolves the confusion: **displayed shipping** (listing-time estimate, hits the buyer's price — what Vansh cares about) vs **actual shipping** (measured parcel at settlement — what the old doc optimised). Now carries a one-variable A/B protocol (3 image variants: tighter crop / plain background / flat-lay of the deflated pack — the last being the biggest look-vs-ship gap in our catalog), a Net Weight under-declaration check, and the ÷5000 answer: no single public number, partners differ (Shadowfax + Ecom Express ÷4000 surface, Ekart ÷4500), plan at ÷4000. Vendor-recipe specifics still unevidenced. **Open for Vansh:** weigh a packed kit, run the 3-variant test, send the log — a winning variant becomes a rule in the image prompt | 2026-07-25 |
| WW-052 | `[P0][L5]` `SHIPPING-COST.md` v1 — measure-and-declare procedure | Docs | ⚠️ | **Superseded by WW-053.** Built on the assumption that the seller declares parcel dimensions to Meesho. True on Flipkart, false on Meesho — see C-028. The volumetric/slab-boundary material remains valid for *actual* settlement cost and for Flipkart, and survives in §6 of the rewrite | 2026-07-25 |
| WW-051 | `[P0][L4]` Research: what actually sets Meesho's "Shipping (added separately)" fee | Research | ✅ | **The image does not set it — claim rejected (C-027).** Every source saying Meesho derives volumetric weight from the main photo sells an image-cropping tool; fetched pages show no Meesho docs, no screenshots, no experiment. Disinterested sources (Shiprocket, Meesho supplier pages) describe `max(dead weight, L×B×H÷5000)` from *seller-declared* weight/dimensions + zone, reconciled against the courier's scale at pickup. Real levers for our kits, in order: package dimensions (light-but-bulky goods are volumetric-bound — flat mailer over box), accurate declared figures, and the rigid balloon pump forcing box height. Changing image 1 later does **not** recalculate shipping. Written up in `docs/learning/4-meesho-shipping-fee-is-not-set-by-the-image.md`. **Open for Vansh:** measure the packed parcel L×B×H and compare against the declared value in the supplier panel — that is where the ₹68 is decided | 2026-07-25 |
| WW-050 | `[P0][L5]` Hero image must contain only what is in the box | Feature | ✅ | ANP-1's staged bedroom hero shot had curtains in it, and the field-writing AI turned them into "2 red curtain backdrops" in Model Name, Materials and Description of a kit with no curtain (C-026) — nearly shipped a listing advertising an item the buyer would not receive. Fixed at both ends: `image-playbook.md` Message 2 now frames image 1 against a plain wall at ~85% fill with an explicit ban on curtains, furniture, plants, food, people and floor, and the lifestyle/room variant is restricted to images 3–5; `START-HERE.md` now tells the field-writing model that image 1 is a staged scene from which no pack item, material or `Decoratives Attached` entry may ever be taken, + a new checklist line. `ANP-1` product and image-meta files cleaned, `not_visible` now empty | 2026-07-25 |
| WW-049 | `[P0][L4]` Harden the merged prompt after the first two real runs (ANP-1) | Docs | ✅ | Audited two live responses for the Annaprashan kit. Structure held (two clean halves, no commas, no cross-category leak, title/keywords mirrored), and run 1's colour conflict was a wrong inventory file — run 2 with the correct inventory is consistent. Six prompt gaps found and closed in `START-HERE.md` + `image-playbook.md`: (1) **`not_visible` inverted** — flagged pump/tape/glue that ARE in the contents photo's corner sub-panel, missed the 2 curtains that are in no photo; rule now covers composite grids, sub-panel captions, and exempts setup aids from the hero shot (C-024). (2) **image-2 piece count** must open as a numeral with reject-cases named. (3) **`Key Spec`** = counts/measurements, never slogans. (4) **`Character`** = only wording genuinely printed on an item. (5) **`Balloon Type`** must list every balloon kind (metallic latex *and* foil). (6) **`Foldable`** defined — pack as a whole, "No" for balloon-led kits. `Shape` marked single-dropdown-value; `Description` must account for every inventory line incl. accessories. Six new pre-answer checklist lines. Corrected `products/ANP-1.json` (new) + `image-meta/ANP-1.json` (replaced the stale gold-and-white draft) written. **Open for Vansh:** are the 2 red curtains really in the pack, and Weight/dimensions still to be added by hand | 2026-07-25 |
| WW-048 | `[P0][L4]` One merged AI prompt (Excel-fed) → both JSONs | Docs | ✅ | START-HERE Part 3 rewritten: upload all photos + paste **Excel inventory rows** → one JSON with two halves, `products` (66 Flipkart fields) + `image-meta` (per-image descriptions/title/keywords). **Counts now come from the inventory, not AI re-counting the photos** (aligns START-HERE with the never-let-AI-count rule; photos are only for colour/appearance + the per-image descriptions). Replaces the old two separate prompts (66-field prompt + image-playbook Step 0b). Cross-category fields still excluded (Vansh's decision). image-playbook Step 0b + THE-FLOW Conversation 1 now point here. Meesho-only = keep just the `image-meta` half | 2026-07-25 |
| WW-047 | `[P0][L5]` `finish` simpler regime — whole-tree recurse, collision guard, `--square` | Feature | ✅ | (1) `--in` now walks any depth (whole `Whatsapp DW` root → every category → every listing) in one command via `finishTree`. (2) Two files at one position number (leftover `1.jpg` beside AI `1.png`) now STOP that listing loudly (`duplicatePositions` in `image-meta.ts`) instead of silently shifting — shared with `images.ts`. (3) `--square` opt-in pads/crops to 1:1 via new shared `src/square.ts` (extracted from images.ts; 50 tests still green). +5 finish tests (55 total). **Verified on the real tree: 6 categories → 29 flat files, sources untouched, and the guard caught a genuine collision in `HBD-space - p` (`1 ???.jpeg` + `1 ok.jpeg`).** `THE-FLOW.md` Flow B rewritten to the one-line command | 2026-07-25 |

| WW-062 | `[P0][L4]` Fold Meesho title/description/pack-contents into the one prompt | Feature | ✅ | Partner's view: Meesho ignores image metadata, so its SEO and CTR ride entirely on the product name + description text. Previously a separate ad-hoc prompt (*"Act like meesho expert and create the high CTR and SEO content based on 2nd image uploaded"*). Now a third part of the single START-HERE Part 3 prompt, so one paste returns Flipkart's 66 fields + per-image descriptions + Meesho copy. Written into `image-meta/<ID>.json` under a new `meesho` block — additive, so `descriptionsFor()` ignores it and nothing broke (56 tests green). Three values: **title** (target 70-95 chars, first 40 must carry the search phrase + piece count alone because Meesho truncates under the phone thumbnail; numeral piece count; no brand, caps, emoji, symbols or promo words), **description** (600-900 chars, short lines, "What you get:" with per-group counts + "Perfect for:", primary keyword 2-3× max, no contact details/guarantees/price — all rejection triggers), **pack_contents** (one whitespace-collapsed line, " - " separated, no commas — Vansh pastes inventory straight from Excel and it arrives full of stray tabs/newlines/double spaces). EXAMPLE json updated and measured: 83-char title, 646-char description, clean single-line contents. ⚠️ **Limits are UNVERIFIED** — see C-034 | 2026-07-26 |
| WW-063 | `[P0][L4]` Deterministic validator for the `meesho` block | Feature | ⬜ | Every rule in WW-062 is mechanically checkable (char counts, double spaces, commas in pack_contents, banned promo words, contact details, first-40 content). Per CLAUDE.md the deterministic half should validate and Claude should only write the copy — so this belongs in code, not in the prompt's honour system. Proposed `npm run meesho -- --id=<ID>`: print the three values ready to copy, flag every violation. Becomes a GUI tab later | — |

| WW-064 | `[P0][L5]` Seller's image advice — bracketed piece count, 20px border, 32KB cap | Feature | ✅ | Three observations from Vansh's partner. (1) **Title must end with "(Set of N Pcs)"** — done, folded into the WW-062 prompt as a non-optional rule with the count taken from the INVENTORY total and the front-loaded duplicate removed so it doesn't waste characters. (2) **20px border** — implemented opt-in as `--border=20` in BOTH `images.ts` and `finish.ts` via new `addBorder()` in `square.ts`; insets the picture so the output stays exactly 1500x1500 and nothing is cropped. 5 tests (61 total). Off by default because it is **unproven** — and the nearest existing data point argues against it (V5 "tiny in frame" cost ₹69 vs V1's ₹63). Ready for a clean A/B; the estimator is proven deterministic so one pair settles it. (3) **32KB cap — NOT implemented, and now CLOSED**: desk analysis showed it unreachable at 1500x1500 (quality 1 still yields 36-46KB) and that the only route, ~300x300, renders banner text illegible. Then **Vansh settled it properly with a live test — same image at a range of file sizes, fee did not move by one rupee.** File size is not an input to the shipping estimate. Pipeline unchanged, quality stays at 90. See C-035. (4) 1:1 ratio was already handled. `SHIPPING-COST.md` gained a section marking these as untested axes, NOT a re-run of the closed 14 | 2026-07-26 |

### Project system

| ID | Title | Type | Status | Left at | Done |
|---|---|---|---|---|---|
| WW-059 | `[P0][L2]` Pre-GUI code audit — path resolution, size cap, typecheck coverage | Bug | ✅ | Full read of all 15 `src/` files + tests before starting GUI work. Four real defects fixed, all verified. (1) **`listProducts()`/`loadProduct()`/`scan.ts` resolved data folders from `process.cwd()`** — they returned *empty* from any directory but the project root, and a packaged app's cwd is `/`. New `src/paths.ts` is now the single answer for all four data dirs (`WW_*_DIR` overrides preserved, `WW_CATEGORIES_DIR` added); proven fixed by calling `listProducts()` after `chdir('/tmp')` — was `[]`, now finds both products. **This was the hard blocker for any .app/.dmg build.** (2) **`finish.ts` had no 5MB guard** — it never resizes, so a big source at q95 could exceed Meesho's cap and be rejected at upload with no explanation; new shared `src/encode.ts` applies the same step-down to both image tools. (3) **`tsconfig.json` only included `src`**, so `tests/` and `verify-engine.ts` were never typechecked and were hiding a real error (`sharp.Region` used as a namespace off a default import); now included, `npm run typecheck` added. (4) `images.ts` told the operator to check `images/ready/`, a folder deleted in an earlier refactor. **56 tests + `npm run verify` (22 checks) green after** | 2026-07-26 |
| WW-060 | `[P0][L1]` Git: first commit + gitignore correctness | Chore | ✅ | Repo had **zero commits** despite ~3400 lines of working code — no undo, no history, one `rm` from total loss. Caught before committing: the machine's global `~/.gitignore_global` excludes `*.json`, which silently dropped `package.json`, `package-lock.json`, `tsconfig.json`, both `products/*.json` and both `categories/*.defaults.json` — the repo would have cloned and been **impossible to install**, with `git status` never mentioning them. Root `.gitignore` now re-includes project JSON (a repo `.gitignore` outranks the global) while keeping `profile/`, `image-meta/`, `.claude/settings.local.json` and credential patterns out. Verified: `profile/Default/Cookies` (live Flipkart session) confirmed ignored | 2026-07-26 |
| WW-061 | `[P0][L2]` `ensureProfileFree()` is macOS-only — silently no-ops on Windows | Bug | 🔄 | **Blocks the Windows app.** `connect.ts` finds leftover Chrome processes with `ps -A -o pid=,command=`, which does not exist on Windows: it throws, is caught, `pids` stays empty, and the cleanup does nothing. The symptom is the single most confusing one this file exists to prevent — Chrome comes up with **no session**, indistinguishable from "it logged me out", even though the cookies are saved and valid. Partner is on Windows, so this hits them and not Vansh. Needs a `tasklist`/WMI branch, and it must be **tested on the real Windows machine**, not assumed. Already fixed in passing: `execSync("sleep 0.3")` (also Windows-only) replaced with a cross-platform `Atomics.wait` sleep; the `SingletonLock` cleanup was already portable | — **Fixed in code 2026-08-03 (WW-108), still unproven on Windows — status stays 🔄 until the partner runs it.** `chromeHoldingProfile()` now branches: `ps` as before, and on Windows `wmic process where "name='chrome.exe'" get processid,commandline /format:csv`. **`tasklist` was the obvious choice and is the wrong one** — it prints image name and PID but *not* the command line, and the command line is the only thing separating our Chrome from the user's own; matching on image name would close their personal browser mid-work. The parsing is extracted as a pure exported `pidsHoldingProfile(lines, profileDir, platform)` and **tested against real fixtures of both formats**, because the failure here is silent by nature: return an empty list on a machine that does have a stale Chrome and everything looks fine until the app comes up logged out. The Windows fixture pins the one thing that is easy to get backwards — wmic's CSV is `Node,CommandLine,ProcessId`, so the PID is the **last** column and reading position 0 returns a hostname |
| WW-020 | `[P0][L1]` Reframe `docs/architecture/` away from data-analytics | Docs | ✅ | **Resolved by deletion, not rewriting** (WW-065). The eight-file set described a Postgres/Prisma/Fastify/npm-workspaces monorepo that was never built and never will be at P0, so reframing it would have been polishing fiction. Deleted 2026-07-26 | 2026-07-26 |
| WW-024 | `[P0][L1]` Build `docs/tracks/notion/` tracking pair + corrections ledger | Docs | ✅ | This file, `CORRECTIONS.md`, `NOTION_BOARD_SEED.md`. C-011 recorded. (`NOTION_SYNC.md` was part of this and was retired 2026-07-26 — WW-065) | 2026-07-21 |
| WW-065 | `[P0][L3]` Pre-GUI trim — delete dead code and fictional docs | Chore | ✅ | Repo weight audit before the Electron pivot. Reality check: only **3,666 lines of TypeScript** against **6,881 lines of markdown**, most of it describing software that does not exist. Deleted: `docs/architecture/` (8 files — a Postgres/Prisma/Fastify/workspaces design, never built, and the doc-first approach was itself rejected in C-001), `wishworks_seller_intelligence_platform_planning.md` (1,171 lines), `TECHNICAL_DEEP_DIVE.md` (920), `ADOPT_THIS_SYSTEM copy.md` (456), `NOTION_SYNC.md` + `NOTION-INVENTORY-SETUP.md` (a hand-regenerated delta and a guide whose design was folded into the board seed), `src/metaprobe.ts` + `photo/` (9.3 MB; metadata closed permanently per learning note 4, and the probe's own `RESULTS.md` said to delete the folder), a stray `.json.bak`, an empty `reference/legacy-bots/`, and 2.6 MB of loose PNG/JPEG at the repo root. **75 → 50 tracked files, ~4,400 doc lines gone, zero features removed.** `CLAUDE.md` rewritten: it had been advertising a Postgres/Fastify/workspaces stack that never existed, pointing at four files that no longer do, and omitting the GUI pivot entirely — it now states the real stack (tsx + Playwright + sharp + vitest, no DB, no server) and carries the A–D build order. All dangling cross-references repaired in `README`, `HANDOFF`, `SHIPPING-COST`, and both ledgers. **Near-miss, and the reason C-036 exists: the `--border` flag and `SHIPPING-COST.md` were deleted in the first pass and Vansh caught it.** "Closed" (metadata, file size, image content) is not "untested" (the 20 px border, per WW-064). Restored in full along with `inspect.ts`/`fill.ts`, which are how selectors get re-derived when Flipkart changes its form. **Then the same mistake a second time:** `NOTION_BOARD_SEED.md` was deleted as dead ceremony when in fact **the Notion board has never been built** — a spec for an unbuilt deliverable, not a leftover. Vansh caught that too. Restored *and* rewritten to current state (it was frozen at 2026-07-21: said "load C-001…C-011" when there are 36, Done list ended at WW-024, referenced four deleted files, and had no GUI pivot). `docs/samples/METADATA-TEST-upload-this.jpg` restored on the same rule — WW-037 is still open and that is its artifact. 61 tests + typecheck green, unchanged from baseline | 2026-07-26 |
| WW-066 | `[P0][L4]` **GUI pivot A** — make the engine callable (options in, structured result out) | Chore | 🔄 | **Image engine done 2026-07-27; browser CLIs deliberately not yet.** `src/images-core.ts` exports `runImages(opts, onRow?) → ImagesResult` and `src/finish-core.ts` exports `runFinish(opts, onRow?) → FinishResult`, `cleanId`, `numberedImages`. Both are pure: no `process.argv`, no `console.log`, no `process.exit`. `images.ts` and `finish.ts` are now flag-parsing + printing wrappers and nothing imports them. The two 10-arg / 8-arg positional functions the ticket named are options objects and are no longer exported at all — `runImages`/`runFinish` are the whole surface, so there is no wide positional call for a GUI to mis-wire. Three things fell out of the split, each of which the GUI needs anyway: **(a)** the stage-2 folder→description pairing is now returned as `pairings[]` and the missing-file stop as `blocked.missing[]`, so tab 4 can render WW-077's table itself instead of scraping stdout; **(b)** `runFinish` takes `id` (renames) and `metaId` (descriptions only, `"none"` to embed nothing) as separate options, so WW-078's bug is now unrepresentable rather than merely fixed — there is no code path where a descriptions choice can rename a listing; **(c)** `cleanId`/`numberedImages` are exported because tab 4 has to answer *"is this folder a listing, and what will it be called?"* before writing. `chooseMeta`'s unused `folderName` parameter dropped. Contract held: **63 tests, `npm run verify`, typecheck all green untouched** — the tests run the real CLI as a subprocess, so they prove the printed output is byte-identical, not just that the core works. **Left for WW-069:** `login.ts`, `scan.ts`, `fill.ts`, `check.ts`, `start.ts` are untouched — each gets split when its tab is built, per "don't build ahead". Only known behaviour change: the `Stage N:` header and the batch `Finishing every listing…` header now print after the run rather than before (same text, same order in stdout) | — |
| WW-066b | `[P0][L3]` **GUI pivot A, part 2** — split the browser CLIs the same way | Chore | ⬜ | The half of WW-066 left standing, split out so it is not blocking WW-067. `login.ts`, `scan.ts`, `fill.ts`, `check.ts`, `start.ts` still do argv parsing + `console.log` + `process.exit` inside `main()`. Same treatment, same contract: **no behaviour change**, proven by the existing tests + `npm run verify` + every `npm run …` passing untouched. `paths.ts`, `encode.ts`, `images-core.ts`, `finish-core.ts` are the shape to follow. Do each one when WW-069 builds its tab, not before — the shape of the result object should be decided by what the tab needs to draw, and guessing that ahead of the UI is how the wrong abstraction gets built. `fill.ts` is the one to be careful with: the ✅/⚠️/⏭️/❌ read-back must survive into the result as *structure*, not as pre-formatted strings, or the UI can only reprint terminal output | — |
| WW-067 | `[P0][L5]` **GUI pivot B** — Electron shell + step 1 (AVIF → JPG/PNG) + folder memory | Feature | 🔄 | **Shell + step 1 + folder memory + Settings all land 2026-07-31; not yet clicked through by a human.** Stack settled first and written up in `docs/learning/8-the-desktop-app-stack.md`: React 19 + plain CSS, electron-vite, electron-builder, living **inside `flipkart-autofill/`** (one package.json, one node_modules, `runImages()` is a relative import). Tauri was the real alternative and lost on one fact — a Rust shell still has to run `sharp` and Playwright, so it ships a Node sidecar plus a protocol to save a download that happens twice. The process boundary is the durable part and every later tab inherits it: **main is the engine, renderer is pure view, `gui/shared.ts` is the only contract, IPC carries paths and small JSON but never image bytes** (thumbnails are `file://` URLs). Contract held — **108 tests, `npm run verify`, `npm run typecheck` all green and untouched**; the GUI has its own `typecheck:gui` so the engine's typecheck still covers exactly `src`/`tests`/`verify-engine.ts`. **Three things found while wiring it, each a packaging bug avoided:** (a) `app.setName("WishWorks")` must be main's first line — Electron derives `userData` from the *package* name, `flipkart-autofill`, while `paths.ts` spells it `WishWorks` (WW-094), and the disagreement would surface as WW-061's "it logged me out" from a brand-new cause; (b) `paths.ts` does **not** survive packaging on its own — inside a package `import.meta.url` resolves into the read-only `app.asar`, so the `WW_*_DIR` overrides are pointed at a workspace under `userData` *before* the engine is imported, which is why images-core is loaded with `await import()` rather than a static import; (c) `runImages()` takes no input folder, so a dropped folder is **copied** into `<WW_IMAGES_DIR>/1-raw/<name>/` rather than read in place — the folder's name becomes the product ID and the originals are never touched, which beats editing tested engine code to save ten lines. Also: `File.path` was removed in Electron 32, so a dropped folder's path comes only from `webUtils.getPathForFile()` in the preload; an ESM preload cannot load sandboxed, so `sandbox: false` with `contextIsolation` still on; `asarUnpack` for `sharp`/`@img` is in `electron-builder.yml` from day one because packed in the asar the app launches and then dies on the first image. **One requirement deleted rather than built:** "format + quality controls" — the engine has one correct answer (JPEG 4:4:4, 1500², step-down under 5 MB) and PNG/a quality slider only make the output worse; reversible in one option if that call is wrong. **Verified so far:** build green, app launches with no stderr, and a smoke run through main's exact env-override path converted `docs/samples/2.avif` and produced the soft-source warning the table renders. **First click-through, same day, found four things — three fixed, one answered.** (i) **The picker could not select an image.** It was `openDirectory`, so standing inside `GTB-4` every `.avif` was greyed out. Two buttons now, *Choose images…* and *Choose a folder…*, because **Windows cannot show one dialog that accepts both** — it honours whichever property came first — and drag-and-drop takes either. No format work was needed: the engine already reads AVIF/WebP/HEIC/PNG/TIFF/GIF/BMP/JPEG, the dialog was simply refusing to hand them over. (ii) **The app was a wizard and must not be.** Every step now opens at any time and nothing gates anything — a run that dies halfway needs *one* step re-run, and most listings skip converting entirely because the photos already arrive as JPEG. Free navigation costs nothing because step state is derived from disk, not stored; a stored cursor would have been the thing that made order matter. Steps not yet built are still reachable and their panel names the command that does the job today. (iii) **The workspace folder was hardcoded** to `userData/workspace`; Settings can now move it. It **relaunches** on change, because `paths.ts` resolves its dirs into consts at import time and a fresh `import()` would not re-evaluate them — only the cache-busted URL reloads, not its static imports. Nothing is moved, so a wrong choice costs nothing. (iv) **"Does this work on Windows?"** — the app code is cross-platform and `electron-builder.yml` targets nsis, but **no `.exe` has ever been built or run; that is WW-068 and it is the next thing.** One Windows-only bug was found and fixed while answering: thumbnail `file://` URLs were built by splitting on `/`, which produces broken images for every `C:\Users\…` path. Still Windows-unproven: `connect.ts`'s `ensureProfileFree()` uses `ps` and is macOS/Linux-only (WW-061) — steps 8-9 only, nothing step 1 touches. **Left at:** Vansh clicks through the rebuilt app. Also unsettled — the spec numbers its own steps two different ways (login is Step 2 in one section, absent in the other); the rail numbers the nine listing stages and leaves login unnumbered at the foot. **Original spec, unchanged except where noted above:** Window, step rail (rest disabled), drag-and-drop a folder, format + quality controls, thumbnails and a result row per image with the existing warnings (soft source, Meesho residue, 5MB step-down). **Imports `runImages()` directly** — same process, same Node, no IPC serialisation of image data and no port. Smallest genuinely useful thing to hand the partner. Test input: `docs/samples/2.avif`. **Includes per-step folder memory**, ~20 lines that every later step inherits: each step that opens a picker has **its own** remembered path (converting reaches for `~/Downloads`, finishing reaches for the WhatsApp archive — one global "last folder" would have them fighting). First run has no memory and falls back to `~/Downloads`, the one folder that exists on every Windows machine. **Nothing is hardcoded** — `Whatsapp DW/GTB/GTB 1` is Vansh's layout on Vansh's Mac and the partner's will differ; never ship a path off a developer's disk. Stored as one small JSON in Electron's `userData`; it is a convenience cache, so **deleting it must leave the app working**. A Settings panel lists the remembered paths with a Clear button so a wrong turn is recoverable without hunting for a file | — |
| WW-068 | `[P0][L4]` **GUI pivot C** — GitHub Actions → the real `.exe` **and** `.dmg` | Chore | ⬜ | Unsigned installer built on CI, **deliberately early — this is a strategy, not a warning.** Packaging is the only part of this project whose failures cannot be seen from a Mac: `sharp`'s native `.node` binary shipping (or not, or for the wrong architecture), data folders resolving when the working directory is `/` rather than the project (`paths.ts` was written for exactly this and has never been proven *inside a package*), and SmartScreen flagging an unsigned build. None of those show up in `npm run …`, in the 99 tests, or on Vansh's machine — only the first time somebody double-clicks an installer on Windows. Building it after ONE screen means a failure has one obvious cause and everything after is built on something proven to ship; building it after nine means bisecting a packaging bug across weeks of work, on a machine you do not own, while the partner still has nothing. Same work, but the unknown gets answered while it is cheap. **Expect the first `.exe` to be broken — that is the point.** Document the SmartScreen "unknown publisher" warning for the partner so it does not read as a virus. **Builds BOTH targets from day one**: `electron-builder` produces `.exe` and `.dmg` from one config, so a mac/windows CI matrix costs a few lines. The `.dmg` is not a nicety — Vansh runs this flow daily and can read a stack trace, so bugs he finds on the Mac never reach the partner; shipping Windows-only means every bug is found by the person least able to describe it, on the machine nobody can inspect. Unsigned macOS builds hit **Gatekeeper** (right-click → Open once, or `xattr -d com.apple.quarantine`) — the Mac twin of SmartScreen, documented in the same place. Signing needs a paid Apple Developer account and is not worth it for two users. Distribution is a GitHub Releases link | —  **Add `electron-updater` here, not later.** Pointed at GitHub Releases it turns "push a fix" from a support conversation into a no-op: the partner opens the app and it is current. That is what makes the release loop affordable — **fix and test on the Mac, build a `.dmg`, confirm the *packaged* app works, only then push a partner build.** Most iterations never leave the Mac, because a `.dmg` catches the whole packaging class (native binary loading, `paths.ts` under a `/` working directory, read-only app folder). What it does NOT catch is platform-specific: a different `sharp` binary, `\` vs `/`, where Chrome lives — **WW-061 was Windows-only** — which is why ONE early `.exe` still has to run on the partner's real machine before iterating on the Mac with confidence. |
| WW-069 | `[P0][L5]` **GUI pivot D** — remaining tabs: login → images → finish → check → fill | Feature | 🔄 | **Five of eight steps built 2026-07-31, after Vansh saw the first draft and said the unbuilt steps were "just giving the commands there".** He was right — a panel that prints `npm run paste -- <ID>` is a screenshot of the problem, not a fix. Built: **hero image** and **infographic** (one button copies the whole prompt file — the prompt files are "nothing but the prompt" for exactly this reason, so it is `readFile` + clipboard with no parsing that could drift; plus a drop zone that files the returned `.json`), **listing copy** (both prompts side by side with the back-to-back-in-one-chat warning from WW-081, the WW-097 inbox importer inline, then every `paste` check rendered the moment the files land — lengths against their real limits, Model Name drift, banned and urgency words, commas, emoji — each value with a one-click copy), **finish images** (listing picker, **two sources** because Vansh asked for both: the listing's own clean folder or any other folder, since the AI's recreated hero and infographic arrive as downloads; the pairing sentence spells out that changing descriptions does not rename anything, which is WW-078 made visible), and **fill Meesho** (the three Meesho values, one click each, until WW-093 scans the panel). Required extracting **`paste-core.ts`** — `runPaste(id) → { halves, fields, problems }`, options in and nothing printed — with `paste.ts` reduced to a printer; **all 24 paste tests still pass unchanged**, which is the proof the output is byte-identical. Still commands: **check**, **log in**, **fill Flipkart** — all three drive a real browser or need WW-066b, and fill goes last on purpose because the ✅/⚠️/⏭️ read-back has to survive into the UI as structure and nothing may auto-save while a field reads ⚠️. Every picker in the app now remembers its own folder and nothing asks anyone to type a path or an ID. **121 tests + verify + both typechecks green**. **Original spec:** In that order. Login tab needs a **live "you're logged in" indicator** instead of the terminal's dots, and must distinguish *logged out* from *not yet navigated* (WW-061's exact failure mode). Fill-listing tab goes **last** — it drives a real browser against a live form, the ✅/⚠️/⏭️ read-back report must survive into the UI intact, and **nothing may auto-save while any field reads ⚠️** | — |
| WW-070 | `[P0][L2]` Model Name spec in the Part 3 prompt was writing a label, not a title | Docs | ✅ | Prompted by a GTB-1 title disagreement: the partner uploaded a 250-char Amazon-style title, Vansh a 77-char one. Root cause found while researching it — **Flipkart does not let a seller type a title at all.** It composes the title from Brand + Model Name + category attributes, so Model Name is the *only* lever, and the prompt was asking for `"Red Rose Anniversary Decoration Kit"` (35 chars) — most of the controllable title surface thrown away. Live competitors on the same query run 150-280 chars, but blog-sourced Flipkart guidance is consistent that stuffed titles are penalised and that attributes/description are indexed separately, so a full pack-list dump in the title buys no reach. New spec: 80-120 chars, search phrase inside the first 60, ends `(Set of N Pcs)`, no item-by-item counts (those live in Key Spec / Key Features / Decoratives Attached / Description). Checklist line added. Meesho section untouched — its 70-95 target already matches every source found. **All figures blog-sourced, none from Flipkart or Meesho documentation** (see C-007/C-008); WW-022 would settle them | 2026-07-26 |
| WW-071 | `[P1][L1]` "Search Keywords – 6 to 8 phrases" may exceed Flipkart's real field limit | Docs | ⬜ | Turned up while researching WW-070: two seller blogs put Flipkart's backend search-keyword field at **3** and **5** entries respectively, and both say it should hold terms *not already in the title*. The prompt asks for 6-8. Sources conflict and neither is Flipkart's own. **Do not count slots by hand — there is nothing to count.** `scan` records the field as `multi: true` ("Multiple values allowed. Press Enter or ','") and Flipkart publishes no cap on the label; a cap, if it exists, only shows when values are dropped. The existing read-back already measures exactly that: `fillField()` returns `status: "mismatch"` plus `actual` (the surviving pills joined by ` | `) whenever what landed differs from what was sent. **Test: `npm run fill ANP-1` on a draft — it carries 7 keywords, straddling both disputed numbers. ✅ on the Search Keywords line = no cap at 7, keep 6-8. ⚠️ = `actual` names the survivors and their count IS the answer. Do not save the draft.** Two prompt changes made now because they hold under every possible answer: keywords must be ordered most-valuable-first (so a silent truncation drops the cheapest), and must not repeat phrases already in Model Name — which WW-070 just made 80-120 chars, so the title now absorbs the top phrases and duplicates would waste slots. Untestable this way and still unverified: the blogs' claim that title-duplicate keywords are actively *penalised* rather than merely redundant — that is a ranking claim, not a form limit | — |
| WW-072 | `[P0][L2]` Flipkart Description: dull copy, wrong char limit, and `\n` in every paste | Docs | ✅ | Three complaints, three different causes. **(a) Dull.** Root cause was HARD RULE 5 — "plain, concrete, no marketing fluff" — plus a Description spec asking for "5 to 8 sentences". The prompt was doing exactly what it was told; the instruction was the bug. Rule 5 rewritten to ban only the *unverifiable* claim ("elevate your celebration", "unmatched quality"), and Description now has a full template: headline, 2 paragraphs, 🎁 What You Get, ✨ Key Features, 🎈 Perfect For, 💡 Why Choose This Kit?, 👉 closer — modelled on a hand-written GTB description Vansh confirmed he wants. **(b) 1400 chars.** Prompt said 5000. Vansh read 1400 off the live form; the panel wins (blogs say ~1000, none authoritative). Template measured against his example: **1422 chars**, so it barely fits — spec therefore carries a named trim order (2nd paragraph → Why Choose block → Key Features 5→4→3 → Perfect For 5→4) and three things that are never cut: headline, first paragraph, any What You Get line. **(c) `\n`.** NOT a prompt defect and no wording can fix it — a line break inside a JSON string *must* be escaped or the file will not parse. ChatGPT told him this was the AI "using /n instead of clean formatting"; that diagnosis is wrong. Fixed by adding a PART TWO **paste block** after the JSON: the same four values (Flipkart Description, Meesho title/description/pack_contents) reprinted unescaped, character-for-character, ready to copy. `START-HERE` now warns to stop at the closing brace when splitting the reply into files | 2026-07-26 |
| WW-073 | `[P1][L2]` Brand-collision rejection — first constraint from a live flag, not a blog | Docs | ✅ | Vansh hit two flags listing GTB-1, and the screenshot showed they are **not the same problem**, which changed the fix. **"elegant"** is an unverifiable quality claim → HARD RULE 6, a closed word list (premium, elegant, luxury, royal, exclusive, best, finest, 100% original, guaranteed, high quality). **"Golden Star"** is something else entirely: `This keyword is similar to brand "Golden Star". Please change it.` — an automatic check against *registered brand names*, firing on an ordinary colour beside an ordinary shape. A word list can never cover that; there are thousands of registered brands and no way to know which pairs are taken. So HARD RULE 7 is a **pattern**, not a list: never leave a colour or quality word directly in front of a noun as a bare two-word pair, because that is the shape a brand name has — always put the shape/material/function word in between. `"Golden Star"` → `"Star Shape Gold Foil Balloon"`, `"Golden Curtain"` → `"Gold Colour Fringe Foil Curtain"`. Costs nothing: the expanded form is also what buyers type. Applies to every field including all three Meesho values, with a phrase-by-phrase checklist entry. Rejected as harmful while drafting: a "never Title-Case" bullet — Model Name and the Description template are Title Case throughout, and capitals are not the signal, the bigram is. **First prompt constraint sourced from a live rejection rather than a seller blog; it outranks everything blog-sourced. Append every new flag here** | 2026-07-26 |
| WW-075 | `[P1][L2]` Prompt output restructured into three labelled sections | Docs | ✅ | Vansh asked whether to split Part 3 into three separate prompts (images → copy → Flipkart fields) and answered his own concern correctly: by message three the AI is working from *its own earlier summary* of the photos rather than the photos, which is precisely how invented items and wrong colours get in. Kept as one message, restructured into three dividered sections instead. Side benefit that decided the shape: sections 1 and 2 are now **standalone file-shaped JSON** — `image-meta/<ID>.json` (flat `title`/`keywords`/`images`/`not_visible`/`meesho`) and `products/<ID>.json` (`category`/`values`) — matching what `descriptionsFor()` at `src/image-meta.ts:145` already reads. The old single wrapper `{products:…, "image-meta":…}` had to be surgically split by hand every time, so this removes a manual step rather than adding one. Section 3 is the paste block. Added: an ordering instruction (decide Model Name and Search Keywords before writing section 1, since section 1 copies both), a table in "After the AI replies" mapping each section to its destination, and a warning never to save section 3 into a `.json`. **No tooling change — both file shapes are unchanged** | 2026-07-26 |
| WW-074 | `[P1][L2]` Is Flipkart's Description control a `textarea` or a single-line input? | Bug | ✅ | **ANSWERED 2026-07-29 by looking at the live form: it keeps the line breaks.** Vansh filled GTB-2 with `npm start` and read the field back off the page — blank lines, the 🎁/✨/🎈/💡/👉 section markers and the one-item-per-line What You Get list all intact. So `el.fill()` is safe here and **the bot fills Description; nothing needs pasting by hand.** Worth keeping the reasoning: the `✅` in the fill report did NOT prove this, because `norm()` at `src/fields.ts:337` collapses all whitespace before comparing — a `<textarea>` that preserved the layout and an `<input>` that stripped every newline report identically. **The only thing that could answer it was a human looking at the page**, which is exactly the class of question the ✅/⚠️ model cannot cover and worth remembering when designing the GUI's read-back | 2026-07-29 (corroborated 2026-08-03: the scan captured it as `Description · textarea`) |
| WW-076 | `[P1][L1]` Prompt moved out of START-HERE into its own file | Docs | ✅ | The prompt lived inside a ```` ```text ```` fence in the middle of `START-HERE.md`, so every use began with hand-selecting 417 lines out of a 220-line-longer document — a daily papercut and a real risk of clipping the first or last line. Now `docs/guides/PROMPT.md`: **the entire file is the prompt**, no headings, no fence, no notes. Select all, copy, done. `START-HERE.md` Part 3 is now a pointer plus the three send steps (upload images → paste file → replace the `<PASTE YOUR EXCEL ROWS HERE>` line). Cross-references repointed in `START-HERE` (×3), `THE-FLOW.md` and `image-playbook.md` (×4) — `THE-FLOW` also still described "one JSON with two halves" and was corrected to the three sections from WW-075. `CLAUDE.md` now names `PROMPT.md` and says to edit it there and nowhere else, since the one way this regresses is somebody pasting the prompt back inline | 2026-07-26 |
| WW-077 | `[P0][L3]` `images --final` embedded nothing, silently, when the folder name was off | Bug | ✅ | Unlike `npm start` (and unlike `finish.ts`, which already has a `chooseMeta` menu), `npm run images -- --final` takes no product argument — **the folder name IS the product ID**, and it is how `descriptionsFor()` locates `image-meta/<ID>.json`. Rename or mistype a folder and every image came out looking perfect with no metadata at all; the only signal was one line inside a results table, printed *after* the files were already written. Now stage 2 prints the resolved `folder -> description file` pairing **before writing anything** and **refuses to run** if any folder resolved to nothing, with `--force` to finish without metadata. Two things found while building it: **(a)** the obvious predicate — "no per-image descriptions" — was wrong and broke a working feature. A file that exists with only `title`/`keywords` and no `images` block is a *legitimate* fallback (`composeDescription` uses Model Name + keywords, and it is tested), so the guard keys on the FILE being absent, not on it being quiet. Same distinction as C-036: absent and empty are not the same trace. **(b)** `descriptionsFor()` returned no way to tell those apart, so `Descriptions` gained a `source` field (the resolved path, or `null`) rather than duplicating the four-candidate lookup in a second function where it would drift. A malformed JSON file also resolves to `null` and now stops the run — previously it was silently skipped, which is the last thing you want from a broken file. Tests: 61 → 63, with the two that pinned the old silent behaviour rewritten to assert the guard, plus new coverage for `--force` and for the pairing line. `finish.ts` checked and needs nothing — its single-listing path already asks | 2026-07-27 |
| WW-078 | `[P0][L3]` `finish`'s descriptions menu silently renamed the listing | Bug | ✅ | **Caught live, on real files.** Vansh ran `npm run finish --in=".../GTB/GTB 1"`, the menu asked *"Which descriptions file should I use for GTB 1?"*, the only file on offer was `ANP-1` (no `image-meta/GTB-1.json` existed yet), he picked it — and the tool wrote **Annaprashan descriptions into Groom-To-Be photos and named the output `ANP-1.1.jpg … ANP-1.4.jpg`**. Both products then sat in one flat `~/Downloads/wishworks-ready`, indistinguishable from a real ANP-1. Root cause: `id` did double duty — filename prefix *and* description lookup — so `id = chosen.id` at `finish.ts:339` let a descriptions answer rename the product. `finishListing()` already took name and descs as separate arguments, so the fix was to stop overwriting `id`: the menu now sets descriptions only, naming stays with the folder, and `--id` remains the one explicit rename. Menu rewritten to say what it is doing ("these photos will be named GTB-1.1.jpg … which descriptions go INSIDE them? (this does not rename anything)"), to mark every non-matching entry `⚠️ a DIFFERENT product` instead of leaving it unmarked, and — when nothing matches — to say outright that the right answer is *(none)*. A mismatched pick that is still deliberate now prints a Ctrl+C warning. **Same root cause as WW-077 one command over:** the product ID is inferred from a folder name and nothing checked that inference against reality. That is now two live near-misses from the same design, which is the strongest argument yet for WW-066…WW-069 — a GUI can show the pairing before anything is written instead of asking a question whose consequences are invisible | 2026-07-27 |
| WW-079 | `[P1][L1]` Write the GUI spec down in one place | Docs | ✅ | Vansh asked whether the app's tabs and features were being recorded anywhere. They were not — the thinking was spread across `CLAUDE.md`'s A-D build order, four ticket rows, and prose inside WW-077/WW-078. Now `docs/guides/GUI-SPEC.md`: who it is for, the four rules the app exists to enforce (each traced to a real failure, not style), a tab-by-tab spec in build order, an explicit **not in the app** list (no AI calls, no keys, no Meesho upload, nothing from Phase 2), and a testing table that separates "the WW-066 refactor changed no behaviour" from "the .exe runs on the partner's actual Windows machine". Records the stronger argument for the pivot: not that the partner cannot use a terminal, but that **the CLI let its own author silently produce wrong files twice in one day** (WW-077, WW-078). `HANDOFF.md` now links it and its stale next-steps list was corrected — items 1-5 were done or superseded, leaving only "ship one real listing end to end through QC", which remains the true done-check and still has not happened | 2026-07-27 |
| WW-080 | `[P1][L2]` The paste block made the prompt too big to answer — deleted, replaced by `npm run paste` | Docs | ✅ | Vansh pasted `PROMPT.md` into ChatGPT and got a refusal: *"I can't produce the requested output exactly as written… it exceeds my response limits"*, then a second refusal after agreeing to split it in two. The prompt asked for four values (Flipkart Description, Meesho title/description/pack_contents) **twice** — once escaped inside the JSON, once again as plain text in section 3 — for the single reason that `\n` cannot be pasted into a marketplace form. That duplicate is roughly a third of the reply, and it is the third that carries no new information. Section 3 deleted (WW-072/WW-075 introduced it). The unescaping now happens locally: **`npm run paste -- <ID>`** (`src/paste.ts`, 25 lines) reads `image-meta/<ID>.json` + `products/<ID>.json` and prints the same four values with real line breaks — `JSON.parse` already does the unescaping, so the AI never had to. Prompt is now **two** sections, both file-shaped JSON, nothing to copy by hand. `START-HERE.md` (section table, the "never save section 3" warning, the Meesho paste step) and `THE-FLOW.md` updated. Verified on real files: `npm run paste -- ANP-1` printed the Description; its `meesho` block predates the field and correctly showed `(missing)` rather than crashing. Typecheck clean, no test touched — nothing in the engine changed. **Worth noting the refusal itself was wrong** (the full output is a few thousand tokens, well inside any limit), but the fix stands on its own: the shortest correct prompt is the one that does not ask for the same text twice. **Same session, second ask:** deliver the two sections as **downloadable .json files named after the listing**, not text to select. The ID was already in front of the AI and unused — Vansh's Excel header row carries two codes (`WKU004  ANP003`) and the second is the listing. Prompt now reads the ID from that header and names the downloads `image-meta-<ID>.json` / `products-<ID>.json`; the prefixes exist only so the two don't collide in Downloads, and are dropped when filing. `START-HERE`'s paste example gained the header row (it was being left out, which is why the ID never arrived), with a text fallback for chats that can't attach files | 2026-07-28 |
| WW-081 | `[P0][L2]` ChatGPT silently truncated the listing JSON — prompt split in two | Bug | ✅ | **Caught live, on ANP003.** `npm run paste -- ANP003` printed `(missing)` for the Flipkart Description. `products-ANP003.json` was short **twelve fields**: `Description` (the 1400-char template, the single biggest field), `Safety Features`, all five Yes/No dropdowns, and every size field. The reply had simply stopped, mid-file, with no warning — the JSON still parsed, so nothing downstream noticed. `image-meta-ANP003.json` was complete, which located the failure precisely: **the answer ran out of room, not the prompt.** WW-080 read the earlier ChatGPT refusal as bogus and only trimmed the output by a third; that was not enough, and this ticket is the correction. Now two prompts, roughly half an answer each: **`PROMPT-meta.md`** (photos + inventory → `image-meta/<ID>.json`: image descriptions, `not_visible`, Flipkart title/keywords, all three Meesho values) and **`PROMPT-product.md`** (→ `products/<ID>.json`: the Flipkart form fields). `PROMPT.md` deleted. Three decisions worth keeping: **(a) same chat, back to back** — WW-075 rejected splitting because by message three the AI works from its own summary of the photos rather than the photos; that objection is about the *conversation*, not the file, so the files split and the chat does not. **(b) The photo-critical half runs first**, while the images are newest. **(c) `title`/`keywords` are decided in prompt 1 and copied verbatim into `Model Name`/`Search Keywords` by prompt 2** — the old file-order dependency, inverted, with "character for character, two versions is a bug" said in both files. The banned-words list and the brand-collision rule are duplicated in both files on purpose: each has to survive select-all-copy alone. `PROMPT-product.md` also gained a checklist line naming the fields a truncated answer loses first. Refs repointed in `CLAUDE.md`, `START-HERE.md`, `THE-FLOW.md`, `image-playbook.md` (×5), `GUI-SPEC.md`, `HANDOFF.md`. ANP003 repaired by hand from a follow-up ask: Description 1227 chars, 25 fields; `Width/Height/Depth/Diameter` correctly absent per HARD RULE 1 | 2026-07-28 |
| WW-082 | `[P1][L2]` Meesho copy was half-length and merged two inventory lines | Bug | ✅ | Three complaints on one live listing (ANP003), two real, one a misread. **(a) Description 470 of 1400 characters.** The prompt asked for 600-900 — a blog-derived target — and the AI came in under even that. Meesho reads **no** image metadata, so title + description are the entire basis for matching a product to a search; two thirds of the field empty is search surface given away. Target raised to **1100-1400** with a structure that earns it: a look-and-feel paragraph, material and size on every "what you get" line, "Perfect for" 3-5 → 5-7, and a "How to set it up" block. **(b) `pack_contents` merged two inventory lines** — "40 Metallic Balloons" where the inventory says 20 red + 20 gold. The colour is the search term *and* the thing that makes the line agree with the photos. Prompt now requires **one entry per inventory line, never merged**, keeps colour/material/shape words on every entry, and — when over the cap — says shorten the wording, never merge and never drop a line. **(c) "you confused Flipkart's limit with Meesho's".** Checked: `255` appears nowhere in the repo and `1400` was only ever applied to Flipkart, from Vansh reading the live form (WW-072). No confusion happened, but the underlying point stands — the Meesho figures were never anything but blog guesses. **Fact-check, five searches incl. Meesho's own Learning Hub: no published source states a Meesho description limit or a pack-contents limit at all.** Vansh's panel readings (1400 and 255) are therefore the only authority and are now recorded as such. On his partner's "longer titles rank better": no official source either way, and blogs lean *against* it — several report Meesho penalising keyword repetition. Nobody claims length itself is penalised, so the title target went 70-95 → **90-115** with the rule that every added word must be a NEW phrase, never a repeat. **`npm run paste` now prints `n/limit` per field** with ⚠️ for over-limit and for under 70% on the three fields where unused characters cost reach (not pack contents, whose length the inventory decides) — the limits become visible instead of remembered, which is what let a 470-char description ship unnoticed. START-HERE now carries a confidence-marked limits table | 2026-07-28 |
| WW-083 | `[P2][L1]` Meesho title needs pipe separators; both descriptions overshot their limits | Bug | ✅ | Follow-up on WW-082, all three caught by the new `npm run paste` counters. **(a) Pipes.** At 90+ characters a Meesho title is an unreadable run-on; Vansh wants `Kit \| Colours \| Items \| Occasion (Set of N Pcs)`. The prompt banned `* \| # % @` wholesale as spam markers — right about the others, wrong about the pipe, which is the standard separator on every Indian marketplace listing. `\|` is now REQUIRED as the separator (3-5 parts, piece count in brackets after the last pipe) and remains the only symbol allowed. **(b) Both descriptions blew their ceilings** on the regenerated copy: Flipkart 1615/1400, Meesho 1537/1400. Flipkart already had a documented cut order and the AI ignored it; Meesho had **none at all** — that gap is the actual defect, now fixed with its own order (closing lines → "How to set it up" → Perfect for 7→6→5 → shorten the look-and-feel lines), and "never drop a What you get line" carried over from the Flipkart rules. Checklist gained "over is worse than under — the form truncates silently, so the end of your text simply vanishes". **(c) User reported "old one is coming only".** Not a bug: `paste` reads the saved file, and the regenerated `meesho` block was still sitting in the chat window unsaved. Worth recording because it is the second time this shape of confusion has come up — the tool prints files, and a value that only exists in a chat reply does not exist. ANP003 now reads 1375/1400, 108/120, 1393/1400, 218/255, trimmed by the documented cut orders rather than by eye | 2026-07-28 |
| WW-080 | `[P0][L2]` One product, four filenames — every ID lookup was exact string equality | Bug | ✅ | ChatGPT names its downloads `image-meta-ANP003.json` / `products-ANP003.json`; a folder called `ANP 3` resolves to `ANP-3` via `cleanId()`. **The tool minted an ID by one rule and looked it up by another**, so `finish`'s menu offered `image-meta-GTB002` for folder `GTB 2` marked *"⚠️ a DIFFERENT product"*, and `npm run paste -- ANP-3` failed with an ENOENT. Four places matched by `===`: `descriptionsFor()`'s four hardcoded candidates, `availableMetaIds()`, `paste.ts`, `fill.ts`. Fixed once in `src/id.ts` — `normalizeId()` strips the download prefix then case/punctuation/leading zeros (`ANP 3` = `ANP-3` = `ANP003` = `image-meta-ANP003` = `ANP3`), `findById(dir, id)` does the lookup. All four now route through it, so **any of the shapes works anywhere**: `paste -- ANP-3`, `fill -- ANP003` (fill now takes a bare ID as well as a path), `finish` on `GTB 2`. `finish`'s menu now appears only when there is a real question — an unambiguous match prints `Descriptions: image-meta/image-meta-GTB002.json ← matches these photos` and proceeds. **Not fuzzy on purpose:** no edit distance, no prefix matching; two files that normalise the same are reported (`Descriptions.ambiguous`, shown by both CLIs) and never silently ranked — Vansh has `ANP003.json` beside `image-meta-ANP003.json` right now and one is stale. My first answer was *"rename your files"*, which is a per-product manual step forever; see C-038 — an instruction to rename files so the tool can find them is a bug report about the tool. Tests 63 → 73 | 2026-07-28 |
| WW-084 | `[P1][L1]` `paste` now catches Model Name / Search Keywords drift between the two files | Bug | ✅ | Found while fixing WW-080, in Vansh's own live data. `PROMPT-product.md` says in capitals that `Model Name` and `Search Keywords` must be the meta file's `title` and `keywords` **character for character** — *"two different versions of either one is a bug, not a variation"* — because Flipkart builds the listing title out of Model Name. Nothing checked it, and ANP003 had **three** different phrasings across `image-meta/ANP003.json`, `image-meta/image-meta-ANP003.json` and `products/ANP003.json`: three titles, three keyword sets, zero overlap on some entries. Send the two prompts twice, or hand-edit one file, and the title, the image descriptions and the Meesho copy each describe the same kit differently — with the keyword work split three ways instead of compounding. Invisible until the listing is live. `paste.ts` is the one place both files are open at once and it runs before every paste, so the check lives there: 10 lines, prints both versions side by side, warns and continues rather than blocking (the values still paste fine; it is the *listing* that is wrong, not the output). No test — it is a warning printer with no side effects, verified live against ANP-3 (fires, both fields) and GTB-2 (silent) | 2026-07-28 |
| WW-085 | `[P0][L2]` Two names for one product still asked a question; `finish` warned about a size Vansh had chosen | Bug | ✅ | Follow-up to WW-080, caught on a live `finish` run of GTB 2 — see C-039. **(a) Duplicate files were still a menu.** WW-080 made every spelling of an ID resolve to the same *product*, then stopped short: two files answering to it (`ANP003.json` beside `image-meta-ANP003.json`, which is exactly what Vansh has) printed *"one of them is probably stale"* and opened `chooseMeta`, and `npm start` listed both as separate products. That is the rename instruction again in a new costume — *delete one* — for a decision the tool can make itself: files that normalise to the same ID **are the same product**, so there is no wrong-product risk here (that is WW-078, a different branch), only *which copy is current*, and mtime answers it — the newest is the one you just downloaded. `findById` now sorts newest-first and returns the rest as `others` for display only; the exact-filename preference was **deleted** so there is one rule, not two (before this, `ANP003` resolved to the older `ANP003.json` while `ANP-3` resolved to the newer `image-meta-ANP003.json` — same product, two answers). `finish` prints `(ignoring older copy: …)` and asks nothing; the menu now appears only when **nothing** matched. `listProducts()` de-duplicates by `normalizeId`, so `npm start` shows one row per product. **(b) `finish` checked the wrong property of the image.** It warned `SOURCE ONLY 350px — get a larger original` on every run. Vansh does not want a larger original: Meesho prices shipping off the main image and a bigger one costs the buyer more (`SHIPPING-COST.md`), so the resolution is a decision. **First fix was wrong — I deleted the check outright**, reasoning that a folder reaching `finish` is one the seller has finalised. Vansh: *"I wasn't saying finalised in that way. I was saying finalised in the quality way… I want you to just check that it is a 1:1 image."* Re-aimed rather than removed: `NOT SQUARE <w>x<h>` when `w !== h` (silent under `--square`, which already fixes it), nothing about pixel count. **The ratio is never a deliberate choice; the resolution often is** — that is the line the first pass missed. `images.ts` keeps its size warning, where the input is a raw Meesho download and re-downloading is real advice. `finish` still never resizes without `--square`. **Neither warning ever blocked anything** — confirmed on the real run: all four GTB-2 images carry their EXIF (952 bytes, `ImageDescription` + Artist + Copyright, read back off `GTB-2.1.jpg`), and all four are 1:1 so the new check is silent on them. Tests 73 → 75: two new (non-square warns / small-but-square stays quiet, and `--square` suppresses it), `id.test.ts`'s exact-filename case rewritten as "every spelling gives the same file" | 2026-07-29 |
| WW-086 | `[P1][L2]` Small-image note back in `finish`, and `paste`'s warnings made unmissable | Bug | ✅ | Two halves of one complaint: *"it should give a similar warning at least, not blocking the flow… warning in the GUI flow too, but adding the metadata even then."* **(a) `finish` size note restored, re-worded.** WW-085 removed `SOURCE ONLY 350px — get a larger original` because the advice is wrong here (Meesho prices shipping off the main image, so a bigger one costs the buyer more) and retargeted it to `NOT SQUARE`. Correct on the advice, wrong to leave nothing: *deliberate* and *nobody noticed* still left the same trace. Now `SMALL 350x350 — fine if that is your shipping choice; otherwise soft when a buyer zooms`, plus `and under ~500px Meesho may reject it outright` below 500 (blog-sourced, worded as a maybe). **Prescribes nothing, never resizes, never blocks, metadata still embedded** — which is exactly the split GUI-SPEC now records: *the ratio warning says what to do, the resolution warning says what is true.* **(b) `paste` summary.** The over-limit check already existed and had already caught GTB-2's pack contents at **263/255** — but it printed inline, above ~1,400 characters of Description, so by the time you scroll down to copy it is off screen. All problems now collect into one `⚠️ N THINGS TO FIX` block **after** the values, each saying what to do (`the panel will silently cut the last 8 character(s) — trim it in image-meta/`), and a `✅ nothing to fix` line when clean. Missing values (a truncated reply, WW-081) join the same list. Exit code stays 0 throughout — the printed values are correct to paste either way. Tests 75 → 86: new `tests/paste.test.ts` (7) and four in `finish.test.ts` pinning that the note names the size, does **not** say "larger original", adds the rejection caution only under 500px, and is silent at 1254px | 2026-07-29 |
| WW-087 | `[P1][L3]` Buyer-language SEO rules into both prompts, and the mechanical half into `paste` | Feature | ✅ | Vansh brought a YouTube SEO summary plus ChatGPT's 18-point critique of the live GTB-2 listing and asked for a system, not a one-off edit. **Triaged rather than applied** — three items contradicted rules already paid for in rejections. **Taken: (a)** name items the way buyers search, not the way suppliers invoice — `Foil Letter Kit` → `Groom To Be Foil Banner`, `Mug Foil Balloon` → `Beer Mug Foil Balloon`. Single biggest gap, and free. **(b)** say WHERE, not just when: "room decoration", "hotel room decoration", "banquet hall" are high-volume queries the whole category forgets; added as a fourth of the "five questions" (what / who / **where** / what's inside / occasion) the description must answer. **(c)** drop padding "Colour", *but* only where a material or shape word already separates the colour from the noun — `Gold Colour Latex Balloons` → `Gold Latex Balloons` is safe, `Gold Balloons` is the HARD RULE 6 rejection shape. That tension is spelled out because the naive edit reintroduces the live rejection. **(d)** Key Features must state what a feature does, not restate its label. **(e)** attributes outrank prose — filters run on structured fields, so a blank one is a filter you never appear in. **Rejected, with reasons in the prompt: commas in pack contents and titles** (HARD RULE 2 — Flipkart splits list values on commas; this advice would have quietly halved every list value), and **urgency wording** ("limited stock", "trending") from the video — banned by HARD RULE 5 and a policy risk; ChatGPT flagged it too and was right. Both are now *explicitly* countermanded in the prompts, because the advice will be met again. **Code — everything in those rules a machine can verify, all in `paste`:** banned-quality-word and urgency sweeps across every value in BOTH files; comma detection in the fields where a comma is a defect (prose fields exempt); `TODO_` placeholders; **Model Name length (80-120, ceiling 128) — the single highest-leverage number in the listing and previously uncounted**; and keyword coverage, warning only when *most* keywords appear in neither description, with a matching new prompt rule so the check has something to enforce and does not fire on every run (learning note 7). Live results: GTB-2 shows 6 of 7 keywords orphaned + pack contents 263/255; ANP-3 shows the three-way Model Name drift. Tests 86 → 95 | 2026-07-29 |
| WW-088 | `[P0][L2]` Comma rule split by marketplace, and length checked at BOTH ends | Bug | ✅ | Two corrections to WW-087, both from Vansh. **(a) The comma rule was Flipkart's, applied to Meesho's text.** I rejected "use commas in pack contents" citing HARD RULE 2, then coded a checker enforcing it — but `meesho.pack_contents` is pasted into the Supplier Panel by hand and **never reaches Flipkart**, so Flipkart's list-splitting cannot apply to it. The prompt's own HARD RULE 2 had carried the same defect since it was written: it named `meesho.pack_contents` and justified the ban with *"Flipkart splits list values on commas"*, a reason false for that exact field. Now the rule states the boundary — no commas in `title`/`keywords` (Flipkart, split), commas normal in all three `meesho` values and the image descriptions — `pack_contents` uses `", "`, and `paste` tags each swept value with the side it belongs to. See C-040: when one file serves two consumers, a constraint without its consumer attached is unusable. **(b) Length now checked at both ends, against the prompt's own numbers.** Over-limit was caught; under-limit ran on a blanket 70% that was *looser than the prompts ask for*, so a reply could miss its stated target and pass. Each field now carries the prompt's floor and ceiling: Flipkart Description 1100-1400 (floor newly stated in `PROMPT-product.md`, and its checklist line rewritten to cover under as well as over), Meesho title 90-120, Meesho description 1100-1400, pack contents 0-255 — no floor, because its length is the pack's and warning on a small kit would be noise. Both live listings were under: GTB-2's Meesho description 1031/1400, ANP-3's 1082/1400 — invisible before this. Tests 95 → 99 | 2026-07-29 |
| WW-091 | `[P0][L3]` ~~GUI spike — EXIF write + JPEG quality in the browser~~ | Research | ❌ | **Not needed — the web platform it existed for was rejected the same day.** Kept as a record, not deleted, because the reasoning is what stops it being re-proposed. It was the gate on a Vercel web app: canvas strips EXIF (and embedding descriptions is the whole point of the finish step) and `canvas.toBlob` gives no chroma-subsampling control (`encode.ts` uses 4:4:4 on purpose — 4:2:0 smears exactly the saturated reds and golds these products are made of). A WASM mozjpeg would probably have solved both. **On Electron the question evaporates: `sharp` does both correctly today and is tested.** These were never real problems, only problems the browser would have created — which is itself the clearest argument for the platform choice. Also recorded from that day's analysis, so they are not re-proposed either: **Python** (needs a server, and libvips already outruns Pillow — rewriting 99 tests' worth of working code buys nothing) and **Cloudflare Workers** (V8 isolates, no native binaries, and forty 1500×1500 resizes would blow the free CPU budget, while uploading the user's own files to do work their laptop does instantly) | 2026-07-29 |
| WW-092 | `[P3][L4]` Browser extension for the seller panels — optional, not required | Feature | ⬜ | **Demoted from P1 once the platform settled on Electron, which fills both panels without it.** Kept open because the idea is sound and Vansh already runs one (*Flipkart Lens*), which proves a content script can reach the seller pages using the session the browser already has. Its advantage over Playwright is real: no separate Chrome profile, so `connect.ts` and every bug in HANDOFF §6.4-6.6 disappear — profile lock, cookie flush on Ctrl+C, the CDP attach Chrome ≥136 blocks. Its disadvantage is that it is a second deliverable, a second install, and cannot touch local folders, so it can never cover steps 1-7. **Revisit only if Playwright becomes a maintenance burden** | — |
| WW-089 | `[P0][L4]` **GUI** — the listing frame: selector, step rail, state derived from disk | Feature | ⬜ | The structural decision from 2026-07-29: **the listing is the top-level object, not the tab.** Pick a listing once at the top; all nine steps then operate on it. Six tabs each answering "which product is this?" separately is precisely the shape that produced WW-077 and WW-078. Left rail = the flow in `THE-FLOW.md` in order, **jumpable, not a wizard** — redoing step 2 after step 6 is normal and the pipeline supports it. **State is computed from the filesystem on every open, never stored**: `2-clean/<ID>/` exists → step 2 ✅; `1.png` present → the AI's hero replaced our `.jpg` → step 3 ✅; `findById` resolves both JSONs → step 5 ✅; and so on. A stored checklist drifts from the disk the moment anyone touches a folder, and rule 4 is "state you can see". The **only** stored state is steps 8/9 (uploaded to Flipkart / Meesho) in a per-listing `done.json`, because nothing on this machine can see a marketplace. ⚠️ means the step ran and something wants a look; it never blocks the next step, matching the CLI. Skipping is explicit on steps 3/4/5 and marks ⏭️, never ✅ — *done* and *not needed* must not leave the same trace (C-036). Spec: `docs/guides/GUI-SPEC.md` | — |
| WW-090 | `[P1][L4]` **GUI** — prompt panels for steps 3, 4, 5 with the `paste` checks inline | Feature | ⬜ | Vansh's ask: the AI steps must be in the app too, for both users, so the partner can do a whole listing alone. **The app still never calls an AI** — no keys, no credits (see GUI-SPEC *Not in the app*). Each panel is three parts: **(1)** a Copy button that puts that prompt file's entire text on the clipboard — the prompt files are "nothing but the prompt" for exactly this reason, so it is `readFile` + clipboard with no parsing to drift; **(2)** what to attach, on screen, including that `PROMPT-meta.md` and `PROMPT-product.md` go back to back in the SAME chat because the photos must still be in context (WW-081); **(3)** a drop zone for the returned `.json` that files it **under whatever name it arrived with** — `findById` already treats `image-meta-GTB009.json` and `GTB-2.json` as one product (WW-080), so the app must not reintroduce a renaming step. Step 5's panel then runs `paste`'s checks the moment the file lands and shows them inline: both length ends, Model Name drift, banned and urgency words, commas on the Flipkart side only. **That is the whole value** — today those problems surface only if you remember to run `npm run paste`. Built last of the GUI work: it is the only part with no CLI equivalent whose behaviour can be copied, and by then the frame (WW-089) and the checks it renders both exist | — |
| WW-093 | `[P0][L4]` **GUI** — `scan` the Meesho Supplier Panel, then fill it | Feature | ⬜ | **The one genuine unknown left in the GUI plan, and the biggest time saving in it.** Step 9 is 100% hand-typed today, every field, every listing. Corrects a planning error recorded in GUI-SPEC: Meesho was written up as needing a browser extension *"because it has no public API"* — but **the Flipkart path has never used Flipkart's API either.** `npm start` drives a real logged-in Chrome, and `fields.ts` targets fields by their visible label without knowing what site it is on, so the same engine fills Meesho once its fields are known. Work: **(1)** `npm run scan` against the Supplier Panel, exactly as was done for Flipkart's 66 — nobody knows yet what those fields are called or how many there are, and that is a live-page question no amount of reading answers; **(2)** map `image-meta/<ID>.json`'s `meesho` block (title, description, pack_contents — the values `paste` already prints) onto them; **(3)** the same ✅/⚠️/⏭️/❌ read-back, and **nothing auto-saves while any field reads ⚠️**. Until it lands, step 9's screen shows the `paste` output with a Copy button per value — today's flow minus the terminal, useful alone, and the fallback if the panel resists scripting. Note the shipping rule still applies at this step: **read the shipping figure before submitting any main-image change** (`SHIPPING-COST.md`) | — **Re-sized 2026-08-03 (C-046): this is engine work, not calibration.** The spec said `fields.ts` targets labels *"without knowing which site it is on"*, so Meesho was only a `scan` away. It is not — the row anchoring uses `[class*="EditAttributeItemWrapper"]` and three sibling selectors that are **Flipkart's own styled-component class names**, hard-coded. Against Meesho they match zero rows and every field reports missing. The work is: lift those four selectors into a per-site profile, find Meesho's equivalents with `inspect.ts`, *then* scan. Still far less than the browser extension this replaced, and **WW-106 depends on it** — until it lands, that flow's Meesho leg is the Excel file, which is why WW-106 already chose Excel first |
| WW-094 | `[P0][L2]` Chrome profile moved out of the project folder, before the GUI needs it | Bug | ✅ | Done ahead of WW-067 so level one is solid before level two starts. `connect.ts` kept the live Flipkart session at `<project>/profile`, which **cannot work in a packaged app** — that folder is read-only, and Chrome responds to a read-only profile dir by starting with **no session** rather than by failing. That is indistinguishable from "it logged me out" and is WW-061's exact symptom, so it would have surfaced as a mystery on the partner's machine. `PROFILE_DIR` now lives in `paths.ts` and resolves to the OS user-data directory — `~/Library/Application Support/WishWorks` / `%APPDATA%\WishWorks` / `$XDG_DATA_HOME` — **computed to match what Electron's `app.getPath("userData")` returns**, so the CLI and the app will agree on one location with no migration later. **An existing `<project>/profile` still wins**, so this machine keeps its login and nobody re-does an OTP; new machines get the right place. `WW_PROFILE_DIR` overrides both. Second bug fixed in the same edit: `connect.ts` computed its **own** `ROOT` from `import.meta.url` instead of importing `paths.ts` — a second path convention in one codebase, which is precisely C-032. Tests 99 → 104, new `tests/paths.test.ts` pinning the platform paths, the legacy preference and the override | 2026-07-29 |
| WW-095 | `[P0][L2]` Flipkart Description limit is 5000, not 1400 — we were using 28% of it | Bug | ✅ | **Found while answering WW-074 on the live form.** WW-072 had set the limit to 1400 on a single reading off the panel and the prompt has asked for 1100-1400 ever since; the real counter reads **5000**. Corroborated inside this repo: `HANDOFF.md` §6.3 records an early `scan` capturing **`0/5000`** as a junk label off this very form — two independent sightings against the one that produced 1400. Consequence: **every listing written so far uses about a quarter of the largest piece of search surface it has**, on the field the SEO review ranked as well-indexed. GTB-2's Description is 1139 characters in a 5000-character box. Fixed: `PROMPT-product.md` now targets **2500-4500 with a 5000 ceiling**, the trim order triggers at 5000, and the checklist says outright that the field is 5000 and an earlier version of the prompt was wrong. `paste.ts` moved to 2500/5000 for that field only — **Meesho's description stays 1100-1400**, they are different fields on different platforms and one fixture cannot serve both (the test fixture had to be split for exactly this reason). Guard against the obvious failure mode written into the prompt: *more is not padding* — fill it with items named in full with counts, places the kit gets used, occasions and setup detail, and cut any sentence that exists only to raise the number. **Method note for the ledger: 1400 came from one reading and stood for days.** C-007/C-008 already say blog-sourced numbers are the weak spot; this one was seller-sourced and still wrong, so a single reading of a live form deserves the same suspicion | 2026-07-29 |
| WW-096 | `[P0][L3]` Emoji in the Flipkart Description made every save return HTTP 500 | Bug | ✅ | **The first attempt ever to save a listing through this tool failed, and the emoji were why.** `Save` returned *"Could not save your changes"*; DevTools showed **HTTP 500** from `POST /napi/createProductV2/updatePartial` with a body carrying only a `txnId` and *"Internal Server Error"*. After a bot fill even **switching tabs** — normally an autosave — failed identically, so the draft was poisoned rather than the click being wrong. Three hypotheses were wrong first, each recorded in C-041: missing mandatory fields on the untouched tab (no — that is a 400 with field messages), a stale `requestVersion: 1` (backwards — if every update 500s the version never advances, so `1` was the symptom), and Playwright desyncing React state (checked: `fields.ts` never assigns `.value`, it uses `selectOption`/`keyboard.type`/`fill`). **Vansh's A/B found it**: hand-editing an existing listing and switching tabs saved fine, the same page after a bot fill did not — which located the fault in a value, not a mechanism. Deleting the Description saved instantly. Cause: WW-072's template used 🎁 ✨ 🎈 💡 👉 as section headings; those are 4-byte UTF-8 and Flipkart's storage throws on them. **Fixed:** `PROMPT-product.md`'s Description template is now plain ASCII — CAPITALS headings, hyphens, no emoji, no en-dashes, with the reason stated in the prompt so nobody reinstates them for looks. `paste.ts` flags emoji in any Flipkart-bound value (>U+FFFF plus the dingbat/misc-symbol blocks, so 3-byte `✨` is caught too; `–` `—` `₹` and smart quotes deliberately are not). Meesho values exempt — hand-pasted, never seen to break. Tests 104 → 108. **The emoji shipped 2026-07-26 and sat undetected for five days because the save step had never once been run** — a pipeline is only proven as far as the last step actually executed | 2026-07-31 |
| WW-102 | `[P0][L2]` The emoji 500 came back, because WW-096 fixed the prompt and not the files | Bug | ✅ | **"in the morning we fixed this, how? I want to know."** WW-096 rewrote `PROMPT-product.md` so new replies come back plain ASCII. It could not change what was already on disk, and **both listings written before it still carried 🎁 ✨ 🎈 💡 👉 in their Description** — `products/ANP003.json` and `products/products-GTB002.json`, ten characters each. Those are the files that get filled, so the next save 500'd exactly as before and read as a regression. A prompt fix is a fix for the *next* listing; it is never a fix for the ones already written, and nothing said so. Both files repaired and verified (`npm run paste` now reports 0 emoji for each; diff shows only the emoji and their trailing space gone — `🎁 What You Get` → `What You Get`, which is what the corrected prompt produces anyway). **`stripFlipkartEmoji()` added to `paste-core.ts`** with a *Remove the emoji for me* button on the listing-copy step, because this will recur for every pre-2026-07-31 file and any reply that ignores the instruction. Mechanical and safe: the emoji were section headings, `products/` is regenerable, and **Meesho values are deliberately untouched** — they are hand-pasted into a different panel and have never shown the problem | 2026-07-31 |
| WW-103 | `[P1][L2]` Is filling every attribute with "NA" beneficial? — researched, answer is no | Docs | ✅ | Vansh's partner types a value into every field, using `NA` where nothing applies (Hand Fan Type, Blowout Mouthpiece Material, Type of Batteries…). **Six searches: the completeness half is real, the `NA` half is counterproductive.** Multiple seller sources describe a Listing Quality Score with **attribute completion as its largest single component (~30 of 100)** and report LQS 70+ correlating with materially more visibility; the stated *mechanism* is filters — a filled attribute puts the product in the sidebar facet for that value, which is where a large share of browse traffic converts. **`NA` is not a facet value.** Nobody filters for "NA", so it earns none of that, adds no keyword, and the same sources say non-descriptive attribute data lowers the completeness score rather than raising it. Recommendation recorded: fill every attribute that has a **true** value (that is the 30 points, and it is real reach), leave genuinely inapplicable ones **blank** unless Flipkart marks them mandatory — where blank is not an option, `NA` is the least-bad answer and costs nothing beyond the slot. **Source quality is the same weakness as C-007/C-008: every figure is seller-blog-derived, none from Flipkart's own documentation**, so the numbers (30 points, LQS 70) are directional, not facts. Cheap live test if it ever matters: the panel shows a completeness/quality indicator — fill a draft's inapplicable fields with `NA`, read it, blank them, read it again. Five minutes, and it beats six blogs | 2026-07-31 |
| WW-104 | `[P0][L3]` Third click-through: a phantom listing, a check pointed at an empty folder, unexplained tags, an unflagged Meesho breach | Bug | ✅ | Four things Vansh found by using the app, none findable from the code. **(a) `ANP003 (1)` showed as its own listing.** Chrome's duplicate-download suffix survived `normalizeId` — `products-ANP003 (1).json` normalised to `ANP31`, not `ANP3`, so a second copy of one product became a separate row that no folder and no other half could ever match. One regex in `id.ts` (`/\s*\(\d+\)$/`) fixes it everywhere at once, because every lookup routes through that function; the stale file is now reported as a duplicate via `others` instead of impersonating a product. See C-042. **(b) The Check step defaulted to `workspace/images/3-final`** — the image pipeline's folder — while step 5 writes to `workspace/3-final`. Verified on the real machine: the first is empty, the second holds the finished JPEGs, so the default check read nothing. Corrected, and the step now carries **two remembered folders** on a radio (the finished folder / another folder) so a set from a partner's drive or an old `~/Downloads/wishworks-ready` is one click away and does not overwrite the usual one. **(c) The `copy` / `flipkart` / `images` / `finished` tags** in the listing picker said what exists but never what they meant — legend added inline plus a `title` on each. **(d) Meesho's `263/255` was a number in a pill and nothing else.** The copy step lists every breach in a *things to fix* block; the Meesho step showed the same counts silently, so the one field that was over its limit read like the ones that weren't. It now renders the same problems block, filtered to Meesho values — Flipkart's problems stay out, they are a different form. All 133 tests green, both typechecks clean | 2026-07-31 |
| WW-100 | `[P0][L2]` The app made its own Chrome profile, so it asked for a fresh login | Bug | ✅ | **WW-061's symptom for the third time, from a third cause — and this one WW-094 was specifically written to prevent.** `paths.ts` prefers a legacy `<project>/profile` so nobody re-does an OTP, and it finds it by resolving `ROOT` from its own file location. That is correct from `src/`, but the app's bundle lives in `out/main/`, so `ROOT` came out as `flipkart-autofill/out`, `out/profile` did not exist, and the app quietly created a **second** profile under `userData` with no session in it. Both folders were on disk at once; the CLI stayed logged in while the app asked Vansh to sign in again. Fixed in `gui/main.ts` by setting `WW_PROFILE_DIR` from a root the bundler cannot move (`import.meta.dirname/../..`, dev only — packaged there is no project folder, so it correctly falls through to `userData`). **The lesson is not about profiles: any `import.meta.url`-derived path is a different path once bundled, and `paths.ts` has three more of them.** WW-068 must check `IMAGES_DIR`/`META_DIR`/`PRODUCTS_DIR` inside a real package for the same reason — the env overrides already cover them in the app, but nothing has proven that yet | 2026-07-31 |
| WW-101 | `[P0][L2]` **GUI** — login gets out of the way; saved pages instead of hunting | Feature | ✅ | Vansh's ask: *"in the app for the first time they had to log in… after they have logged in, no need — hide it somewhere so it's not popping up in the eyes of the user"*, and *"it will be beneficial if we can navigate to the my listing page instead of going and selecting the product"*. **(a) Login is now sized to how much it matters:** once the session reads `yes` the whole panel collapses to one quiet line with a green dot; it only grows back when something is actually wrong. Logging in is a once-ever event and a permanent LOG IN panel is a standing reminder of a solved problem. **(b) Saved pages, and deliberately NOT a hardcoded route list.** The seller panel is a hash-routed SPA; nobody here has verified what `#listings` or a category form resolves to, and **a guessed link that silently lands on the dashboard is worse than no link because it looks like it worked** (C-007/C-008's rule applied to URLs instead of numbers). So the user navigates once — My Listings, a category's Add Listing form, a listing worth copying — types a name, and presses Remember; `openSession(url)` then opens Chrome straight there. It works for pages this code has never heard of, which is the point, and it answers the "copy a previous listing" ask without the app needing to understand that flow. Stored in `settings.json`; `writeSettings` now **merges**, because the earlier `chooseWorkspace` wrote the file flat and would have eaten them | 2026-07-31 |
| WW-066b | `[P0][L3]` **GUI pivot A, part 2** — split the browser CLIs the same way | Chore | ✅ | Done 2026-07-31 alongside the Flipkart tab, which is the order the ticket asked for: *"do each one when WW-069 builds its tab, not before — the shape of the result should be decided by what the tab needs to draw."* `src/browser-core.ts` holds **one module-level session**, because that is the single real difference between a CLI and a window: `npm start` is one straight line (open → wait → fill → report → save → exit), while each of those is a separate button press minutes apart, so the browser has to outlive the call that opened it. Surface: `openSession` / `sessionStatus` / `closeSession` / `fillListing` / `saveListing`. `SessionStatus.login` is **three-valued — `yes` / `no` / `unknown`** — because "not navigated yet" reported as "logged out" is exactly WW-061. `sessionStatus()` never navigates, so polling it cannot interrupt an OTP. The care the ticket asked for on `fill.ts` was applied to `listing.ts` instead, which is where the read-back actually lives: **`fillAll` gained an optional `onField` callback, and when it is given nothing is printed** — so the app gets `FieldRow{label,want,status,actual}` as structure while `npm start`, which passes no callback, prints byte-identically. **The ⚠️ guard moved INTO the engine**: `saveListing()` throws when `needsEyes > 0`, so it is no longer a disabled button a refactor could lose. `login.ts`/`scan.ts`/`fill.ts`/`start.ts` are untouched and still work | 2026-07-31 |
| WW-105 | `[P0][L2]` The infographic now shows **sizes**, not just counts | Feature | ✅ | Vansh's ask, with a competitor's image as the target: our image 2 was a flat grid of photos with `20 x Golden Balloons` under each, theirs is a bordered card per item carrying the count **and the dimension** — `10" (25 cm)` on a leader line down the side of the balloon, `3.3 ft (100 cm)` across the bottom of the curtain — under an `ITEMS INCLUDED` header and a `TOTAL CONTENTS: 60 PIECES` footer. Size is the question every balloon-kit buyer asks and the commonest reason a kit gets returned; a count with no dimension leaves them to picture their own. **We do not measure our own stock**, so `PROMPT-infographic.md` now answers in **two steps: a text table of sizes first, then the image.** Step 1 asks for the size Indian sellers most commonly list for each item type on Amazon.in/Flipkart/Meesho (10"/12" latex, 18" foil star, 16" foil letters, 3.3ft × 6.5ft fringe curtain, 5m arch strip) and to say *"not commonly stated"* rather than invent one for glue dots or tape — those cards then get no dimension line at all. Vansh reads and corrects the table before anything is drawn, which is the same shape as the main image's read-pack message and for the same reason: **a wrong number that arrives inside a finished picture is one nobody proofreads.** Step 2 is pinned to the confirmed table, and every sheet carries *"Note: Size may vary slightly."* Prompt file rewritten, `THE-FLOW.md` step 3 and `image-playbook.md`'s Prompt B section say it answers twice. **Amended the same day, after the first real run:** *"most commonly listed"* is the wrong question for these products, because nearly every one of them has two or three standard sizes — a champagne bottle foil sells at 12-14" **and** 32", a star foil at 12" **and** 18", latex at 5"/10"/12". Asked for the common one the model picks a side and prints it on the picture with no sign it chose. Step 1 now resolves size by a stated order — **the inventory line wins outright** (`2 x star foil 12 inch` is 12", never overruled), **size words are sizes** (`small`/`big` map to that item's two standards, and it must say which two it chose between), a single standard fills itself in, and **more than one standard with nothing in my line means it ASKS rather than picks** — with a source column per row so an assumed size is visible next to a stated one. THE-FLOW now tells Vansh to type the size into the inventory line where he knows it, which is the real fix: the pack list is the only place that knows whether we stock the small bottle or the big one | 2026-08-02 |
| WW-113 | `[P0][L2]` A value the form does not have anywhere reported the same as one on another tab | Bug | ✅ | Vansh, after the scan: *"just the warning flag should be good at each page telling what field are left or faulty"* — and the two are genuinely different things that looked identical. A fill reports **⏭️ other tab** for every label it did not find, which is correct and harmless for the ~73 fields living on the two tabs you are not looking at. **But four keys in the pricing defaults were on NO tab at all** (`Breadth`, `Length (unit)`, `Breadth (unit)`, `Height (unit)`), and those sent nothing, reported nothing different, and would never land on any pass. `loadProduct()` now returns `unmapped` — every label that matches no row in `categories/<category>.json` — surfaced on the Flipkart panel as a warning naming them and saying plainly that re-running will not help. **Silent by design when the category has never been scanned**, so an unscanned category is never accused of fields the tool simply cannot check, and a damaged scan file degrades to silence rather than blocking a fill. `TODO_` placeholders are excluded because `checkValues()` already blocks those and two warnings for one cause is noise. Also fixed the scan's own coverage maths, wrong in two ways that between them produced the misleading *"87 vs 66 — 21 extra, delete those lines"*: it now compares **rows** rather than attributes (Flipkart counts a field and its unit picker as ONE, while `attrs` excludes companions — so a fully captured tab showed 3 phantom gaps) and **ignores photo tabs** (`Image addition (0/5)` counts upload slots with no label and no control, which would sit in the total forever as 5 permanently missing fields). Real state after all this: Product Description and Additional Description are **complete**; Price/Stock is 17 of 21, the 4 being the package-dimension block | 2026-08-03 |
| WW-112 | `[P0][L2]` **GUI** — the Flipkart step is three tabs, and the app pretended it was one | Feature | ✅ | Vansh: *"there must be 3 options at the Flipkart listing, each for each tab — the JSON they are picking is the same but the fields they are picking is different"*. Exactly right, and the old panel hid it: one **Fill the form** button under a line of prose saying *"open the tab you want filled"*. Somebody who does not already know the form is three tabs reads that as one action that did not finish — and the result row **⏭️ N other tab** then looks like a failure rather than the other two thirds of the job. Three cards now, one per tab, each carrying its field count and what lives on it, a **Fill this tab** button and a ✓ once run. **Deliberately a label, not a mode:** `fillListing` still fills whatever rows Chrome is actually showing and takes no tab argument, so pressing the wrong card cannot fill the wrong tab — the worst case is a tick beside the wrong name, which the next run corrects. No engine change, no IPC change, no new state on disk. The counts (17 + 7 + 66) come from WW-111's scan and are orientation only, nothing branches on them; **Product Description being just SEVEN fields is the surprise worth printing**, since a person who does not know that cannot tell a finished tab from a broken one. **Skipped on purpose:** per-tab readiness ("12 of 17 ready"), which needs `scan` to record which tab each field came from — it does not, and that is a `--tab` flag plus a re-scan. Worth doing when the one-click flow (WW-106) needs to sequence the three passes itself; pointless before | 2026-08-03 |
| WW-114 | `[P0][L1]` The hero photo only ever had one layout — the garland top-left | Feature | ✅ | Vansh: *"right now the balloons are coming just on the left and upper side, maybe we also want it on the right side, so three sides on"*. True and it was hardcoded — `PROMPT-main-image.md`'s COMPOSITION line says *"a balloon garland FRAMING the top and one side"* and every listing we have shipped looks the same because of it. **Four layouts added as follow-up messages, not four copies of the hero prompt**, and that is the whole decision: the hero prompt is 36 lines carrying the counts rules, the no-props rules and the square constraint, all of which have been amended repeatedly (WW-070, WW-072, WW-082, WW-095, WW-096, WW-105). Five copies means the next amendment gets made once and missed four times. Each layout file is three lines — `PROMPT-layout-right.md` (mirror), `-both-sides.md` (three-sided frame), `-corners.md` (two diagonal clusters), `-arch.md` (floor to floor) — sent in the same chat right after the photo, so the read-pack list is still in context and every rule still applies. Shows as message **2b** on the Images tab, optional like the count-fix message. **Two of them repeat the count cap explicitly** (both-sides and arch), because a longer run is exactly where a model reaches for more balloons to fill it, and the counts are the one thing on that picture a buyer can hold us to. The panel warns to re-check counts and foil-letter spelling on each reply for the same reason `PROMPT-add-border.md` does: these are full redraws. No engine change, no new IPC — `extraResources` already ships `docs/guides/*.md` by filter, so the four files reach a packaged app with no config edit | 2026-08-07 |
| WW-115 | `[P0][L3]` **GUI** — what a kit costs, without twenty dropdowns | Feature | 🔄 | Vansh: *"we have an Excel where we calculate what the raw material is costing us and how much we should ship it at to get a profit, but that Excel is too slow because you have to go to each selection and press empty empty and then do the selection"*. Every line of a kit is a dropdown and a kit is twenty lines — and the counts are **already printed** on the sheet the partner sends. New **Inventory cost** panel: copy a prompt, attach that picture, drop the reply back, read the costed table. `src/inventory-core.ts` + `categories/materials.json` + `PROMPT-inventory.md`, 12 tests. **The design decision worth recording is the one that got reversed.** It was going to be OCR — `tesseract.js` was installed, proven working in Node offline against a bundled `eng.traineddata`, 173 ms per image. It was ripped out, because OCR only produces a *string*, and the hard half is deciding which price row `SILVER MATALIC BALLOONS` is. That needs a fuzzy matcher, and **a line priced off the wrong row is invisible in a total** — the one failure mode a cost sheet cannot have. Instead `PROMPT-inventory.md` is handed the price list and asked for those names back **character for character** (misspellings included: MATALIC, CONFETI, MATTALIC are in the real data and are the lookup keys), so matching is an equality test. Vansh then asked for OCR back as a *cross-check* on the AI; declined and explained: when OCR and the AI disagree you must open the image anyway, so **the image itself is the check** — it sits beside the table at equal size, which is both the verification and the thing that makes the panel look like something. **Three rules the panel enforces.** (a) A name in no price row is left **unpriced and counted**, never mapped to the nearest thing — `kit.unpriced` is printed next to the total with the words *"the total does not include them, so it is lower than the real cost"*, because a total that quietly skipped four items is the whole risk. (b) **Every row is correctable**, including one that matched, via a category-grouped dropdown of the whole list; an override always wins and is never re-scored. (c) Money is integer paise end to end. The list lives in `categories/` **because that needs no build-config change** — `extraResources` already ships `categories/*.json`, and `loadProduct()` only reads `<category>.*.defaults.json`, so it cannot reach the Flipkart form. CI's packaging gate now checks for it alongside the prompts and the defaults. Costing sits **outside the step numbering**, beside Log in, for login's reason: it is not a stage a listing passes through, and its output is the price that later goes *into* one — numbering it 8 would put it after the step it feeds. Also folded the `fileUrl` helper into `ui.tsx`; it was two identical copies and this was the third caller. **🔄 on two things, both from Vansh and both cheap:** the real price sheet has not been sent — `materials.json` today holds ONE kit's nine materials, unit prices derived as `line_cost / quantity` from the MKU003 export whose cost column is a `#N/A` VLOOKUP into a sheet nobody exported — and he has asked for its categories and names to be **cleaned**, which must happen in that file, since the prompt reads the list from there. And `priceAt()` is margin over **cost of materials only**; it knows nothing about marketplace commission, the shipping fee, GST or packaging, so the panel says in as many words that the number is a floor and not a listing price. Real formula lands when the sheet does. Sizes per material are WW-116 and deliberately not started | — |
| WW-117 | `[P0][L3]` The price list was cleaned, and matching moved out of the prompt into our code | Feature | ✅ | **Two reversals in one sitting, both Vansh's and both right.** *(a)* WW-115 shipped with the whole price list appended to `PROMPT-inventory.md` so the AI would return names verbatim and matching would be an equality test. His objection: *"we are ingesting that long as inventory ChatGPT, it will take more time — why not just do it here? this will give us more freedom in terms of saving the data properly, molding it"*. Correct on both counts — it sent hundreds of names up on every sheet, and it left the app owning no data. The prompt is now short and generic (*items in the words the sheet uses*, with `size` as its own field so a size is never welded into a name), and matching lives in `inventory-core.ts`. *(b)* **The price list itself was cleaned**, on his instruction: 121 materials, 10 categories. `KT` → **Curtain**; `Character` and `Kit` (both holding 5-piece sets) → **Themed Set**; `Extra`/`Net`/`LED` → **Decoration**; `Tape` → **Adhesive**; `Foil` → **Foil Balloon**; `pump` → **Equipment**; `Packing` → **Packaging**. One naming shape everywhere — `<Variant> <Item Type>`, Title Case, singular, so `Star Foil Golden` / `STAR FOIL GOLDEN` / `Moon Foil Silver` all became `Golden Star Foil`, `Silver Moon Foil`. Spellings fixed (CONFETI, MATALIC/MATTALIC, Champaign, Emogi, Pestle→Pastel, maniplant→Money Plant, doremon, mariyo, Paw petrol, anniversey, Annaprasan→Annaprashan, BANTING→Bunting). Two real duplicates merged: `RED BALLOON`/`red balloon`, and `happy birthday`/`HBD banner` — both Banner, both ₹10, one item listed twice. The `Empty/Empty` spreadsheet padding row dropped. **The mechanism that makes a rename safe is `aka`:** every one of the 76 renamed rows keeps its old name as an alias that still matches, because the partner's existing inventory pictures and every kit saved before today say the old thing, and *a cleanup that breaks yesterday's data is not a cleanup*. Proven: MKU003's real sheet, in its original spellings, costs to **₹90.50** with zero unmatched lines. **Matching is Dice over words, with plural-stemming and length-scaled edit distance** — `BALLOONS`/`Balloon` and `Metallic`/`MATALIC` must match or the tool is useless, while words under five letters must match exactly or `red`/`led` and `12`/`18` collapse. Exact word hits are counted before typo-tolerant ones and weigh slightly more, so `Blue Balloon` scores 1.0 on `BLUE Balloon` and 0.8 on `BLUE Dark Balloon`. Three bands, and they are states not decoration: `>=0.85` priced and quiet, `>=0.6` priced and **flagged** (`Silver Balloons` fits SILVER MATALIC and CONFETI SILVER equally — 80 vs 200 paise), below that **unmatched**. **And a fourth state that only exists because the real data has it:** `paise: null` for the **13 materials whose price cell is blank**, which the panel reports as *no price set* — a different problem from *not on the list*, with a different fix, and collapsing them would send somebody to add a row that is already there. Never folded in as ₹0. Kits are now **saved** (`WW_KITS_DIR`, workspace not `categories/`, gitignored) storing the reading and the corrections and **never the total**, so reopening re-costs at today's prices instead of freezing last month's. 28 inventory tests, 169 total. **Open and waiting on Vansh:** the 13 blank prices (listed in the panel's *what the price list still needs*), all 121 sizes (WW-116), and the real cost→price formula — `priceAt()` is still margin over materials only and says so on screen | 2026-08-07 |
| WW-118 | `[P0][L2]` A real reply put the colour in its own column, and "Balloons" priced as Black | Bug | ✅ | **Vansh pasted what ChatGPT actually gives him and asked whether WW-117's rename would coordinate with it. Measured rather than answered: the names did, the shape did not.** Two faults. *(a)* **The reply was a markdown table with a separate Colour column** — that is `PROMPT-read-pack.md`'s output, not the inventory prompt's, and read-pack is a different message for a different job. Fed the item name alone, `Balloons` matched **Black Balloon at 67%** and `Metallic Fringe Curtain` matched **Blue** Metallic Curtain over Pink, both on nothing but list order. Flagged rather than silent, so the guard held — but a flag on every line is a review step nobody will keep doing. `PROMPT-inventory.md` now says in as many words that the colour or wording goes **inside `item`**, with the reason (colour is the only difference between two rows, so a bare name fits thirty-four of them), demands JSON with **no table, no prose, no totals**, and asks for the assembly aids to be included — tape and glue dots cost money and that reply left them classed as something other than contents. THE-FLOW carries the same warning plus the one-line fix to type when a table comes back anyway. *(b)* **`Kitty Foil Balloon` scored 80% against `Kitty Foil` and got flagged for nothing**, because the extra word was the CATEGORY (`Foil Balloon`) spelled out. `scoreName` now drops words the material's category explains — **and only ones the row itself does not contain**, which is what stops it manufacturing confidence: a bare `Balloons` against `Black Balloon` is untouched, because that row does have "balloon" in it, and it is still ambiguous at 67%. Both halves pinned by tests against the **real shipped price list**: Vansh's pink-kitty packet now costs to **₹58.50 with 0 unmatched, 0 unpriced and 0 flagged**, and `Balloons` on its own must stay below the confidence line. 171 tests. **The general lesson, and it is WW-081's shape again:** a prompt is only correct against the reply it actually produces, and nobody had run this one | 2026-08-07 |
| WW-119 | `[P0][L1]` The inventory prompt's example was two items off the packet we tested with | Bug | ✅ | Vansh, reading WW-118's fix: *"mentioning this particular name can make the prompt confusing and direct towards that only — then ChatGPT may not pick the actual details but answer with this mentioned only"*. Correct, and worse than a generic bad example: the JSON skeleton said `{ "item": "Dark Pink Balloon", "qty": 20, "size": "10 inch" }`, and those were **the items off the very packet the fix was tested against** — so a blue kit could come back pink and read as perfectly plausible, with `20` and `10 inch` anchoring counts and sizes the same way. **A slot the model fills must not hold a value it can copy.** The skeleton is now `"<the item's full name>"` with a line saying the brackets are a description and every value must come from the sheet; the colour rule is stated structurally (*carry every word that tells this item apart from a similar one; if the sheet puts it in a separate column or a caption, fold it in*) with the reason kept, since that is what makes a rule stick. **The other prompts were checked and deliberately left alone**, which is the part worth writing down: `PROMPT-product.md` is full of `e.g.` and none of it is this bug — those are **field definitions offering a closed list** (`"Material" – from: Latex, Foil, Paper…`), which tell the model what a field means and cannot be echoed as "the answer" because no single answer is on offer. The nearest miss, `"Character" – e.g. ["Happy Birthday"]`, already carries three sentences of guard rail under it. One concrete value in a template is an anchor; several alternatives in a definition is a definition. `docs/learning/9-an-example-in-a-prompt-is-an-instruction.md` | 2026-08-07 |
| WW-120 | `[P0][L2]` The reply is a code block in the chat, and the panel only took a file | Bug | ✅ | Vansh, with a screenshot of what he actually got: *"if ChatGPT gives me JSON text just like this instead of giving me a JSON file, then we should be able to handle it — if I copy paste this JSON only in the session, it will fetch the rates also"*. Right, and the panel was built for the wrong route: **`PROMPT-inventory.md` asks for "one JSON code block", and that is exactly what came back** — a fenced block with a copy button on it, no download anywhere. Asking for a file is a step ChatGPT does not always offer, so the file was never going to be the normal case. **A paste box, above the drop zone rather than beside it**, because it is now the main way a reply arrives; the drop zone stays for a reply somebody kept. `extractJson()` takes everything between the first `{` and the last `}`, which handles the fence, a *"Here you go:"* before it and a sentence after, without knowing anything about markdown — and returns `null` rather than throwing, so a reply that says *"I could not read the image"* produces a sentence and not a stack trace. It **reads as you paste**, no button: the interaction is copy in the chat, click, paste, table. Both routes go through one `costFromText()` in `gui/main.ts` — a file is just text that came from disk — so a pasted reply and a saved file cannot behave differently, which is the kind of drift two near-identical handlers always develop. Pinned by tests on the **verbatim reply Vansh got** (all caps, empty `sku`, no `size` on any line, fence included): 0 unmatched, 0 unpriced, 0 flagged, **₹58.50** — the same figure the file route produces, from the same reply. 33 inventory tests, 174 total. Note the shape of the find: the prompt was right, the panel had assumed a different answer to the one the prompt asks for | 2026-08-07 |
| WW-121 | `[P0][L1]` Pricing had one method, and the business uses another | Feature | ✅ | Vansh: *"here we have margin system, I want one more system of adding direct X amount of money too — right now partner had advised 60rs addition to inventory cost and list"*. So the rule actually in use is **cost + ₹60 flat**, and the panel only offered a percentage. **Both are now shown side by side, always, with no mode switch** — and that is the decision, not the arithmetic. The two disagree more the bigger the kit gets (**+₹60 is 100% of a ₹60 kit and 20% of a ₹300 one**), so which one is right is a judgement nobody can make without seeing them together; a toggle would hide the one comparison that matters. Each card carries what it means — *keeps N% of the price as margin, so it scales with the kit* against *cost + ₹60 flat, which is N% of this price* — the second computed live, so the flat rule's real margin on THIS kit is on screen next to the percentage one. Both values are editable and both are saved with the kit (`flatPaise`, optional so kits saved before today still open, defaulting to 60). **Also fixed, and visible in Vansh's screenshot:** `input[type=number]` had never been styled, so on the dark theme the margin field rendered as a small white box with its value invisible — the panel appeared to show a blank input beside a price of ₹117. Styled with the rest of the form controls, tabular numerals. Both prices still say plainly they are **floors, not listing prices**: cost of materials only, no marketplace commission, shipping, GST or packaging. That remains the open item — the real formula lands when the sheet that computes it does | 2026-08-07 |
| WW-122 | `[P0][L2]` The listing ID came back as ANP003 when the kit was HBD-Kitty01 | Bug | ✅ | **WW-119's bug, in the two prompts WW-119 checked and cleared.** Vansh pasted an inventory whose `sku` was `HBD-Kitty01` and got back `image-meta-ANP003.json` — `ANP003` was the worked example in `PROMPT-meta.md` and appeared **four times** in that one section. Consequence if unnoticed: both halves of a listing filed under another product's ID, which is WW-078's failure (one listing's copy written into another's) arriving by a different route. **Why the first sweep missed it, and the lesson that generalises:** it looked for example values inside JSON *templates*, and this one is prose — *"named image-meta-<ID>.json — e.g. image-meta-ANP003.json"*. **A fillable slot is a role, not a syntax**; it is anywhere the model substitutes a real value, sentence or code block alike. Both filename lines now use `<ID>` with an explicit *do not substitute a code that appears in these instructions*, and the one illustrative value left (`ABC-123`) shows the transformation rather than offering a value. **The fix uncovered a second fault the anchor had been covering**: the ID rule still read *"the listing ID is in the inventory's own header row, which carries two codes"* — the Excel workflow. Since WW-117 the inventory is JSON from the Inventory panel with a clean `sku` field, and the prompt had never heard of it, so the model had no correct source to read and the nearest concrete-looking code won. The rule is now ordered — **JSON `sku` first**, header row second, *say so and stop* third — with "use it exactly as written, do not tidy it, do not pad or strip zeros" spelled out. `PROMPT-product.md` no longer re-derives the ID at all; it is told to reuse the first file's, character for character, since the two halves are matched to each other by that name. Checked: `HBD-Kitty01` and `HBDKitty1` both normalise to `HBDKITTY1` through `normalizeId`, so the halves still find each other. Learning note 9 amended — **it had recorded the incomplete sweep as a finding**, and an anchor is most dangerous exactly where the real instruction has gone stale, because there it is not competing with the truth, it is standing in for it | 2026-08-07 |
| WW-123 | `[P0][L3]` One `Height` was being typed into two tabs in two different units | Bug | ✅ | **Vansh photographed both dimension blocks and settled three open questions at once, one of them a live wrong value.** Ground truth: *Price/Stock → Package Details* is `Length` `Breadth` `Height` in **CM** and `Weight` in **KG**, and **the units are fixed labels, not pickers**; *Additional Description → Dimensions* is `Width` `Height` `Depth` `Diameter` in **inches**, plus `Weight` with a real `kg` dropdown and a multi-value `Other Dimensions`. **(a) WW-113's four "unmapped" keys are explained and deleted** — `Length (unit)`, `Breadth (unit)`, `Height (unit)` addressed controls that do not exist, so they could never have filled anything. **(b) The collision WW-111 suspected is real and was shipping.** `Height` and `Weight` exist on BOTH tabs, `loadProduct()` merges every defaults file into ONE flat map keyed by label, so `balloon-decoration.pricing.defaults.json`'s `Height: 8` went in as 8 cm on one tab and **8 INCHES (20 cm) on the other**, and its `Weight: 0.16` overwrote a parcel Vansh has since measured at **0.250 kg** — both inherited from the other seller's bot this file came from (WW-015) and never once compared with the form. All eight dimension keys are now **gone from the pricing defaults**, so the bot cannot type a wrong value into Package Details at all; Vansh fills those four by hand, which is what he asked for, and the panel prints them ready to copy. Splitting them properly needs `scan` to record a field's tab, which it does not (WW-112) — deliberately not built for four fields. **(c) `src/packaging.ts` + `categories/packaging.json` compute the parcel from the kit**, because the size depends on what is in it: base **20×15×4 cm / 250 g** (measured), **a net makes it 6 cm tall and +40 g**, **a pump makes it 22 cm long and +50 g** (Vansh's figures; the net's grams are a flagged ESTIMATE he could not weigh). Rules match **material rows, not keywords** — "net" as a substring would fire on a future "Netted Curtain" — and take the **largest** of each dimension rather than the last rule to fire, so the answer cannot depend on array order. **Volumetric weight (L×B×H÷5000) is computed and compared**, since couriers bill the greater: a plain kit is 240 g volumetric against 250 g real and bills on real, but **a net kit is 360 g volumetric against 290 g real and bills on volume** — printed on screen rather than discovered at settlement. Over Vansh's 490 g ceiling warns and shrinks nothing. **Deliberately code and not a prompt**, which was his suggestion: it is arithmetic over data the app already holds, CLAUDE.md's rule is that deterministic code computes, and a model would get it wrong silently and untestably. 12 new tests, 186 total — including one asserting the pricing defaults carry no dimension key, so the collision cannot come back, and one asserting every packaging rule names a material that still exists, so a rename cannot silently stop a rule firing  **REOPENED AND PROPERLY FIXED 2026-08-12.** The 2026-08-03 fix was a workaround: delete the colliding keys and fill Package Details by hand. That held only while nothing needed a per-tab default — and then Vansh asked for the inches Dimensions block to be universal (*"lets make it universal for every listing that we do"*), which is impossible under one flat map, because `Height: 1.5` meant as inches is 1.5 CENTIMETRES in Package Details. `loadProduct` now takes a `DefaultsTab` and loads ONE defaults file: `categories/<cat>.defaults.json` is the Additional Description tab, `.pricing.` the Price/Stock tab, `.description.` the Product Description tab. The three Fill buttons pass theirs; `npm run fill` passes nothing and merges everything exactly as before, because the CLI has never known which tab you are on. Width/Height/Depth/Diameter are now stored universally in inches and **`Weight` deliberately is not** — it is the number a courier re-measures and bills back, it differs per kit, and it comes paired with a unit picker that `packaging.ts` sets alongside it. 4 tests |
| WW-124 | `[P1][L1]` A kit's cost said nothing about what a sale actually leaves | Feature | ✅ | Vansh: *"I want to add another field wherein I will add what was the delivery shipping cost in Meesho and in Flipkart… because I want to see how much of the total selling cost is the delivery cost, because that depends on which SKU we will put the ads on."* The panel could say what a kit cost and what it might sell for, and nothing about the gap between them — which is the number that decides ad spend. A row per marketplace: **listed price, delivery cost, delivery as a share of the price, and what is left after materials + delivery.** **Both inputs are typed, and both have to be** — Meesho sets its fee from the main image by a rule fourteen tests failed to pin down (`SHIPPING-COST.md`), and the same kit is rarely listed at the same price on both, so a single shared price would make the percentages incomparable, which is the whole point of the table. Stored per kit as `marketplaces` (paise, keyed by marketplace id so a renamed label is not a migration), optional so kits saved before today still open, and an unfilled box stays **undefined rather than 0** — *not filled in* and *free delivery* are different claims and only one of them is ever true. The last column is labelled in as many words as **not profit**: no commission, no GST, no packaging, no ad spend, so it ranks kits against each other rather than banking a figure. It turns red when a sale leaves nothing. 2 tests, 188 total | 2026-08-08 |
| WW-125 | `[P0][L2]` A prompt edited on the partner's machine would have outranked every release, for ever | Bug | ✅ | Flagged while answering a distribution question, and Vansh's call: *"so I think we should look editing the prompt in the Windows right?"* — yes, and the fix is to **remove the ability, not add a button to undo it**. A prompt edited inside a packaged app landed in `userData/prompts/` and was read in preference to the shipped copy **from then on, permanently**. Every later prompt fix — and WW-070/072/082/095/096/118/119/122 are all prompt fixes — would then be silently ignored on that machine for that file, with nothing on screen saying so, and it would happen to the one person who cannot read a diff to find out why the copy got worse. **Prompts are now read-only in a package.** `savePrompt` refuses rather than writing somewhere that looks like it worked; the editor hides Save and *Put it back*, makes the box `readOnly`, and says plainly that a prompt change is a release, not a setting. **An override left by an older build is ignored rather than obeyed** — honouring it would keep the bug alive on precisely the machines that already have one — and the editor names the leftover file so the edit is not silently binned either. Editing still works in development (`npm run app`), where it writes to `docs/guides/` and git carries it to everyone, which is the only path that was ever correct. 10 tests in a new `tests/prompts.test.ts`, 193 total. **The general shape: a per-machine override of shipped content is a fork nobody declared**, and it is worst where the two machines are meant to be running the same thing | 2026-08-08 |
| WW-126 | `[P1][L1]` The volumetric warning blamed a cost Meesho does not charge | Bug | ✅ | Vansh, correcting me: *"all that observation were for Meesho I guess — Meesho has some AI detect the package cost; Flipkart cost for delivery is because of volume and weight only… Meesho even before writing the weight gives the shipping cost."* Right, and my wording had generalised. `SHIPPING-COST.md`'s fourteen failed experiments were **all Meesho**, and the reason they failed is now obvious: **Meesho quotes before a weight has been entered at all**, so nothing declared could ever have moved it — the number tracks the main image. **Flipkart is a different mechanism: volume and weight**, so a box that grows really does cost more there. WW-123's warning said *"a courier bills the larger one"* as though it applied to both, which would send somebody shrinking a box to fix a Meesho fee that ignores boxes. It now names the platform on both sides — *on Flipkart this parcel bills at 360 g, not its real 290 g… Meesho is unaffected: it sets its fee from the main image, before a weight is entered* — with the same split written into `packaging.ts`'s header and a banner at the top of `SHIPPING-COST.md` so its conclusions are not read as covering both. The test asserts the platform names are present, not just the word "volumetric" | 2026-08-08 |
| WW-127 | `[P1][L2]` A costed kit could only be read as JSON, which the partner cannot read | Feature | ✅ | Vansh, shown the saved file: *"can we make this more human readable, I mean a non-tech person — can we have these in a table? CSV types just like what we were getting in the Excel"*, with his real sheet as the example. **The JSON is the right thing to STORE and the wrong thing to READ**: it survives a rename, re-costs at today's prices, and nothing in it can go stale — but the person it is for has never opened one. `src/kit-csv.ts` writes the same kits as a spreadsheet laid out like the Excel it replaces (header line, then Category / Material / Count / Each / Line cost), plus the three things that sheet never had — **what delivery costs on each marketplace and what a sale actually leaves**, and **the parcel**. One block per kit, blank line between, saved from the panel via a file dialog. **Costed fresh on export**, never from anything stored, for the same reason the panel re-costs on open. Four details that decide whether it is any use: money is a **plain two-decimal number and not `₹58.50`**, or Excel reads the column as text and every total under it comes out blank; a name containing a comma is **quoted**, or `Animal Set, 5 pcs` shifts every column right from there down; the file leads with a **BOM**, because Excel on Windows reads plain UTF-8 as the system codepage and turns the material names — the whole point of the file — into mojibake; and **an uncosted line is named in its own column with a WARNING row above**, because the total silently excluding it is the one thing a reader of this file cannot check for themselves. 8 tests, 201 total | 2026-08-08 |
| WW-128 | `[P1][L1]` Prompt editing was off in the .dmg too, and Vansh runs the .dmg | Bug | ✅ | WW-125 turned prompt editing off wherever the app is packaged, which is correct for the partner's `.exe` and wrong for Vansh: *"hope you did is only for exe right? because from dmg or npm run app actually mostly I will and I should be able to edit this."* `app.isPackaged` cannot tell a `.dmg` from an `.exe`, and keying it off the platform would encode *Mac means developer*, which is false the moment either of them changes machine. So it is a **Settings switch, off by default** — the default is what protects the partner, and the person who needs it can turn it on. The switch says what it costs rather than just offering it, because the cost is not obvious: **an edit made inside a package is saved on that computer only, never reaches the project, so it cannot go out to anyone else — and from then on that machine ignores every future version of that one prompt.** The honest workflow is unchanged and still the only one that ships anything: edit under `npm run app`, where the file is the repo's and a release carries it. Development is unaffected and always editable | 2026-08-08 |
| WW-129 | `[P1][L1]` GST, the save button's place, and a way into the saved kits | Feature | ✅ | Three of Vansh's, from using the panel. **(a) GST.** *"I want to calculate another field to it — the listing price − shipping = tax."* A `GST` column on the delivery table, rate editable and defaulting to **5%** to match the `GST_5` tax code the listings already carry. Taken out of `(listed price − delivery)` and **extracted, not added** — `base × rate / (100 + rate)` — because a marketplace price is GST-inclusive and adding it on top would invent money the buyer never paid. Subtracted in the *left* column, whose caveat is now narrower and more useful: materials, delivery and GST are accounted for; the marketplace commission, **its own 18% GST on that commission**, packaging and ad spend are not. **(b) The save button moved up**, from the very bottom to directly under the last thing that is typed in. WW-127 had put it at the end so it sat below the fields it saves; the parcel underneath it takes no input, so being below that reads as further from those fields than it is. **(c) *Open the saved kits folder*** — Vansh: *"maybe in future I want to see here what any previous listing had costed me."* Creates the folder if it does not exist, because failing on a fresh install where nothing has been saved is a worse answer than an empty window | 2026-08-08 |
| WW-130 | `[P0][L1]` A return costs more than two kits, and nothing anywhere said so | Docs | ✅ | Vansh asked whether the marketplace return penalty is reverse shipping, a flat fine, or both, and **answered it himself by finding Meesho's own Reverse Shipping Fees table** in the supplier panel — which is worth far more than the searches, and is the fourth time in this repo that the live panel has beaten a seller blog (C-007/C-008, WW-095, WW-103). **It is a weight-slabbed shipping fee, not a fine**, quoted per courier and split Self Ship / Warehouse. For the ≤500 g slab every kit here falls in: **₹145–₹165** (valmo ₹153/₹145 · xpressbees ₹155/₹147 · delhivery ₹161/₹153 · shadowfax ₹162/₹154 · ecom ₹165/₹157). 501–1000 g jumps to ₹155–₹203, which is a second, independent reason the 490 g ceiling matters. **The number that changes decisions: a kit costs ₹58–₹73 in materials and one return costs ₹145–₹165** — between two and three kits' worth of stock destroyed by a single return, against a *left after everything* of roughly ₹150 on Meesho. **One return in three orders wipes out the margin on the other two.** Nothing in the costing panel models this yet and it is deliberately not built — a return RATE is a per-SKU observation nobody here has measured, and inventing one would be C-007's mistake with the stakes raised. Recorded so the next pricing decision is made knowing it. Flipkart's side stays unknown: blog sources say ₹160–₹175 with a March 2026 cut of about ₹35, all of it seller-blog-derived, and the seller-hub rate card is the only authority | 2026-08-08 |
| WW-131 | `[P1][L1]` The kits get their own folder setting, so a synced one holds only them | Feature | ✅ | Vansh chose the Drive route for sharing costings with his partner over committing them to git (*"this will be too much commit… let's just take the Google Drive route, it's easy only, also will be helpful once the partner wants to sync in"*) — his call, and reasonable. But *"point the workspace at a shared folder"*, which is what was possible before this, **drags the images along**: megabytes per listing, and worse, **every sync service can evict a file and leave a placeholder**, which `sharp` reads as a broken image — the same hazard that ruled iCloud out for the workspace an hour earlier. So the kits now have **their own folder setting**, defaulting to the workspace and pointed anywhere in Settings. A few kilobytes of JSON per kit, nothing streams them, and the images stay local. Relaunches on change for `chooseWorkspace`'s reason: the engine reads `WW_KITS_DIR` once at module load, and a setting that half-applies until you restart is worse than one that admits it needs to. Nothing already saved is moved. **Nothing syncs by itself and nothing should** — a folder they already trust beats a sync mechanism this app would have to own and get wrong. Still unresolved and more important than any of this, recorded in case it gets forgotten: **Vansh and his partner use different SKU codes for the same listing**, so the same kit under two codes is two kits and no amount of syncing fixes it. One convention has to be agreed first | 2026-08-08 |
| WW-132 | `[P0][L2]` A wrong price meant two different things and there was no way to say either | Feature | ✅ | Vansh, looking at Kitty Foil at ₹1.50 on a real kit: *"here I want the option to change any price too… this price change will be limited to this listing only"*, then immediately *"we should have an option to change any field any item's price for later — I mean change in inventory sheet permanent if I found something wrong."* **Two different claims, and the whole point is that they do not share a control.** *This batch cost me more* is a fact about one purchase; *the list is wrong* has to reach every kit ever costed and the other machine on the next release. **The Each column is now editable** and typing in it prices THIS kit only (`SavedKit.prices`, keyed `category|material`, saved and reopened with the kit) — and it works on a material whose price cell is blank, which is what makes it more than a convenience: **13 real materials have no price, and a kit containing one could not be costed at all before.** A **fix the list** button appears beside any line carrying its own price; that one calls `setMaterialPrice()`, which **rewrites `materials.json` in place** so every `_` comment, every `aka` list and the row order survive — regenerating the file would have thrown away the notes explaining the data. On success the per-kit price is dropped, because the list now says the same thing and leaving it would mark the line overridden for ever. **Refused outright in a packaged app** with a sentence explaining why: `categories/` lives inside the bundle there, and a silent no-op would look exactly like a saved change until the next kit disagreed. 6 tests, 206 total | 2026-08-08 |
| WW-133 | `[P1][L1]` Dropdowns rendered as white system boxes, and one string showed a backslash | Bug | ✅ | Both found by Vansh screenshotting the panel. **(a) `<select>` had no styling at all**, so all 121-row material pickers came out as white OS boxes on a dark panel — the one control on the busiest screen, and the one most likely to be misread. Styled with the rest of the form, `option`/`optgroup` included, because on a dark theme the OS draws those separately and needs telling. **(b) `today\\'s` was rendering with the backslash visible** in the save block, from a shell-escaped apostrophe that went into the JSX literally. `&apos;` now. Small, but it was on screen next to a money figure, and a visible escape character makes a person doubt the numbers beside it | 2026-08-08 |
| WW-134 | `[P0][L2]` The editable price box was blank and dead, and it hid the list price to boot | Bug | ✅ | Vansh, one minute after WW-132 shipped: *"now it is not showing what it is picking from the inventory… this is not even responding for my click, keyboard keystroke is not getting reflected."* Two faults, one immediate and one that made the first invisible. **(a) The proximate cause was a stale main process** — `electron-vite dev` hot-reloads the renderer but NOT the engine behind it, so the new input rendered against the old `costKit`, which returns no `each`. `undefined / 100` is **NaN**, React draws `value={NaN}` as an **empty field**, and every keystroke then round-tripped through that same engine and came back blank — so a control that was working looked completely dead. **(b) The design was what allowed that.** The box read its value from the ENGINE's answer, over IPC, on every keystroke: a controlled input whose source of truth was an async call it did not own. It now reads from local state and only *falls back* to the engine, with `??` instead of `=== null` so an absent number can never become NaN. Same for `fix the list`, which sent `l.each` — the engine's value — and now sends what is actually typed. **(c) And the column had stopped answering its own question.** Vansh: *"we should be able to see that, but by selecting that be able to edit it."* An input alone hides the list price the moment you type in it, which is the one thing the column exists to show, so every row now prints **`list: ₹0.80`** (or *no price in the list*) under the box — what the list says and what you are charging, side by side, always. **The lesson, third time this session in a different disguise:** a renderer change that lands while main does not is not a visible failure, it is a wrong value. WW-100 and WW-107 were the same shape with paths; here it was a number. When a control goes dead in dev, restart the app before believing the code | 2026-08-08 |
| WW-135 | `[P1][L2]` The parcel was computed and unchangeable, and it goes out on two listings and a spreadsheet | Feature | ✅ | Vansh: *"here too provide a dropdown to select various options we have at these, because these info is going in the CSV and we are not able to edit/change it right now"* — and he had already said the net and pump dimensions themselves are wrong and need re-measuring. So the numbers most likely to need correcting were the only ones with no way to correct them. **A box picker and a weight box on the parcel block.** Seven polybag sizes now live in `packaging.json` as data (`boxes`), covering 30-40 up to 80-120 pieces plus the net/pump variants, and they are **marked UNCONFIRMED in the file** with Vansh's own words, because the rules read the same numbers and both want measuring. **Only what is actually picked is stored** (`SavedKit.parcel`), which is the design decision: a kit whose *weight* was corrected still follows the rules for its *size*, so it picks up a corrected rule on the next release instead of being frozen at today's guess. The rule remains the default and the dropdown's first option names what the rule chose, so overruling is always a visible act — and `Parcel.overridden` makes the screen say *Chosen by hand for this kit* rather than *worked out from what is in the kit*, which would be a lie. **The chosen parcel reaches the CSV too**, pinned by a test: the panel showing one box while the spreadsheet declares another, and the courier weighing a third, is the failure this whole area exists to avoid (WW-055). Volumetric weight and the Flipkart billing figure recompute from whatever was chosen. **Nothing new was needed in the engine** — `parcelFor` already took an overrides argument from WW-123, on the grounds that somebody who has weighed the real parcel knows more than a rule table. 4 tests, 208 total. Still open, and now the only thing blocking accuracy here: **measure a real net kit and a real pump kit** | 2026-08-08 |
| WW-136 | `[P0][L1]` One CSS rule had been rendering every number box as a 15px circle | Bug | ✅ | Vansh, on the parcel weight and the GST rate: *"this up and down at here… a pretty uncomfortable thing to press, make good UI fix around here."* Both screenshots showed the same thing — a **circle about fifteen pixels across**, the value invisible inside it, with the native spinner arrows crushed into the remaining few. **The cause is one selector written for a different control:** `label.inline input { width: 15px; height: 15px }`, correct for the checkboxes and radios it was written for, and applied to **every input that later went inside a label** — margin, the flat amount, GST, the parcel weight, four separate controls across three features, each one broken the moment it was added. Scoped to `[type="checkbox"]` and `[type="radio"]`. **A type-less selector setting a type-specific size is a trap that only springs later**, which is why this arrived as three separate "it looks wrong" reports over an hour rather than one. Also, since it was raised: the **native number spinners are hidden** everywhere. They are two five-pixel arrows stacked at the field's edge, and nothing in this app is a value you nudge one step at a time — a price, a weight, a margin are all numbers somebody knows and types. Boxes are padded to a comfortable hit area and sized to show four digits and a decimal at once | 2026-08-08 |
| WW-137 | `[P0][L3]` A ₹25 kit costed at ₹400, with every figure on screen correct | Bug | ✅ | Vansh, with the panel open on a real Annaprashan kit: *"the kit has all these 16 cutting… but this whole kit comes to us and costs ₹25."* The sheet said **PHOTO PROPS · 16**, the list said **Annaprashan Kit With Props · ₹25**, and the panel multiplied them: **₹400 on one line, and ₹456.50 for a kit that costs ₹81.50.** **The worst shape of wrong this panel has produced** — nothing was mismatched, nothing was unpriced, no flag fired, and every individual number displayed was right. **The count and the price were in different units**: an inventory sheet counts what a BUYER sees, and the price is for the PACK those pieces arrive in. **`piecesPerPack` on a material** fixes it: cost becomes `ceil(pieces / piecesPerPack) × price`, so sixteen pieces is one pack and thirty-two is two — `ceil` because half a pack cannot be bought. Set on **11 materials, and only where the pack size is STATED IN THE NAME** (`Sonic Set, 5 pcs`, `Butterfly 3D, 12 pcs`) plus `Annaprashan Kit With Props = 16` which Vansh gave; **nothing guessed**, because a wrong pack size divides a cost silently, which is the very failure it exists to prevent. Four kits still have none (`GTB Kit`, `Masha Set`, `Paw Patrol Topper`, `Annaprashan Banner Kit`) and the file says so. **The Count column is editable too**, which was his first ask and is the general fix: where the AI simply miscounted, no data about the material can help. Corrections live in `SavedKit.counts` keyed by line index, alongside the material and price corrections — the reading in `lines` is never edited, because what was read and what a human decided are different facts and one of them is evidence. **The pack arithmetic is printed under the count** (`1 × pack of 16`) and the CSV gains a *Packs bought* column, because this is precisely the line where a hidden calculation cost ₹375. 8 tests, 218 total | 2026-08-08 |
| WW-138 | `[P1][L1]` GOLD BALLOONS matched Rose Gold Balloon | Bug | ✅ | Found while verifying WW-137 against a real kit, not reported. `gold` → `golden` is **two insertions on a six-letter word**, which the length-scaled edit distance rejects, while `gold` matches `Rose Gold Balloon` **exactly** and wins on the shared word — so the commonest balloon in the business resolved to the wrong row at 80%, flagged, every time. Both are ₹0.80 so no money moved, and that is the point worth recording: **the cost was not the price, it was the flag.** A warning that fires on a correct-looking everyday line teaches a person to click past warnings, and the ones that matter are `Silver Balloons` fitting an ₹0.80 row and a ₹2.00 row equally. Fixed by treating a word that is the **start of another** as the same root — `gold`/`golden`, `metal`/`metallic` — with a **four-letter minimum** that keeps the existing short-word guard intact, so `net` still does not reach `netted` and `red` reaches nothing. Pinned three ways: the right match, the short-word guard, and that `Rose Gold Balloons` still finds its own more specific row | 2026-08-08 |
| WW-139 | `[P0][L1]` Leaving a tab threw away everything typed into it | Bug | ✅ | Vansh: *"when the inventory cost is in the pipeline and I shift the page… my progress gets removed. I don't want to come again and enter all the stuff which I had already previously entered."* **`<Panel step={step} />` rendered exactly one panel, so every switch unmounted the old one and React discarded its state** — on the costing screen that is the read lines, every corrected count, every per-kit price, the margin, the delivery figures, the chosen box, and the dropped picture. The one thing that survived was a kit already **kept**, which is why it read as "save works, the pipeline doesn't". **Fixed in the shell, once, for every tab**, rather than by teaching one panel to remember itself: the panel stays **mounted and hidden** (`display: none`) once visited, so half-finished work on *any* step — the convert options, a picked listing, a fill report — is still there on return. **Mounted on first visit rather than all eight at launch**, because the Flipkart panel starts a 2-second Playwright session poll the moment it appears and that must not run from startup. **The one thing this broke, and it had to be handled:** `row` is a single broadcast channel from main and both Convert and Finish listen on it, so two live panels meant a convert run's progress also piled up in the finish log. Each now takes rows only while it is the one running, gated by a **ref set synchronously in `run()` — not `busy`**, because `setBusy(true)` only lands on the next render and the first rows can beat it. Not persistence across a restart: that is what **Keep** is for, and it is unchanged | 2026-08-11 |
| WW-140 | `[P1][L2]` A Meesho-only product had to run the whole Flipkart pipeline to get three lines of copy | Feature | ✅ | Vansh: *"have a place where you add a prompt for just asking for desc, included items and product name for meesho, when we don't make a product.json this will be useful."* **`PROMPT-meesho-only.md`**, offered on the Listing copy step under *Listing on Meesho only?*. The existing pair is the wrong shape for this: they exist to produce two **files** the app reads, and the first one's whole opening half is per-image descriptions, `not_visible`, and the Flipkart title and keywords — none of which a product with no Flipkart listing has any use for, and all of which need the photos uploaded in order. The new one takes **the items and counts, photos optional**, and returns **three blocks of plain text** — product name, description, pack contents — under fixed headings, with no file, no JSON and **no listing ID**, because with no `products/<ID>.json` there is nothing downstream to read one and the Supplier Panel is typed into by hand either way. So it also has no drop zone, and the panel says outright that the checks below it will have nothing to check, rather than leaving somebody hunting for a download that was never coming. **Every rule that came from a real rejection is carried, not summarised**: banned quality words, the brand-collision expansion (the *Golden Star* flag), buyer-word item naming, the "Colour" padding trap, place phrases, the urgency ban, the piece count in brackets, one entry per pack line, 255 characters, whitespace collapse. **Duplicated on purpose** — same decision as WW-081, where the banned-words list and brand rule went into both prompt files: each file has to survive select-all-copy alone, and a prompt cannot import. **No app plumbing was needed** — `promptText` reads any filename out of the guides folder and `extraResources` ships the whole folder, so the file exists in the packaged app for free, and the prompt editor opens it like any other. Refs added to `CLAUDE.md`, `THE-FLOW.md`, `START-HERE.md`, `GUI-SPEC.md` | 2026-08-11 |
| WW-141 | `[P0][L2]` The parcel was in centimetres, and every guessed box was smaller than the smallest bag we own | Bug | ✅ | Vansh, on the parcel picker: *"change these units to inches now, i know that better only here — the autofill should pick whatever converts and flipkart ask, be careful with that."* Then the measurement that mattered: *"in inches the smallest one i use is 8\" bredth and 10\" length and 1-2 inches of height, look that 4 cm is close to"*, and for a pump kit *"i guess 10\" 12\" 3-4\" i guess"*. **Two separate faults, and the unit was the smaller one.** (a) **The screen spoke a unit he cannot check.** He buys polybags by inch size, so `20 × 15 × 4 cm` is a number he has to convert in his head before he can tell whether it is the bag on his table — and this is a panel whose whole job is being checkable. The picker and the size readout now read in inches, **the stored value is untouched**: the option's `value` is still the cm triple, `packaging.json` is still cm, and a test pins that Flipkart's Package Details still receives `25.4 / 20.3 / 4` cm while the Additional Description tab receives `10 / 8 / 1.6` in. Changing what is shown changed nothing that is declared, which is what he asked to be careful about. The cm stay on screen under the inches, because the Package Details block is typed in by hand and a number you cannot see is a number you cannot check. (b) **The sizes themselves were wrong, and wrong in the expensive direction.** The base was `20 × 15 cm` = **7.9 × 5.9 in — smaller than the smallest bag that exists here**, and the seven `boxes` were a guessed cm family around it. Polybags are sold in a fixed inch family (8×10, 10×12, 10×14, 12×16, 14×18 — checked against Indian courier-bag suppliers, since Vansh asked for the common sizes), so a size in this file is now a bag you can actually buy: three footprints at the thickness each packs to, replacing seven invented ones. **The consequence is on screen and is the point of the change:** the base bag bills volumetric at **412 g against a real 250 g**, so an ordinary kit is charged on volume — the old guess billed at 240 g and hid that entirely. A pump kit, on his estimate, bills at about **1394 g against 300 g**. **His word was "guess", twice, so the file and the test both say so**: the footprints are real, the thicknesses are not, and one measured pump parcel corrects it in one line. Provenance is per-value in `packaging.json` — measured / his figure / his guess / catalogue — because the failure mode here is a courier reweighing at pickup and charging the difference back at settlement (WW-055) | 2026-08-11 |
| WW-142 | `[P0][L3]` The parcel the panel computed reached the Flipkart form nowhere — the AI's guess was being typed instead | Bug | ✅ | Vansh: *"this dimension i am putting here i want the flipkart autofill json to pick this and enter at the position where it is required, otherwise what is the use of having this all flow if mismatch is actually happening there."* **He was right, and the card on that screen was actively lying.** It said *"In inches, and the bot fills these — nothing to do here."* The bot fills `products/<ID>.json`. Those four values came from `PROMPT-product.md`, which asked the AI for *"Size of the product (numbers only, in inches)"* — a figure it can only guess from a photograph — while the parcel underneath was computed from the packed kit and was written into that file by nothing at all. **Two sources for one fact, and the guess was the one that reached the live form.** Fixed at both ends. **(a) `applyParcelToListing` in `paste-core.ts`** merges the parcel into the product file, and the panel gains a *Put these on the &lt;SKU&gt;* button that reports each key with the value it replaced (`Width 12 → 8`), because a file that quietly disagrees with what was on it a moment ago is the same class of failure. **It is a whitelist of five keys** — `Width`, `Height`, `Depth`, `Weight`, `Weight (unit)` — precisely because the caller is the renderer and the target is a file a bot types into a live listing; the Description, the keywords and the price are unreachable from here by construction, and a test passes `Description: "hijacked"` to prove it. **(b) `PROMPT-product.md` no longer asks for them at all.** The lazy half of the fix is the deletion: the AI is told to omit all four and why — *you cannot see a parcel in a photo, and a size the courier disagrees with is charged back to me after the sale*. One source, and it is the measured one. **Also, custom sizes, which is what started it:** *"sometimes we take 25cm envelope but the stuff we put into it is of 22 max, we also fold that margin on length — so for stuff like this we should have freedom."* The picker offered a fixed list of bags and nothing else, so a folded-down envelope could not be declared. Three inch boxes now sit beside it, per axis, and a typed size shows as **typed by hand** in the dropdown rather than letting it claim the rules chose a size a human overruled. **The typed string is held separately from the centimetres it becomes** — round-tripping through cm would eat the decimal point mid-keystroke and turn `8.` into `8`, then `87` on the next digit: WW-134's lesson through a unit conversion instead of an IPC call. 4 tests, 222 total | 2026-08-11 |
| WW-143 | `[P2][L1]` A hero layout that looks fuller without selling more balloons | Feature | ✅ | Vansh: *"we have some designs where they concentrate on the top corners to make it more bulkier… and the accumulative count of the balloon should remain the same, so cut some balloons from the sides already in the design and add those into those corner."* **`PROMPT-layout-corner-bulk.md`**, a fifth follow-up in the layout family, offered on step 2b. **It is the odd one out and that is what makes it worth writing down:** the other four move the garland somewhere else, this one changes *density* while the position stays — which is the only kind of change that has to be paid for out of the same budget. The instruction is therefore framed as an exchange rather than a request: *every balloon that goes into a corner comes OUT of the run, same colour, same size, one in one out*, plus the three ways a model would otherwise cheat — adding, duplicating, and **shrinking balloons to fit more in**. It also says outright that **a sparse middle is the correct outcome**, borrowing the arch prompt's move: a model told to make a corner full and not told what may get thinner will keep the middle and quietly add. The rest is craft that makes the look land — overlap in two or three layers of depth, biggest balloons outermost, densest right in the corner and tapering away, a few cropped by the top edge — and the standing guard that the banner and foil letters may not be covered or crowded. The panel's note says this is the layout to **re-count hardest**, because filling a corner is exactly where the extra balloons appear | 2026-08-11 |
| WW-144 | `[P1][L2]` The saved-kit list took up the screen and said nothing; now it is folded away and shaded by margin | Feature | ✅ | Vansh: *"hide this under a toggle… and take a color, do it's dark shade if the left after material of that listing is close to 60rs, as far as it goes change the color to light so that we know later on what we need to fix. There could be cases that meesho and flipkart have diff margin — if any of those is far from 60 then we will flag that for later by this."* **Two changes to the same block, and the second is the one that earns its place.** The list is now behind *show N* (closed by default): it only grows, and it sat between the two things used on every visit — dropping a reply in, and reading the table. **The shading turns a list into a worklist.** Each kit is coloured by what it **leaves** after materials, delivery and GST against the ₹60 rule — one hue, lightness carrying the distance, dark on target and lighter the further out. On a dark panel the light chips are the ones that catch the eye, which is the right way round. **The WORSE of the two marketplaces decides the colour**, exactly as asked: they are priced separately and a kit can be healthy on Flipkart and losing money on Meesho, which an average would hide; hovering gives both figures. **A kit with no listed price is left plain and is never shaded** — no margin is a different state from a bad one, and colouring it would invent a problem, the same distinction `no price set` vs `not on the list` draws on the cost table. The count of off-target kits (more than ₹20 out, a third of the target) stays on the heading **while the list is closed**, so folding it away cannot hide the warning. **`listKits` now takes the price list and returns what each kit leaves**, costed fresh — a stored figure would be last month's balloon price, the reason nothing here stores a total — and `leftAfterEverything` is the one place that arithmetic lives, GST **extracted** from the price rather than added on top, because an Indian marketplace price already includes it. 5 tests, 227 total | 2026-08-11 |
| WW-145 | `[P0][L2]` "not commonly stated" printed on a finished infographic, and the prompt edit that would have fixed it was never in the project | Bug | ✅ | Two faults from one screenshot. **(a) A working note reached the buyer.** The sizes infographic printed *"not commonly stated"* under Arch Tape and Glue Dot Roll. Vansh: *"customer actually don't even care about their size, this should not be written here or any other such thing."* The prompt already said *omit the dimension line entirely for anything that came back "not commonly stated"* — but it **named the phrase**, and a phrase named in a prompt is a phrase available to draw. Rule 5 now leaves the size cell EMPTY and says *no size* only in the source column, the card rule says outright that no words go where a size would (listing the placeholders it must not invent: *N/A*, *—*, *standard size*, *approx*), and a final check asks the model to read every word in the rendered image and remove anything that is not a count, a name, the header, the occasion pill, the total or the footer. **Step 1 is a conversation with me; step 2 is a picture for a buyer, and nothing crosses.** **(b) The bigger one, found while answering "am I editing the latest prompt?" — no.** See C-050: in development the app READ `userData/prompts/` and SAVED to `docs/guides/`, so an edit Vansh made in the app on 2026-08-07 sat in a file only his screen could see. `PROMPT-infographic.md` was 941 bytes in the repo and 2869 on his machine, and the missing part was the total-piece arithmetic guard — *never copy a total from IMAGE 2* — which is exactly WW-122's failure mode. The override was **promoted into the repo** (a strict superset; nothing in the shipped copy was lost) and kept in that prompt's version history. `activeFile` now returns the shipped file in every mode, the leftover-override warning fires in development too, and the dead `edited` flag is gone. 2 regression tests, 229 total | 2026-08-11 |
| WW-146 | `[P1][L2]` The size chart measured the bundle, not the balloon | Bug | ✅ | Vansh, on a finished baby-shower infographic: *"here for size only 1 item count should be picked, look here bundle of balloons are coming… single balloon single foil should come and we take size with respect to that, now the user may get confused that whole group size is this."* Every latex card drew a **cluster of fifteen** with the 10" arrow spanning it, so the picture said *this bunch is 10 inches*. **The prompt had never said what the arrow attaches to** — it described the dimension line in detail and left the subject implicit, and the model drew the pack shot it had seen on the counts infographic. Three rules now, one per place the mistake can enter. **Step 1:** every size is the size of ONE PIECE — never the pack, never the bunch, never add them up. **The card:** the picture is a single item, and *the count label does not change* (his correction — "15 Pcs" still reads 15 Pcs above one balloon); the grouped full-pack look is what the OTHER infographic is for, this one is a size chart. **The final check:** follow every arrow and confirm it starts and ends on a single item. **And the case that has no single piece:** a foil letter banner is sold as *16 inch letters*, so a "WELCOME BABY" card now draws the complete banner small in a corner, a leader arrow across to ONE enlarged letter, and the dimension on that letter — Vansh's own layout, and it keeps the thing the buyer recognises while measuring the thing that is actually 16 inches. Also noticed while comparing the two runs and worth watching: the same kit came back with **4 silver stars once and 3 the next time**, which is the count rule failing quietly on a redraw | 2026-08-11 |
| WW-147 | `[P1][L2]` A how-to-decorate infographic, written to be occasion-blind | Feature | ✅ | Vansh, with a competitor's card as the target: *"welcome baby is just for example here… this is a very universal thing in the prompt, don't ever mention a specific — for eg the ChatGPT picks detail for that one. This should be sku json or inventory table specific for each listing."* **`PROMPT-how-to-decorate.md`** — the four-step assembly card plus a *package includes* grid and a footer strip, as message 6 in the app. **The instruction he actually gave is the design: the prompt contains no occasion, no colour, no kit and no example values at all**, which is learning note 9 and WW-122 applied before the fact rather than after — the ANP003 bug was an example ID in a prompt being copied into a real answer, and an example occasion here would do the same thing to a whole picture. So the occasion is read off the pack (or asked for, never guessed), the two accents are colours that are in the kit, and **the steps are derived from the accessories that are actually there**: the inflating step names the pump only if a pump is in the pack, the arch step names the arch tape only if it is in the pack, and so on. The rule is stated with its consequence, because that is what makes a model obey it — *an instruction telling a buyer to use something that is not in the box reads as a missing item and comes back as a return*. The grid reuses WW-146's one-piece rule, and the footer strip is the one place the reference image had to be **rejected rather than copied**: it says "PREMIUM QUALITY BALLOONS" and "luxury finish", both on our banned list from a real marketplace flag, so the prompt requires plain checkable facts instead. Final check ends with *would this picture be wrong for a different kit? It should be* | 2026-08-11 |
| WW-148 | `[P0][L2]` The sizes infographic was resolving foil sizes from what other sellers list | Bug | 🔄 | Vansh: *"i think we are picking wrong foil size too — foils for welcome baby is nearly the same for heart or star ones. Will they get inflated? Because right now raw material heart foil is 8*10 and star is even smaller 6*8. I would like you to check the generic sizes again, or shall I check them manually now."* **He should, and the prompt should stop trying.** Rule 3 (*one standard only, fill it in*) was letting every foil shape through on a size derived from what Indian sellers commonly list — which describes **somebody else's stock**, and his is smaller. That is C-007/C-008's rule (never source a fact from a search summary) reaching the one place it does the most damage: a dimension printed inside a finished picture, which nobody proofreads and which the buyer measures against what arrives. **Every foil shape is now rule 4 — ask, always** — hearts, stars, moons, numbers, letters, characters, bottles, even where one size looks obviously commonest. The question it must ask carries the distinction that makes this go wrong: **a foil is sold by its INFLATED size and wholesale stock is described by its FLAT uninflated size**, and one must never be printed as the other. **Still 🔄 until Vansh measures one of each shape inflated, point to point.** Those figures belong on the material rows in `materials.json` (WW-116), so they are answered once for every listing instead of per picture | — |
| WW-150 | `[P1][L2]` Finished images are named from the listing's own title, not just its ID | Feature | ✅ | Vansh, after the research below: *"just change the code man, what test — we were anyways doing that, good that we are injecting a name to it too."* His decision, and his reasoning is the strong part: **the rename already happened**, so this is not a new mechanism, only a different string. `<ID>.<n>.jpg` becomes `ANP003-annaprashan-decoration-kit-red-gold-1.jpg`. **Two of the three rules behind the shape have nothing to do with SEO.** The ID stays FIRST so a listing's images sort together in the flat output folder and the token you search on leads; the **position stays LAST** because 1-2-3-4 must sort into upload order — image 1 is the main image, and a name that sorts wrong puts the infographic on the search grid. The words come from `image-meta/<ID>.json`, which already exists, so there is **no new prompt and no extra AI step**; the bracketed piece count is stripped (length that changes per listing) and the slug is cut to 60 characters on a whole word, keeping the path inside Windows' 260-character limit. **No copy file yet falls back to the old `<ID>.<n>.jpg`** — a listing is often finished before its copy exists, and failing or inventing words would both be worse. **The thing that nearly went wrong: the first version read the title from the PICKED descriptions**, which would have made choosing another listing's copy rename the files — rebuilding WW-078's coupling in a new place, and contradicting the Finish panel's own promise that picking does not rename. Now `finishListing` derives the words from `descriptionsFor(id)` and `descs` is a separate argument it never reads for naming: the folder decides the NAME, the picker decides the DESCRIPTIONS, structurally. 6 tests, 235 total. Whether a marketplace reads any of it is still WW-149's open question — the code note and the playbook both say so | 2026-08-12 |
| WW-149 | `[P2][L1]` Do keyword-rich image FILENAMES do anything on Meesho or Flipkart? | Research | 🔄 | Vansh, from a YouTube video by a well-known Meesho seller who works on SEO and CTR: *"he said the file name also matters while we are deploying the images… so we should rename the file in such a way that it is more CTR, more SEO optimized. Please research on it."* **Researched before building, and the premise does not survive the reading.** Every source claiming filenames matter is about **a site you own** — Shopify, your own CDN, image sitemaps — and every one describes the same mechanism: *persistence*, your filename becomes the URL and Google reads the URL. A marketplace gives that mechanism nowhere to happen: you upload into a form and the platform re-encodes onto its own CDN under a generated path. Neither current Meesho listing guide mentions file naming at all. `image-playbook.md` Part 3 has said *"filenames are discarded"* from the start — **and that line was never measured**, so this is one unproven claim meeting another, which is why it is a ticket and not an answer. **Vansh chose to test before building.** The test rides on a real upload and costs nothing extra: name the main image in words on the next listing going up anyway, then read the served image URL off the live page. Words in it, and the build is **one line** — `finish-core.ts`'s `outName` — with the words coming from the Meesho title and keywords **already in `image-meta/<ID>.json`**, so no new AI step and no new prompt. A hash, and it is closed for good. Written up beside the metadata experiment (WW-037) it mirrors, with the warning that matters here: *do not skip straight to renaming because it is cheap — that is exactly how an unproven lever gets into the flow and then gets defended.* **Blocked on one upload** | — |
| WW-151 | `[P0][L2]` The product name buyers see is built from two fields nobody was writing as a title | Feature | ✅ | Vansh noticed the listing form's "Product Title" read *"Multicolor Decoration Kit"* and asked whether it mattered. It is `Color` + `Type`, character for character — and he then **proved the whole mechanism himself** by pulling up an older listing where he had already tried it: form `Type = Balloons`, `Color = Groom To Be Decoration | Mutlicolor Balloons | Sash & Foils`, live page *"PartyDreams Groom To Be Decoration, Mutlicolor Balloons, Sash & Foils Balloons"*. So **NAME = brand + Color values in entry order + Type**, measured rather than guessed, and `Model Name` is not in it. **The consequence is out of proportion to how the fields look:** the most-read text on the whole listing was being filled as if it were a filing category, defaulted to `Multicolor` for every product. `Color` is now per-listing and `PROMPT-product.md` writes both as a headline — occasion first (it survives truncation), then contents with the real colours, then what makes the kit different, then a tail that reads correctly after the third phrase since Flipkart inserts no comma there. **The cost, decided not assumed:** these values make the colour FILTER useless. Vansh's call — shoppers search words far more than they filter party decorations by swatch — and rule 2 (*name the real colours*) buys most of it back for free if the filter matches words inside a value, which is still unverified and worth one check. **And the thing that is now visible:** the Flipkart panel composes the name after a fill and shows it, flagging an `&` or a word spent twice. Deliberately NOT a spell-check — a word list would rot and still miss the next typo, and a live listing already reads `Mutlicolor` (matching nothing anyone searches) precisely because the finished sentence existed nowhere before Save. 5 tests, 260 total. Found on the way: C-053 | 2026-08-12 |
| WW-152 | `[P1][L2]` A field left out from doubt looked identical to one nobody thought of | Feature | ✅ | Two of Vansh's corrections in one. First: *"an occasion could be of various type not only the ones that you have wrote in the prompt but many other — i would like the chatgpt to read the image and other inventory details we have filled to guess that."* He is right and the repo already proved it — the scan records `Occasion` as **`kind: "pills"`**, a free-text chip input, so the closed list of seven in `PROMPT-product.md` was a fiction inherited from an early draft. It now says to work the occasion out from the printed banner wording, the props and the INVENTORY, and to name it the way an Indian shopper says it. Second, and the more useful half: *"if any confusion take my permission, flag it somewhere."* Rule 1 has always told ChatGPT to omit rather than invent, which is right and **silent** — an omission made from doubt and one made from oversight are the same empty box, and nothing downstream could tell them apart. There is now an **`_ask`** key: ChatGPT states the question in one sentence, still gives its best answer in the real field, and the Flipkart panel shows every question before Save. It cost almost nothing because the convention already existed — `loadProduct` has always stripped `_`-prefixed keys as human notes, so this collects the `_ask` ones on the way past and drops the rest. Only `_ask`, deliberately: the defaults files carry paragraphs of `_comment` prose that would bury a real question. Styled as a question, not an error — nothing is wrong yet, and a red box trains the eye to dismiss it. 4 tests, 264 total | 2026-08-12 |
| WW-153 | `[P0][L2]` "No file in products/" said nothing about which folder, and that folder could not be chosen | Bug | ✅ | The partner, mid-fill on ANP003: `No file in products/ matches "ANP3"`. He created a `products` folder by hand and got the same sentence back — Vansh: *"this fill this form button is not able to check for the products folder... right now we don't know where is it coming from and what else is happening."* **The folder was being read correctly, and his own screenshot proved it** — the picker draws a row for any `.json` in `PRODUCTS_DIR` under any name, and ANP003 showed only `copy` with *no Flipkart file*. `products-ANP003.json` had simply never been imported. **The bug is that `findById` returns `null` for three different situations** — folder missing, folder empty, folder holding other listings — and every caller printed ONE sentence for all three, naming a relative `products/` that appears on no screen. `whyNoMatch` in `id.ts` now separates them and prints the absolute path, used at all three call sites (both `browser-core` lookups and the in-app JSON editor). **The second half is the one he asked for and the more useful:** every other step names its folder in a dialog, this one alone used an implicit `<workspace>/products` printed nowhere. The Fill Flipkart panel now shows that path with **Open** and **Choose…**, mirrored in Settings, stored like the kits folder and relaunching for the same reason — `paths.ts` reads the folder once at module load. Pointing it straight at Downloads is a legitimate answer and skips importing entirely. **Also:** `showFolder` creates the folder before opening it — `shell.openPath` on a folder that does not exist does nothing, silently, which is exactly the folders most worth looking at. 1 test, 282 total. See C-055 | 2026-08-14 |
| WW-154 | `[P0][L3]` Four seller accounts, two people each, and one app that shows whose data it is | Feature | ✅ | Vansh: *"amongst these six or seven partner, we are divided into four different parts... the two people who are on the same account should see the same data, but if the account differs then the data should also differ."* **The identity already exists and is not ours to invent: each seller account IS a Gmail**, the one the Flipkart and Meesho logins were made from — his idea, and it removes the whole question of where an account's data lives. One Gmail → one Drive folder → one workspace, shared in Drive with exactly that pair. **No login screen, no server, no Google OAuth or Drive API.** The isolation is Google's folder sharing, which is real access control; a password check inside an Electron app is theatre, because the files are on the local disk and Explorer opens them regardless. Drive Desktop syncs a folder, we read a path — that is the entire integration. `settings.json` gains an `accounts` list (`label`, `workspace`, `skuPrefix`), switching relaunches exactly as `chooseProductsFolder` does (WW-153), and **the current account is on screen at all times in the rail** — that permanent label is the actual safety feature, being a standing answer to a question nobody thinks to ask before making the mistake. **Vansh cut two things from the design and both cuts are right:** no per-occasion SKU folders (*"all the WishWorks ready product... will be stored in just one folder each for each account, just be simple with that"*), and no multi-account Chrome yet — see WW-155. **The SKU guard flags and never blocks** (*"everyone in my partnership are new to this... maybe flagging is the option"*): marked at import, marked on the listing row afterwards so it survives the hour between importing and filling, and counted in the Fill pre-flight's `needsEyes`. Unset `skuPrefix` means no flagging at all — an account that does not want it never sees a nag. **Built 2026-08-15.** `settings.json` gains `accounts` (`label`, `workspace`, `skuPrefix`) and `activeAccount`; an account's folder IS the workspace, so `storedWorkspace()` prefers it and every `WW_*_DIR` below is untouched. Add / switch / remove all relaunch, which is WW-153's pattern and not laziness — `paths.ts` resolves those env vars once, at module load. `chooseWorkspace` now writes into the ACTIVE ACCOUNT when there is one, because writing the flat `workspace` key it would then ignore looks exactly like a button that does nothing. **The flag is checked in the renderer, not in `src/inbox.ts` as the handover planned** — the renderer already knows the prefix and already renders every ID, so one `SkuFlag` component covers the import list, the listing row and the Fill panel, the engine stays account-blind, and the import warning now shows BEFORE the button rather than after it. The predicate is `isForAccount` in `gui/shared.ts` (the renderer cannot import `src/id.ts`, which pulls `node:fs`), tested in `tests/id.test.ts` — the case that must never regress is the empty prefix meaning never, not always. The permanent account label sits under the brand in the rail. Nothing new is blocked anywhere, and a machine that adds no account behaves exactly as it did before | — |
| WW-156 | `[P0][L2]` Two shared folders on Drive, and nothing else leaves the machine | Feature | ✅ | Vansh, cutting through a round of questions the handover had already answered: *"the images that are ready with image meta injested will be synced nothing else using the google drive, and the inventory jsons will be also in sync, just simple as that. 2 diff folder for mentaining that."* **Two synced folders, both already settings: the ready folder (Finish step's output, which Check reads) and the kits folder.** Everything else — raw photos, the AI's downloads, `image-meta/`, `products/` — stays local, because **the descriptions are written INSIDE the finished JPEGs**, so the copy file never has to travel. That is why the `image-meta/` "gap" proposed alongside this was withdrawn rather than built (C-056). **The ready folder now groups by SKU code** — *"1 folder for all GTB and another for ANP"* — via `skuGroup` in `finish-core.ts`, placed in `finishListing` so single and batch runs and the CLI cannot disagree; an ID with no leading letters stays in the root rather than inventing a folder nobody can predict. **This reverses the handover's "no per-occasion folder trees", deliberately.** `runCheck` had to become recursive in the same change: it read one flat level, so the ready root would have reported zero images — indistinguishable from the finish step having written nothing. The account folder's wording is now explicit that it is **local**, and names what lives in it (*"for what files but? you should state that"*) | — |
| WW-157 | `[P0][L2]` Every folder the app saves into is choosable, and all of them exist on first launch | Feature | ✅ | Two of Vansh's, one session apart. First: *"for each saving either it is about the ai gen images or json or final images or inventory json, we should be able to select the folder where they get saved. if we can't would like you to build around that."* An audit found **three of five already settable** (finished images, kits, products) and **two not** — `images/` and `image-meta/` moved only by relocating the whole workspace. Both now have a setting, and the four stored ones collapsed into **one generic pair of channels** (`folders()` / `chooseFolder(key)`) with **one `FolderSetting` component used four times**, replacing two bespoke handlers and a bespoke panel block — the labels and the dialog wording live in `main.ts` beside the default, so a screen cannot describe a folder differently from the code that picks it, which is what WW-153 was. Second, on a new Windows PC: *"when i click bring files for the first time at page 3, these folders aren't created by default with correct location."* **Root cause: each folder was created by whichever step wrote to it first, so every step that only READS got a missing folder** — three symptoms, one cause (`shell.openPath` on a missing folder does nothing silently, `readdir` throws ENOENT which reads as a dead button, and `whyNoMatch` correctly reports a folder that does not exist and sends someone off to create it in a guessed place). `ensureFolders()` now mkdirs all four at launch, non-fatally — `mkdir -recursive` on an existing folder is free, and no step can be the one that forgot | — |
| WW-158 | `[P0][L2]` An account's folders were the machine's folders, so two accounts shared them | Bug | ✅ | Vansh, working through the setup: *"after logging in for my account, my folder details should stay diff then my partner's one"*, and then the sharper version — *"if 2 accounts login with same computer, that raw data they are going to use is coming from one machine only hence no separation will be made... if they are not pointing to two diff folders you know."* **He is right, and WW-157 had just made it worse**: five folder settings, all machine-wide, so switching account changed the workspace and nothing else. They now resolve **account → machine → default**, and `chooseFolder` writes into the active account. The **ready folder stopped being a per-step memory and became a real setting**, which is what made it visible: it is the folder that goes on Drive, so two accounts sharing it would put two sellers' finished images in one place. Finish and Check both read it now, so they cannot disagree about where the images went. **Saved Flipkart pages went per-account too** — a remembered URL carries that seller's own `vid=`/`requestId=`, so a shared list opens the wrong seller's dashboard from a correctly-named button. And because the app cannot enforce what folder someone picks, **it says so**: an account sharing a workspace with another is flagged wherever it is listed. Flag, never block — sharing on purpose mid-setup is a real state | — |
| WW-159 | `[P1][L1]` Which account am I working? — asked once at launch | Feature | ✅ | Vansh, opening Settings looking for something that was not there: *"here do i select account login and all?? i expected it to come on the app launch."* **There is no login and this is not one** — no password, nothing to authenticate. What it fixes is that the permanent rail label answers the question *only if you look*, and nobody looks before making the mistake; making the first click of the day be "yes, I am working GTB" means the answer was seen once, deliberately. **Shown only when the machine holds more than one account**, so today nobody sees it — a screen you dismiss every launch to tell you the one thing it could say is worse than no screen. Switching from it relaunches and asks again, which is confirmation, not a bug. Vansh also asked for it generic rather than built for two: it is, everything is array-and-index and the gate's condition is *is there a choice*, not *are there two* | — |
| WW-161 | `[P0][L2]` A saved kit opens with its own JSON in the box, so its SKU can be changed | Feature | ✅ | Vansh: *"when i click any of these listing i want it's respective json to open in the upper box too... main reason to ref it's json in that box was to give me freedom to enter a new sku no."* **Reopening a kit used to CLEAR that box**, so the SKU was whatever the AI had written at the top of the reply and there was no control anywhere on the screen to change it — which matters because that one word names the kit's file, the `products/<ID>.json` the parcel button writes, and the image folders. The box now opens filled with the kit's own reading (`sku` + `lines`, not the whole saved file: the prices and corrections are edited in the table, and a JSON copy of them in a textarea would be a second place to change one number). **Editing it no longer wipes the kit.** `took()` reset everything on every re-read, on the rule *a new reply is a new kit*; with the open kit's JSON sitting in the box that rule would throw away the typed prices on the keystroke that renames it. It now compares the item list: same items = the same kit revised, keep everything; different items = a different kit, reset as before, because `overrides` and `counts` are keyed by LINE INDEX and would land on the wrong lines. **A rename is a rename, not a copy** — the filename comes from the SKU, so keeping after a change writes a second file and leaves the old number in the list; the panel remembers the file it opened and deletes it (`deleteKit`, confined in `main.ts` to a `.json` directly inside the kits folder — a path from the renderer must never be able to point anywhere else). `tests/inventory.test.ts` pins the two-file behaviour that step exists for. **And renaming in Finder does show up**: the list is read off the folder every time, now also on window focus — but it renames the FILE only, the `sku` inside still says the old number, which is why the in-app rename is the one to use | 2026-08-15 |
| WW-162 | `[P0][L2]` A tie is never quiet, the colour is never dropped, and the price list is searchable | Bug | ✅ | Three findings from one costing session, all the same failure in different clothes: **a name that does not say which row it is, priced off whichever row happened to be listed first.** (1) *Glue Dot Roll* was costed as **Glue Dot Strip ₹1**. Vansh: *"glue strip is another product, glue tape aka glue dot roll is other"* — so `Glue Tape` ₹3.50 gains the aka `Glue Dot Roll`, and the ₹1 strip is left exactly as it was. **Only the name carrying the word ROLL is listed as an aka, deliberately**: a sheet that just says *glue dot* now ties the two at 0.80 and gets flagged for a human, which is the honest answer to a genuinely ambiguous word. Confirmed with him before touching it — the two rows are a ₹1 / ₹3.50 difference and I had it backwards first. (2) **A TIE now flags on its own, at any score.** `Star Foil` scores 0.80 against `Blue Star Foil`, `Golden Star Foil` and `Pink Star Foil` — identically — and a silver kit was costed as the golden one. The old rule only flagged below SURE, so an exact tie at 0.90 would have been silent; the winner of a tie is `materials.json` row order, which is a coin toss and must never be quiet. (3) `PROMPT-inventory.md` already asked for the colour, so the fix is to show it the failure rather than repeat the rule: the `Star Foil` case is quoted in it, a colour that appears only as a group heading is claimed for every line under it, and a colour the sheet does not print stays out — an incomplete name gets checked, an invented one does not. **Audit of the nine saved kits** (which is how to find the ones already wrong): ANP004 and SVP033-ANP002 both hold a bare `Glue Dot(s)` line, now flagged instead of silently ₹1; ANP003-en has `Chrome Red Balloons` and `Chrome Golden Balloons` tied against `Chrome Balloon`; HBD-DORE01 has `Blue and White Balloons`, which is two items on one line. **The material dropdown became searchable** in the same pass — *"the dropdown should open listing everything bcz i don't remember everything, but there we should have searching option too"*: a native `<input list>` + `<datalist>`, no combobox library, substring search instead of a `<select>`'s first-letters-only. Focusing empties the box so the list opens on all 121 rows, because the browser filters a datalist by what is already in the field; only a name that IS a row commits, anything half-typed is dropped on blur, so it cannot be left in a state that says something the line is not costed as | 2026-08-15 |
| WW-163 | `[P0][L2]` The JSON box and the cost table are one kit, in both directions | Feature | ✅ | Vansh, after correcting four materials by hand: *"this was my final inventory pick, but this \u007bjson\u007d is its respective json in that box at that time — is my change of changing the json in that box based on the inventory we pick not possible?"* It was not: the box showed only what ChatGPT read, so `Silver Metallic Balloon`, `Space Theme HBD Set`, `Silver Moon Foil` and `Glue Tape` existed nowhere in the text he could copy. **A correction now appears in the box as `pick`, beside the line it belongs to** — and comes back in as a correction when the text is read again (`readKitFile` returns `picks`, `resolvePicks` turns the names into `category|material` keys against the price list). **`item` is never overwritten with the picked row's name**, which was the tempting version and the wrong one: that column is what gets read against the picture, and replacing it with the app's own answer would destroy the only evidence on the screen — *Silver Metallic Balloon* would then be indistinguishable from what the sheet actually said. Two facts, two fields. **`pick: ""` is kept distinct from no `pick` at all** (`"pick" in l`, not truthiness): empty is a human saying *this is on no row*, and dropping it would let the line quietly re-match to whatever it scored best against. A pick naming no row is dropped rather than resolved to the nearest — an explicit correction is the last thing that should be guessed at. **The two directions cannot fight, because focus decides which one owns the text**: while the box has the cursor what is typed wins, and the moment it is let go the kit is written back out. A plain "rewrite the box whenever the kit changes" effect would have rewritten the text under the cursor on every keystroke, since every keystroke re-costs the kit. Counts stay out of the box deliberately — they are a correction too, but they are already visible in their own column and a second copy is a second thing to keep in step. Also fixed in passing: the new material picker had no `type="text"`, so `input[type="text"]` did not match it and it rendered as a white browser-default box in a dark panel | 2026-08-15 |
| WW-160 | `[P2][L2]` Several accounts on one PC, each synced to its OWN Drive — the checks, not the plumbing | Feature | ⬜ | **Deferred on Vansh's call, 2026-08-15:** *"diff data diff account but synced where it should do... on next step this i should do later i guess, bcz as now we don't need this."* **The plumbing already exists (WW-158)** — `ready` and `kits` resolve per-account, so GTB's finished images can go to GTB's Drive folder and ANP's to ANP's, today, with no code. What is missing is everything that catches a wrong one. **(a) The shared-folder warning only compares `workspace`.** Two accounts with different workspaces but the SAME `ready` folder are not flagged, and that is the folder where a mix-up travels furthest — it is the one on Drive, seen by other people. Extend the check to all five folders. **(b) Nothing verifies a Drive folder belongs to that account's Gmail.** The app sees a path; it cannot tell `My Drive` from whose Drive. **(c) Drive for desktop on Mirror needs a distinct local folder per Google account** — the first is `~/My Drive`, the rest must be pointed elsewhere by hand, and nothing in the app says so at the moment you pick one. **Blocked in practice by WW-155**: several accounts on one PC share one Chrome profile, so the Flipkart login is still the wrong seller's. Do that first — this ticket is only worth building for a machine that can safely hold more than one account | — |
| WW-155 | `[P2][L2]` Two seller accounts on ONE computer — deferred, with the collision written down | Feature | ⬜ | **Deliberately not built, and Vansh's reasoning is why:** *"computers are different, we are not logging in in each and every account from just one PC. One PC will have just one account. It's just that we have many PCs and two or three PC can share a same account."* So the collision cannot happen yet, and the guard for it would be untestable code protecting a case nobody is in. **What it is, for whoever picks this up:** `PROFILE_DIR` is ONE Chrome profile in `userData` — a single Flipkart seller session for the whole app. The day one machine holds two accounts, switching leaves you logged in as the previous seller while the app fills the next seller's listing, on a live marketplace. The fix is `userData/profiles/<account>/`, one per account, and it must stay in `userData` and **never** go in the Drive folder: a synced Chrome profile corrupts. Related: WW-061/WW-094, where a profile in the wrong place read as "it logged me out" three separate times | — |
| WW-116 | `[P1][L2]` The inventory knows what each item costs and not how big it is | Feature | ⬜ | Vansh's stage three, asked for in the same breath as WW-115 and deliberately left until that one is running on real prices: *"right now we don't have an option where we can fill the size, so our inventory is also only storing the cost for each item and not the size and dimension"*. **This is not a new problem and half the answer already exists.** `PROMPT-infographic-sizes.md` resolves a size per item today, and WW-105 records what it cost to get right: nearly every one of these products has two or three standard sizes (a champagne bottle foil at 12-14" **and** 32", a star foil at 12" **and** 18", latex at 5"/10"/12"), so *"the common size"* is the wrong question — asked it, the model picks a side and prints it on a picture with no sign it chose. That prompt's fix was an ordering: the inventory line wins outright, size words map to that item's two standards, a single standard fills itself in, and more than one standard with nothing stated means it **asks** rather than picks. **The work here is to move the answer one step earlier**: a `size` on the material row in `materials.json`, so the size is stated once where the price is stated and the sizes infographic reads it instead of re-deriving it per listing. That also removes the step where Vansh types the size into the pack list by hand, which THE-FLOW currently tells him to do. **Do not web-search a size list into the file** — C-007/C-008's rule, and WW-105 is the same rule already applied to this exact data: a blog-sourced dimension that arrives inside a finished picture is one nobody proofreads. Sizes come from the stock or from Vansh | — |
| WW-111 | `[P0][L3]` The Price/Stock tab would have filled almost nothing — four labels matched no row, three fields had no source | Bug | 🔄 | **Found by Vansh pasting the live form.** `balloon-decoration.pricing.defaults.json` was harvested from another seller's bot and had never been compared with Flipkart (WW-015 says exactly that). `fields.ts` matches labels **exactly** after normalising case/space/trailing `*` — line 103 is `if (norm(rowLabel(row)) !== want) continue` — so a near-miss silently fills nothing. **Four keys were near-misses:** `Fulfillment By` → the form says **`Fullfilment by`**, which is Flipkart's own misspelling and we have to copy it; `Minimum Order Quantity` → `Minimum Order Quantity (MinOQ)`; and `Length (cm)`/`Breadth (cm)`/`Height (cm)`/`Weight (kg)` → the form labels are bare (`Length`) with the unit in a **separate qualifier control**. `(unit)`/`(option)` is a real syntax `findControl` supports — `"Length (unit)": "CM"` addresses the picker — but `(cm)` is not, so those four were read as literal labels matching nothing. Split into eight keys. **Three required fields had no source at all:** `Seller SKU ID`, `MRP`, `Your selling price` are per-listing, were in no defaults file and in no product file. `PROMPT-product.md` now emits them as `TODO_SKU`/`TODO_MRP`/`TODO_PRICE` with an explicit instruction never to guess a price — and `checkValues()` already blocks on any `TODO_` prefix, so this stops before the browser opens rather than typing somebody else's price into a live listing. Verified end to end: both defaults files merge, the corrected labels resolve, and the three placeholders come back as blocking problems. **`Tax Code: GST_5` is no longer a guess** — the file's own comment said the previous owner's bot picked it by pressing UP eight times blind; Vansh read GST_5 off the live form. **Still 🔄 because three things need the form, not this repo:** whether `Luxury Cess` is really `-1` (looks like an empty-state placeholder, left at `0`), whether the Additional Description tab calls its weight `Weight` or `Net Weight` — **`loadProduct` merges every defaults file into ONE flat map used on every tab, so a label on two tabs cannot be told apart**, and WW-055 records a live listing carrying `Net Weight = 10000 g` that costs real money — and confirmation of every label above. One `npm run scan` session answers all three | — **First real scan ran 2026-08-03 and settles most of it.** `categories/balloon-decoration.json` now exists — 90 rows, **17 (Price/Stock) + 7 (Product Description) + 66 (Additional Description)**, which is the whole form. Every corrected label above is confirmed against the DOM: `Fullfilment by *`, `Minimum Order Quantity (MinOQ)`, `Procurement type`, `Shipping provider`, `Tax Code *`, `Luxury Cess` all present exactly as written (the trailing `*` is stripped by `norm()`, so it does not affect matching). **Two surprises worth recording.** (a) **The Product Description tab is only 7 fields, and they are not the ones anyone assumed** — `Country Of Origin`, `Manufacturer Details`, `Packer Details`, `Importer Details`, `Model Name`, `EAN/UPC`, `Pack of`. The first three had been filed under Price/Stock in `pricing.defaults.json`; harmless, because `loadProduct` merges into one flat map and fill works per visible tab, but the file's comments were wrong about where they live. (b) **Four keys still match no scanned row: `Breadth`, `Length (unit)`, `Breadth (unit)`, `Height (unit)`.** The Price/Stock scan captured `Length *` and then no other dimension, while `Height`/`Weight` in the file come from the Additional Description dimensions block (`Width`/`Height`/`Depth`/`Diameter`/`Weight`, no asterisk) — so **those two will fill the wrong tab's row**, which is the exact `Weight` ambiguity flagged before the scan and is now proven rather than suspected. **Not guessed at a second time**: the package-dimension block needs one more Price/Stock scan with that section scrolled into view before those keys are trusted. `Height (unit)`/`Weight (unit)` companions DO exist in the capture, confirming the `(unit)` syntax fix was right |
| WW-110 | `[P1][L3]` The Product Description tab has never been scanned, so its fields fill by accident or not at all | Feature | ✅ | Vansh's question: *"additional details has form filling, its fields are there — what about product desc ones, the Items Included field and all the other fields we can automate?"* **The engine is not the limit; the calibration is.** `fields.ts` anchors on `EditAttributeItemWrapper` rows and fills whatever tab is in the foreground, so it is already tab-agnostic — but it does **not navigate tabs**, and `fill.ts` says so: *run fill once per tab, it only touches fields it can see*. What is per-tab is the **field list**, and only two tabs have ever been captured: Additional Description (66 attributes, `balloon-decoration.defaults.json`) and Price/Stock/Shipping (21, `.pricing.defaults.json`). **The Product Description tab has no defaults file and has never been through `npm run scan`.** Consequence: any label on it that happens to match a key the product JSON already carries (`Description`, `Key Features`, `Key Spec`, `Search Keywords`) fills when that tab is in front, and everything else is untouched — and nothing anywhere records which tab each of those labels is actually on. **`Items Included` does not exist in this repo at all** — not in a product file, not in a defaults file, not in `PROMPT-product.md` — so it is not "partly automated", it is unmapped. Work: run `scan` on that tab, commit `balloon-decoration.description.defaults.json`, add the genuinely per-product labels to `PROMPT-product.md` so ChatGPT fills them, and leave the shared ones in defaults. **Do it before WW-106 wires the one-click flow**, or step "fill Flipkart" will confidently complete a listing with a whole tab blank. Two open questions land here and should be answered by the same scan: **WW-074** (is Description a `textarea` or a single-line input — if the latter, `el.fill()` strips every newline and the read-back still shows ✅ because it normalises whitespace) and **WW-071** (the real cap on Search Keywords) — **DONE 2026-08-12.** Vansh, having typed the tab by hand once too often: *"you are entering automatically in the price, stock and shipping charge tab and also in the additional description tab, but you are not writing through Flipkart automation in the product description tab… there we have a sensitive field too which is Items Included, which if I go and manually type then it will take me a while."* **The diagnosis above was right and the fix needed no engine change: seven labels, none of which existed anywhere in this repo, so `fill` reported all 90 known labels as "belongs to another tab" and the tab stayed blank.** He supplied the field list off the live form, which is what `scan` would have produced, so the scan was not the blocker after all. **Two of the seven are DERIVED rather than entered, and that is the part worth keeping:** `Model Number` is the `Seller SKU ID` (same string — a second copy is only a place for a typo), and `Items Included` is read straight off the Description's `WHAT YOU GET` lines, which `PROMPT-product.md` has always required. That is the nine-line field he was retyping, and it now costs nobody anything — including on the seven products already written, retroactively. The stop rule is *the first non-blank line that does not start with a digit*, chosen by measuring against all 11 product files: the obvious rule (stop at the next ALL-CAPS heading) swallowed a lower-case `Key Features` in GTB02 and three explanatory paragraphs in GTB-1. `Type`/`Color`/`Size`/`Size in Number` go in a new `balloon-decoration.description.defaults.json`; **`Quantity` deliberately does NOT**, because it is a measured weight in GRAMS and WW-055 is the record of what a wrong weight on a live listing costs at settlement — it is per-product, like the parcel. The seven labels are also appended to `balloon-decoration.json` with a `source` note saying they were typed, not scanned, so the GUI's unmapped warning stops false-accusing them and a future real scan (which merges by label) leaves them alone. **Still worth running `scan` on that tab once** — it is the only thing that can confirm each widget's kind and what the `Type`/`Size` dropdowns actually offer. Found on the way: C-051. **Two follow-ups landed in the same session, both from his answers.** *"the packed parcel, 280 gm"* settled what the `Quantity` box means, and that turns it from data entry into arithmetic: it is `Weight` × 1000, the same measurement `packaging.ts` already computes, so it is derived too and only the products that have actually been weighed get one — blank stays blank rather than inheriting somebody else's weight. And *"can't we have a button for this in the app itself? so that my partner is able to scan too"* — `npm run scan` was the last calibration step locked behind a terminal, which put it out of reach of the person the GUI exists for. **`browser-core.scanTab()` + a *Learn this tab* button next to each Fill button.** The merge and the junk guard moved into `listing.mergeScan()`, shared by the CLI and the button, because that is the code that decides what reaches disk. Pressing it is safe by construction: it only ever ADDS labels, never types into the form, never touches a listing, and refuses a page that is not a form. **And one proposal he rejected, C-052:** `Size in Number` was going to be sourced per-product from the INVENTORY, which names materials as `10 INCH METALLIC BALLOONS` — but *"the 10 inch foil we are folding, the 10 inch balloon are not blown and then packed so they are small"*. That figure is the INFLATED size and nothing ships inflated, so the default 8 was already the measured packed reality and the "fix" would have put a dimension on every listing describing an item not in the parcel. The prompt now carries an explicit *do not correct this from the INVENTORY* rule, because it is a good idea that is wrong and the next reader will have it too. 10 new tests, 251 total | 2026-08-12 |
| WW-109 | `[P0][L3]` Shipping a fix stops being a courier job — auto-update from GitHub Releases | Feature | ✅ | **Vansh: *"this is a long task where I have to download the exe and send it to WhatsApp and then download"*.** It was: Actions artifacts expire in 30 days and **require a GitHub login to download**, so the partner could never fetch one himself — every fix meant Vansh downloading a zip, finding a host WhatsApp accepts (it blocks `.exe`), and talking somebody through SmartScreen again. `electron-updater` + `publish: github` in `electron-builder.yml` replaces all of it: the app checks Releases on launch, downloads in the background, installs on quit. **The release is tag-driven, deliberately** — pushes to `main` still build and still upload artifacts but publish nothing, so most commits never reach the partner, which is the whole point of WW-068's *"fix and test on the Mac, only then push a partner build"*. Bump `version`, tag `v<that>`, push the tag. **The check is silent on every failure** (no releases yet, offline, no access) because an update is a convenience and the app must work exactly as well without one — a dialog in front of somebody trying to list a product is worse than a stale build. **The subtlety worth writing down: `--publish always` uploads the installers *and* `latest.yml`/`latest-mac.yml`, and the app only notices a new version because of those.** Publishing the `.exe` alone produces a release that looks complete and that no installed app ever reacts to — the classic way to conclude auto-update is broken when it was simply never told. Verified locally: `app-update.yml` is now in the built `.app`'s Resources, which is the file that carries the feed location into the package | 2026-08-03 |
| WW-108 | `[P0][L2]` A packaging sweep, so the partner is not re-downloading for gaps we could have found here | Bug | ✅ | Prompted by *"please look for any other gap like this because I can't afford downloading this again and again"* — a full pass over everything the packaged app touches that a Mac dev run cannot exercise. Two real gaps beyond WW-107, both Windows-facing. **(a) WW-061 fixed in code** (see its row): `ensureProfileFree` was `ps`-only, so on the partner's machine it silently did nothing and left the stale-profile "it logged me out" symptom. **(b) No Chrome, no explanation.** `channel: "chrome"` drives the user's own Chrome — which is why the installer is ~120 MB rather than half a gigabyte — but on a machine without it Playwright throws a wall of text about registry keys and executable paths, accurate and useless to the one person this app is for. `friendlyLaunchError()` now rewrites that one class into a sentence naming Chrome, the download URL, and the fact that **Edge will not do**. GUI-SPEC listed this as required before packaging and it had never been built. **Checked and found sound, so nobody re-checks them:** `src/` is bundled into `out/main/` as chunks, so `tsx` is never needed at runtime and no engine file can go missing; `preload`/`index.html` resolve inside `out/`; prompt edits correctly land in `userData` with `canEditShipped: false` when packaged; Playwright's browsers live outside `node_modules` so nothing bloats the installer. **Every path root in `gui/main.ts` is now accounted for by group** — writable state, code, and the read-only shipped data that WW-107 found missing. The remaining Windows unknowns are the ones only a real machine can answer: whether wmic exists on his build (it is deprecated in recent Windows, and the code correctly treats its absence as non-fatal), and SmartScreen | 2026-08-03 |
| WW-107 | `[P0][L2]` The first real `.exe` could not read a single prompt file, and would have filled listings with the defaults silently missing | Bug | ✅ | **Found by the partner double-clicking the first installer — exactly what WW-068 said an early `.exe` was for.** Every prompt panel showed *"couldn't read PROMPT-….md"*. The code was right: `gui/main.ts` reads `process.resourcesPath/docs/guides` when packaged and `../../../docs/guides` in dev, which is the correct split. **`electron-builder.yml` simply never shipped the folder** — `files:` listed `out/**` and `package.json`, so compiled code went in and nothing else did, and there was no `extraResources` block at all. `docs/` also sits *above* `flipkart-autofill/`, so it was never even a candidate. Fixed with `extraResources` (`from: ../docs/guides`, verified — electron-builder does resolve `..` out of the project dir). **The second instance is the one that mattered more, because it fails silently:** `categories/*.defaults.json` holds the shared per-category answers `loadProduct()` merges under every product, and `WW_CATEGORIES_DIR` pointed at `<workspace>/categories` — an empty folder on a fresh install. `loadProduct` guards with `if (fs.existsSync(catDir))`, so a missing folder is not an error; it merges zero defaults and fills a listing with those fields **quietly blank**. Nobody would have seen it until a live listing came out wrong. Categories now ship in resources and are read from there when packaged, because unlike images/meta/products they are **shipped product data, not user state** — they should update with the app, and only `npm run scan` writes there, which is a CLI on Vansh's Mac. **The general lesson, and it is the third variation on WW-094/WW-100:** the app's paths fall into three groups and only two had ever been handled — *writable state* (`WW_*_DIR` → workspace, `profile`/`settings.json`/prompt overrides → `userData`) and *code* (`out/**` in the asar, `sharp` unpacked beside it). The third group, **read-only data the app reads but never generates, was empty in the config and invisible from a Mac**, where both folders resolve inside the repo whether the config ships them or not. Proven by building a real `.dmg` and listing `WishWorks.app/Contents/Resources/` — all 12 `.md` files and both category JSONs present, at exactly the path `main.ts` computes. **A CI gate now fails the build if either is absent**, alongside the existing `sharp`-outside-the-asar check; the two together cover every packaging failure that cannot be reproduced in development | 2026-08-03 |
| WW-106 | `[P0][L4]` **GUI** — one button runs the whole flow, and the report says which of three things happened | Feature | ⬜ | Designed 2026-08-03, spec is **`GUI-SPEC.md` Step 10**; not built. `finish → check → fill Flipkart → fill Meesho → STOP`, on a listing that already has its images and its JSON. **Two decisions settled by Vansh, do not re-litigate:** it **fills and never submits** (a wrong listing live on two marketplaces is slow and public to undo; the pipeline was always generate→validate→review→upload, and Submit stays human), and **Meesho gets the Excel bulk-upload file first**, browser fill later on top of WW-093 — a spreadsheet format changes rarely, a web form constantly, and Flipkart's already needed `inspect.ts` to re-derive selectors once. Field mapping is the same work either way, so nothing is wasted. **The report is the feature, not the automation** — `runFinish`, `runCheck`, `fillListing` and `paste`'s validation are all already options-in/result-out, so chaining them is plumbing; what makes this worth building is that a failure names one of three states instead of saying "it failed". **Blocked** = this step could not run and nothing after it did → stop, name the step, the cause, and the one fix. **Needs a hand** = the flow continued, a human should look afterwards → keep going, collect, show at the end. **Done** = say so once. Every result names **where to resume — the step to re-run, not "start over"**. The line is drawn concretely rather than left to taste. **Blocking:** two files at the same position number (`duplicatePositions` already detects it), no images or no `image-meta`/`products` JSON, not logged in, a required field the JSON has no value for. **Not blocking:** a value over the character limit (`paste` reports which and by how much), a non-1:1 image, a missing per-image description, emoji in a Flipkart field (the app already fixes that itself), a shipping fee that moved after a new main image. **The rule that makes it safe: a step never runs after a blocked one** — half a listing on Flipkart and nothing on Meesho is worse than a clean stop with a reason. New work is the orchestrator, the three-way result type, and the Meesho Excel writer | — |
| WW-098 | `[P0][L3]` Second click-through: four bugs, and a prompt step that was simply missing | Bug | ✅ | **All four found by Vansh in ten minutes of pressing things; none would have been found by a test.** (a) **The normalised ID was being used as a filename.** `listListings()` returned `id: "GTB4"` — a *matching* key from `normalizeId`, where `GTB-4`, `ANP003` and `image-meta-ANP003` all collapse to one string — and the finish step built `2-clean/GTB4` from it. That folder does not exist (`GTB-4` does), so it threw ENOENT; worse, when pointed at another folder it *worked* and named the output `GTB4.1.jpg` in a workspace where every other file says `GTB-4`. A normalised ID is for lookups and must never reach a path. `Listing` now carries `folder` (the real `2-clean` name) and `label` alongside `id`, the picker hands back the whole row rather than a string, and **`finish` passes `id: null` so the engine derives the name from the folder** — which also makes the WW-078 separation structural: the folder decides the NAME, the picker decides the DESCRIPTIONS, and neither can reach the other. (b) **"Bring in N files" did nothing.** `image-meta/` and `products/` do not exist in a fresh workspace — only the image pipeline creates its own folders — so every `copyFile` failed with ENOENT inside an unhandled promise. `mkdir` first. (c) **The Hero image step was wrong three ways**, and Vansh spotted all three: it skipped `PROMPT-read-pack.md` entirely, so the photo prompt (which opens *"use the exact DISPLAYED numbers from **your list**"*) had no list and would invent counts; it said to attach image **1** when the pack sheet is image **2**; and it split hero and infographic into separate steps when they are messages in ONE chat — the infographic prompt reads *"from IMAGE 2"*, already in context. Now a single **Build the images** step with the four messages in order, each copied on its own, never all at once. (d) **`npm run check` was shown as a fallback.** See below. **Also built: `check-core.ts`**, so step 5 is a real button that reads the EXIF description back out of each finished JPEG and says plainly why that is worth doing — Finder and Preview do not display the field at all, so a folder carrying nothing looks identical to a correct one, which is exactly how WW-096 sat undetected for five days. 121 tests + verify + both typechecks green | 2026-07-31 |
| WW-099 | `[P0][L2]` Terminal commands must not appear in the app at all | Bug | ✅ | **"my partner don't have npm in his pc bro why aren't you understand that"** — and he is right, the placeholder panels were a category error. The unbuilt steps printed `npm run check` / `npm start` as a fallback, which for the one person this app exists for is not a fallback but a dead end dressed up as instructions. Every command block is gone. `NotBuilt` now says what the step is for and that it is coming, and nothing else. **The important thing this makes visible: the app does not need Node or npm on the machine it runs on, and never did.** Electron *is* the Node runtime, `sharp` ships as a native binary inside it, and `connect.ts` drives the user's own installed Chrome via `channel: "chrome"` — so Flipkart filling will be a button, and the work left on it is the screen, not the plumbing. The one real external requirement is **Google Chrome must be installed**, which WW-068 already records and which needs a friendly "Chrome not found" screen rather than a stack trace | 2026-07-31 |
| WW-097 | `[P0][L3]` **GUI** — file the AI's downloads automatically (`src/inbox.ts`) | Feature | ✅ | **Vansh's idea, and it closes the last hand-copy in the flow.** The prompts hand back `image-meta-<ID>.json` and `products-<ID>.json` into Downloads; `paste` and `start` read from `image-meta/` and `products/`. Somebody has been closing that gap by hand once per listing, which is exactly where a file lands in the wrong folder — the same class of mistake as WW-077/WW-078, one directory over. `scanInbox()` says what importing would do and changes nothing; `importInbox()` does it. **Two rules make the button safe to press repeatedly.** (a) **The destination is decided by the file's CONTENT, never its name** — a products file has a `values` object, an image-meta file has `meesho`/`images`/`keywords`, and `classify()` returns `null` rather than guessing, because a wrong guess files a listing's Flipkart fields under `image-meta/` where nothing will ever read them and it is silent until somebody reads the output. The prefix is only a hint and a hand-renamed `ANP003.json` has none left. (b) **Nothing already filed is replaced by something older** — an import is `new`, `update` or `older`, and `older` is skipped, so re-running after an edit cannot quietly undo it. The download prefix is dropped on filing (`image-meta-ANP003.json` → `ANP003.json`): it existed only to keep the two downloads apart in ONE folder (WW-080), and in separate folders it is noise that `findById` sees through anyway. Copy by default, `move` optional so Downloads stops piling up; `rename` falls back to `copyFile` across volumes. **13 new tests** (`tests/inbox.test.ts`) pinning both rules plus idempotency. Also lands `src/listings.ts` — every listing this machine knows about, **newest first**, with which halves exist — so no step ever asks anyone to type an ID from memory, which is WW-078's root cause | 2026-07-31 |
| WW-025 | `[P0][L1]` Wire the three maintenance triggers into `CLAUDE.md` | Chore | ⬜ | §7c: post-commit hook, on-compaction, on "new status". Not yet added | — |
| WW-026 | `[P0][L1]` Backfill `docs/tracks/LEVELS.md` | Docs | ⬜ | `docs/tracks/` had no contents before today. Levels above are provisional | — |

---

## Phase 1+ — not started, do not build ahead

| ID | Title | Phase | Status |
|---|---|---|---|
| WW-030 | `[P1][L1]` Combo Generator | P1 | ⬜ |
| WW-031 | `[P0][L6]` Meesho bulk-upload Excel generator | P0 | ⬜ |
| WW-032 | `[P2][L1]` Flipkart price/stock/orders API sync | P2 | ⬜ |
| WW-033 | `[P3][L1]` Intelligence dashboard | P3 | ⬜ — **deprioritised** by the 2026-07-21 reframe |

---

## Where the project actually stands

**Working and proven:** login, scan, fill (Additional Description tab), category defaults,
the image pipeline end-to-end.

**Working but unproven against the real system:** the Price/Stock/Shipping tab (WW-015).
This is the highest-risk open item — it has never touched the live form.

**Blocked on Vansh, all cheap:** WW-021 (send sample images), WW-022 (send the category
template), WW-023 (ask one question). WW-022 in particular would replace every blog-sourced
number with an authoritative one.

**⚠️ Do this one first — WW-055.** A live listing is currently carrying `Net Weight = 10000 g`
from the WW-054 experiment. That test correctly proved declared weight does not move the
*displayed* shipping fee, but the field still governs the *actual* charge: the courier weighs
the parcel at pickup and Meesho charges back the difference at settlement. Revert it to the true
packed weight, and weigh a sealed kit while you're there. This is the only open item that can
cost real money if left alone.

**Level one is finished, 2026-07-31.** The terminal pipeline runs end to end and has been driven
against the live Flipkart form — images in, descriptions embedded, 30 fields filled and read back,
listing saved. 108 tests + `npm run verify` + typecheck green. The week's fixes: WW-066 (engine
callable), WW-080 (one product, any filename), WW-084/86/88 (`paste` became the pre-flight check),
WW-087 (buyer-language SEO into both prompts), WW-094 (profile out of the project folder), WW-074
closed (Description keeps line breaks), WW-095 (that field is 5000 not 1400), WW-096 (**emoji made
every save return HTTP 500**). The last two were only findable by running the final step, which
had never once been run — *a pipeline is proven only as far as the last step actually executed.*

**One thing still open in level one:** `Balloon Type` reads back `Latex` when three values are
sent — single-value field (fix the data) or mis-detected multi-select (fix the code)? Until it is
answered the ⚠️ guard blocks `npm start`'s auto-save every run.

**Next build step: the GUI pivot, WW-066 → WW-069, in that order.** The partner is
non-technical and on Windows; the terminal is the actual blocker to them using any of this.
WW-066's image half landed 2026-07-27 (`images-core.ts`, `finish-core.ts`), which is everything
WW-067's tab 1 needs — **WW-067 is now unblocked and is the next thing to build.** The browser
CLIs are WW-066b and are done per-tab inside WW-069. WW-061 (the Windows session bug) still has
to land before the login tab can be trusted.

**Still on the shelf:** the ANP/GTB prefix presets (WW-027) — one defaults file per kit type so
the prefix on a product ID pre-fills its shared fields across both marketplaces.

**Note on Notion:** the board has **never been built.** `NOTION_BOARD_SEED.md` is the spec to
paste into a Notion-connected Claude when Vansh wants it, and was rewritten 2026-07-26 to match
this file. Nothing in this repo writes to Notion.
