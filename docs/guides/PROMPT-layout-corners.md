Regenerate that same photograph with ONE change: the balloons now sit as TWO diagonal corner
clusters — one at the top-left and one at the bottom-right — instead of a continuous garland,
leaving the middle of the backdrop open so the banner reads clearly.

Everything else is identical — the same items, the same exact counts from the list, the same plain
wall and daylight, the same square 1:1 shape, still nothing in the scene that is not on the list,
still zero text anywhere in the image.
